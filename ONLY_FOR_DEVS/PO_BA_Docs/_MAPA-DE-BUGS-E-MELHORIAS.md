# Mapa de Bugs, Problemas e Pontos de Atenção — Curva Mestra

**Projeto:** Curva Mestra
**Data de Criação:** 15/07/2026
**Última Atualização:** 24/07/2026
**Autor:** uc-issues-tracker (via Claude)
**Fonte:** Casos de Uso UML em `ONLY_FOR_DEVS/PO_BA_Docs/UC-01` a `UC-53` (+ 1 item de processo/infraestrutura não originado de UC, ver Seção 4 — `ADR-QA-AUTOMATION`; + 1 item antes reservado como UC-54, descartado como UC formal e documentado como ADR aprovado, ver Seção 7.1 — `ONLY_FOR_DEVS/TO_DO/ADR-backup-geografico-automatizado.md`, v1.1; UC-51 já foi escrito e aprovado [`UC-51-gerar-relatorios-gerenciais-avancados-e-custeio-por-procedimento.md`, v1.1], depois de dois "UC-51" sucessivos anteriores ("Módulo Procedimentos" e "Investimentos & ROI") terem sido descartados como UC próprio e absorvidos no que é hoje esse UC-51 final, ver 7.1; UC-52 também já foi escrito e aprovado [`UC-52-consultar-projecao-de-reposicao-de-estoque.md`, v1.0], sem renumeração — ver 7.1; UC-53 também já foi escrito e aprovado [`UC-53-consultar-e-exportar-trilha-de-auditoria.md`, v1.0], sem renumeração — ver 7.1)
**Versão:** 3.15

> Backlog técnico consolidado a partir dos achados registrados em cada Caso de Uso mapeado. Serve como mapa de rastreamento para priorizar correções e melhorias — não substitui os UCs individuais, que continuam sendo a fonte de verdade detalhada de cada achado.

---

## 1. Resumo Executivo

| Severidade | Aberto | Em Correção | Corrigido (doc pendente) | Corrigido e Documentado | Descartado | Total |
|---|---|---|---|---|---|---|
| Crítica | 0 | 0 | 0 | 5 | 1 | 6 |
| Alta | 0 | 1 | 0 | 22 | 1 | 24 |
| Média | 36 | 1 | 0 | 1 | 1 | 39 |
| Baixa | 66 | 0 | 0 | 16 | 1 | 83 |
| **Total** | **102** | **2** | **0** | **44** | **4** | **152** |

Além dos 152 itens classificados por severidade, este levantamento também consolida **9 decisões de produto pendentes** (Seção 4 — 8 originadas de UCs + 1 item de processo/infraestrutura, `ADR-QA-AUTOMATION`, sinalizado a ser revisado antes de qualquer nova implementação), **16 itens de código morto/rotas órfãs** (Seção 5) e **12 gaps entre a landing page comercial e o sistema real** (Seção 7 — 4 com decisão de implementar, sendo **UC-51, UC-52 e UC-53 já escritos e aprovados** ([UC-51](UC-51-gerar-relatorios-gerenciais-avancados-e-custeio-por-procedimento.md) v1.1, [UC-52](UC-52-consultar-projecao-de-reposicao-de-estoque.md) v1.0 e [UC-53](UC-53-consultar-e-exportar-trilha-de-auditoria.md) v1.0) e o item antes reservado como **UC-54 agora documentado como ADR aprovado** (`ONLY_FOR_DEVS/TO_DO/ADR-backup-geografico-automatizado.md`, v1.1) em vez de caso de uso formal, por não envolver ator humano nem tela — **com isso, os 6 itens "a implementar" da Seção 7.1 estão 100% documentados**, nenhum resta apenas como número reservado sem nenhum documento; 5 com decisão de corrigir apenas o texto da landing, baixa prioridade; 3 com decisão adiada), todos sem severidade atribuída e sem impacto na tabela acima. Dois itens "UC-51" sucessivos deixaram de ser reserva de UC própria — "Módulo Procedimentos" (v3.9) e "Investimentos & ROI" (v3.10) — ambos descartados/absorvidos no que é hoje o UC-51 final ("Expansão de Relatórios Gerenciais e Custeio por Procedimento"), formalmente escrito e aprovado na v3.11; o UC-52 ("Motor de Recomendação/Projeção de Reposição", redefinido como "Consultar Projeção de Reposição de Estoque"), reservado desde a v3.10 sem nenhuma rodada de descarte, foi formalmente escrito e aprovado na v3.12, sem mudança de numeração; o UC-53 ("Trilha de Auditoria Completa", redefinido como "Consultar e Exportar Trilha de Auditoria"), reservado desde a v3.10 sem nenhuma rodada de descarte, foi formalmente escrito e aprovado na v3.13, sem mudança de numeração; o item antes reservado como UC-54 ("Backup Geográfico Automatizado") foi descartado como UC e documentado como ADR aprovado na v3.14, por ser um processo de infraestrutura agendado sem ator/tela, mesmo perfil do único outro ADR do projeto (ver notas v3.9/v3.10/v3.11/v3.12/v3.13/v3.14 e Seção 7.1 para o detalhamento completo).

> **Nota desta atualização (v3.15, 24/07/2026 — Modo B):** cinco itens de Alta severidade verificados corrigidos em código, todos no commit único `66c75fa` (`fix: cinco itens de alta severidade (UC-21, UC-28, UC-35)`), com os UCs de origem já atualizados pelo `uml-use-case-writer`: `UC-21-RN-03` (e-mail de boas-vindas do admin de clínica deixa de incluir a senha em texto plano, template padrão e `getPreviewEmail()`, com defesa contra reintrodução manual do placeholder — UC-21 v1.2; `requirePasswordChange: true` já garantido desde o RN-05), `UC-28-RN-04` (`generateConsultantWelcomeEmail` perde o parâmetro `tempPassword` — senha removida do e-mail de boas-vindas do consultor — UC-28 v1.1) e `UC-35-RN-04/05/06` (`ProtectedRoute.tsx` passa a ler `maintenance_mode` e redirecionar para `/maintenance`; `POST /api/access-requests` passa a rejeitar com 403 quando `registration_enabled` é falso, ressalva de que apenas o servidor valida, sem checagem espelhada de UX em `/register`, registrada como melhoria futura no próprio UC; `useSessionTimeout.ts` passa a ler `session_timeout_minutes` com fallback de 15 minutos — UC-35 v2.0, **major bump**, pois a premissa central do documento — tela inteiramente desconectada do restante do sistema — deixou de ser verdadeira). Todos os 5 movidos de "Aberto" para "Corrigido e Documentado" diretamente. **Efeito colateral em UC-04:** o mesmo commit também fecha `UC-04-RN-07` (achado cross-referenciado que documentava o hardcode de `session_timeout_minutes` como pré-condição do fluxo de timeout, cross-referenciando UC-35) — UC-04 atualizado para v1.1.4, com RN-07 marcado `[CORRIGIDO]`. **`UC-04-RN-07` nunca teve entrada própria neste mapa** — só existia como nota informativa cross-referenciada dentro do próprio texto de UC-04 (achado distinto de `UC-04-RN-09`, ex-`Q3`, já `Corrigido e Documentado` desde a v3.6, que trata do bypass de `requirePasswordChange`), nunca catalogado como item de bug na Seção 2/3 deste mapa; confirmado nesta execução por releitura de UC-04 e busca no próprio mapa — nenhuma linha nova foi criada nem movida para ele, apenas o estado real foi verificado e registrado aqui. **Divergência de branch verificada antes da atualização:** `fix/uc21-uc28-uc35-alta-prioridade` criada a partir de `develop` logo após o merge de `docs/adr-backup-geografico-automatizado` (branch de docs anterior) — `git status` confirmado sem nenhuma alteração concorrente neste mapa ou no `README.md` nesta sessão; nenhuma reconciliação foi necessária. **Com esta atualização, a coluna "Aberto" da severidade Alta zera** — os 24 itens de Alta severidade da Seção 2 têm todos decisão/status final registrado (22 Corrigido e Documentado, 1 Em Correção — UC-46, aguardando implementação de uma decisão de produto já tomada —, 1 Descartado — UC-14, remoção de funcionalidade). Resumo Executivo (Seção 1) ajustado: Alta — Aberto 5→0, Corrigido e Documentado 17→22 (Total 24, inalterado); Total geral — Aberto 107→102, Corrigido e Documentado 39→44 (Total 152, inalterado). Itens atualizados nas Seções 1, 2 e 6, e no destaque nº 6 da Seção 1. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado com os novos números.

> **Nota desta atualização (v3.14, 24/07/2026 — Modo B):** o item antes reservado como candidato a **UC-54** ("Backup Geográfico Automatizado", último item reservado da Seção 7.1) **não virou um caso de uso formal**. Na elicitação com o usuário, ficou decidido que este item deveria ser documentado como um **ADR** (Architecture Decision Record), não um caso de uso Cockburn — não há ator humano nem tela envolvidos (é um processo de infraestrutura agendado: backup automatizado do Firestore + Cloud Storage via Cloud Scheduler/Cloud Functions), e havia decisões técnicas reais de arquitetura a formalizar (mesmo perfil do único ADR já existente no projeto, `ONLY_FOR_DEVS/TO_DO/ADR-automacao-qa-playwright-firebase-emulator.md`). **Arquivo criado:** `ONLY_FOR_DEVS/TO_DO/ADR-backup-geografico-automatizado.md`, v1.1, Status: Aprovado — decisões de arquitetura fechadas; implementação não priorizada (aguardando janela futura), conforme decisão explícita do usuário de "documentar agora, implementar depois". **Escopo fechado:** `gcloud firestore export` agendado via Cloud Scheduler + Cloud Function, gravando em bucket do Cloud Storage na região `southamerica-west1` (Chile); réplica do bucket `/danfe/{tenantId}/...` via Cloud Storage Transfer Service; lifecycle rule de 90 dias (retenção automatizada); restore mensal híbrido (automação + validação humana); custo recorrente de GCP aceito explicitamente pelo usuário (estimativa US$ 5-20/mês para um Firestore de MVP), fora do padrão "Zero DevOps" do `CLAUDE.md`. Diferente do `ADR-QA-AUTOMATION` (Seção 4, ainda pausado/em aberto), este novo ADR já nasce com todas as decisões técnicas fechadas — por isso não é listado como uma nova linha na Seção 4 (Decisões de Produto Pendentes): não há nenhuma escolha de escopo/comportamento em aberto para o PO decidir, apenas planejamento futuro de implementação, que é responsabilidade do `dev-task-manager`, não deste mapa. **Atualização da linha do UC-54 na Seção 7.1:** removida a numeração "UC-54" da tabela (o item nunca teve arquivo `.md` próprio e não passa a ter — o ADR é o documento formal definitivo para este item) e o status passa de "reservado" para "Descartado como UC — documentado como ADR aprovado". **Com esta atualização, a Seção 7.1 está 100% documentada:** dos 6 itens "a implementar" originais, 5 têm UC formal (UC-51, UC-52, UC-53, além dos dois "UC-51" absorvidos em UC-19/RN-08 e no próprio UC-51 final) e 1 tem ADR formal — nenhum item resta apenas como número reservado sem nenhum documento. Título da subseção 7.1 atualizado para refletir esse marco. **Verificação de código:** não realizada nesta execução — item de natureza puramente documental (ADR aprovado, decisões de arquitetura fechadas), sem correção de código a verificar; nenhuma implementação foi reivindicada pelo usuário, e nenhuma foi presumida (o próprio ADR é explícito: "nenhuma branch deve ser aberta agora"). **Resumo Executivo (Seção 1) sem alteração** na tabela de severidade nem no total de itens da Seção 7 (permanece 12: 4 a implementar/5 correção de landing/3 adiados) — apenas o estado do item ex-UC-54 dentro da subseção "a implementar" mudou de "reservado" para "descartado como UC, documentado como ADR". `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.14 e bullet sobre os gaps da landing page ajustado para destacar que a Seção 7.1 está agora 100% documentada (5 UCs + 1 ADR).

> **Nota desta atualização (v3.13, 22/07/2026 — Modo B):** o UC-53 (reservado desde a v3.10 como "Trilha de Auditoria Completa", sem nenhuma rodada de descarte/renumeração como ocorreu com o UC-51) agora tem um arquivo formal, escrito e aprovado pelo `uml-use-case-writer`: [UC-53](UC-53-consultar-e-exportar-trilha-de-auditoria.md), v1.0, Título final: "Consultar e Exportar Trilha de Auditoria", Status: Aprovado — decisões de design fechadas (escopo: `system_admin` com visão cross-tenant e `clinic_admin` com visão restrita ao próprio tenant consultam e exportam, CSV e PDF, um histórico unificado de escritas administrativas sensíveis — criar/editar/ativar/desativar/suspender/mudar papel/definir senha/excluir — sobre Usuários e Consultores, Clínicas, Produtos Master, Documentos Legais e Configurações Globais, somado ao `inventory_activity` já existente e já gravado hoje mas nunca exposto em nenhuma tela; rastreia apenas escritas, não leituras/visualizações; nova coleção `audit_log` proposta; log explícito adicionado rota a rota, mesmo padrão de `writeActivityLogs`, sem Cloud Function `onWrite` automática; retenção indefinida, sem TTL; trilha não é retroativa, só registra a partir da implementação), ainda que o código em si não tenha sido implementado. **Atualização da linha do UC-53 na Seção 7.1:** o status passa de "reservado" para "UC escrito e aprovado — aguardando priorização/planejamento de implementação pelo `dev-task-manager`", mesmo padrão já usado para o UC-51 na v3.11 e para o UC-52 na v3.12 — não há mais ação de elicitação/redação pendente para este item; a única ação restante é de planejamento e execução de código, fora do escopo deste mapa e do `uml-use-case-writer`. **UC-54 permanece inalterado** — apenas reservado, sem arquivo `.md` próprio, sem nenhuma ação nesta execução. **Verificação de código:** não realizada nesta execução — item de natureza puramente documental (UC aprovado, decisões de design fechadas), sem correção de código a verificar; nenhuma implementação foi reivindicada pelo usuário, e nenhuma foi presumida. **Resumo Executivo (Seção 1) sem alteração** na tabela de severidade nem no total de itens da Seção 7 (permanece 12: 4 a implementar, 5 correção de landing, 3 adiados) — apenas o estado do item UC-53 dentro da subseção "a implementar" mudou de "reservado" para "escrito e aprovado". `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.13 e bullet sobre os gaps da landing page ajustado para citar que UC-51, UC-52 e UC-53 já foram escritos e aprovados (aguardando apenas priorização de implementação), com UC-54 permanecendo apenas reservado; número de UCs mapeados ajustado para "UC-01 a UC-53".

> **Nota desta atualização (v3.12, 21/07/2026 — Modo B):** o UC-52 (reservado desde a v3.10 como "Motor de Recomendação/Projeção de Reposição", sem nenhuma rodada de descarte/renumeração como ocorreu com o UC-51) agora tem um arquivo formal, escrito e aprovado pelo `uml-use-case-writer`: [UC-52](UC-52-consultar-projecao-de-reposicao-de-estoque.md), v1.0, Título final: "Consultar Projeção de Reposição de Estoque", Status: Aprovado — decisões de design fechadas (projeção de data estimada de esgotamento por `codigo_produto`, agregada como em UC-15; taxa de consumo calculada a partir de `Solicitacao concluida`, mesma fonte de UC-47; janela de histórico em cascata 90→60→30 dias com disclaimer de qual janela foi usada, RN-03/RN-04, critério de suficiência de dados ainda a validar tecnicamente, pendência não bloqueante; cálculo sob demanda ao carregar a tela — Dashboard da clínica e Dashboard do Consultor —, sem cron novo, RN-07; Consultor Rennova vinculado vê a projeção completa das clínicas vinculadas, achado confirmado de que a regra genérica `tenants/{tenantId}/{document=**}` já cobre essa leitura sem exigir mudança em `firestore.rules`, RN-08; novo card no Dashboard de ambos os portais + nova tela "Projeções Gerais" em `/clinic/inventory/projections` e `/consultant/clinics/{tenantId}/projections`; "sugestão de quantidade a pedir" — Módulo 05 da landing — explicitamente fora de escopo desta versão, RN-10), ainda que o código em si não tenha sido implementado. **Atualização da linha do UC-52 na Seção 7.1:** o status passa de "reservado" para "UC escrito e aprovado — aguardando priorização/planejamento de implementação pelo `dev-task-manager`", mesmo padrão já usado para o UC-51 na v3.11 — não há mais ação de elicitação/redação pendente para este item; a única ação restante é de planejamento e execução de código, fora do escopo deste mapa e do `uml-use-case-writer`. **UC-53 e UC-54 permanecem inalterados** — apenas reservados, sem arquivo `.md` próprio, sem nenhuma ação nesta execução. **Verificação de código:** não realizada nesta execução — item de natureza puramente documental (UC aprovado, decisões de design fechadas), sem correção de código a verificar; nenhuma implementação foi reivindicada pelo usuário, e nenhuma foi presumida. **Resumo Executivo (Seção 1) sem alteração** na tabela de severidade nem no total de itens da Seção 7 (permanece 12: 4 a implementar, 5 correção de landing, 3 adiados) — apenas o estado do item UC-52 dentro da subseção "a implementar" mudou de "reservado" para "escrito e aprovado". `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.12 e bullet sobre os gaps da landing page ajustado para citar que UC-51 e UC-52 já foram escritos e aprovados (aguardando apenas priorização de implementação), com UC-53 e UC-54 permanecendo apenas reservados; número de UCs mapeados ajustado para "UC-01 a UC-52".

> **Nota desta atualização (v3.11, 20/07/2026 — Modo B):** o UC-51 (reservado desde a v3.8, ocupado e liberado duas vezes — "Módulo Procedimentos" na v3.9, "Investimentos & ROI" na v3.10 — antes de assentar definitivamente como "Expansão de Relatórios Gerenciais e Custeio por Procedimento") agora tem um arquivo formal, escrito e aprovado pelo `uml-use-case-writer`: [UC-51](UC-51-gerar-relatorios-gerenciais-avancados-e-custeio-por-procedimento.md), v1.1, Status: Aprovado — decisões de design fechadas (custo médio ponderado pelo consumo real de cada lote, RN-01/RN-02/RN-03; margem por categoria explicitamente fora de escopo, RN-04; Histórico do Lote restrito a Solicitações `concluida`, RN-05; ator único `clinic_admin`; biblioteca de geração de PDF ainda não decidida, RNF-01, pendência técnica não bloqueante), ainda que o código em si não tenha sido implementado. **Atualização da linha do UC-51 na Seção 7.1:** o status passa de "reservado/a implementar" para "UC escrito e aprovado — aguardando priorização/planejamento de implementação pelo `dev-task-manager`" — não há mais ação de elicitação/redação pendente para este item; a única ação restante é de planejamento e execução de código, fora do escopo deste mapa e do `uml-use-case-writer`. **Nota sobre a dupla reserva/descarte do "UC-51" (notas v3.9/v3.10):** o histórico permanece válido e não foi alterado — os dois "UC-51" anteriores ("Módulo Procedimentos" e "Investimentos & ROI") de fato foram descartados como UCs próprios e tiveram seu conteúdo absorvido dentro do que é hoje o UC-51 final; a diferença nesta versão é que esse UC-51 final, que antes só existia como linha reservada na tabela da Seção 7.1, agora tem conteúdo formal por escrito, aprovado. **UC-52 a UC-54 permanecem inalterados** — apenas reservados, sem arquivo `.md` próprio, sem nenhuma ação nesta execução. **Verificação de código:** não realizada nesta execução — item de natureza puramente documental (UC aprovado, decisões de design fechadas), sem correção de código a verificar; nenhuma implementação foi reivindicada pelo usuário, e nenhuma foi presumida. **Resumo Executivo (Seção 1) sem alteração** na tabela de severidade nem no total de itens da Seção 7 (permanece 12: 4 a implementar, 5 correção de landing, 3 adiados) — apenas o estado do item UC-51 dentro da subseção "a implementar" mudou de "reservado" para "escrito e aprovado". `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.11 e bullet sobre os gaps da landing page ajustado para citar que o UC-51 já foi escrito e aprovado (aguardando apenas priorização de implementação), com UC-52 a UC-54 permanecendo apenas reservados.

> **Nota desta atualização (v3.10, 20/07/2026):** segunda reconciliação da Seção 7.1, desta vez com o que era a reserva "UC-51 (Investimentos & ROI)" — ao investigar para escrever este UC, o `uml-use-case-writer` e o usuário decidiram, em conjunto: **(1) FIFO:** o sistema deve calcular custo médio ponderado por ordem de entrada (FIFO real) quando o mesmo produto tiver múltiplos lotes consumidos num período — lógica de cálculo nova, que não muda a escolha manual de lote em UC-16/UC-17 (o FIFO aqui é apenas o método de agregação do relatório, não uma regra de reserva/consumo de estoque); **(2) Margem por categoria:** decisão de **não implementar** — não existe (e não será criado agora) nenhum campo de valor cobrado do paciente/valor de tabela no sistema; essa parte específica da promessa da landing ("Margem por categoria — HA, toxina, bioestimulador…") foi movida para a Seção 7.2 (correção textual da landing, baixa prioridade, `sections-product.jsx`, Módulo 03); **(3) Fronteira UC-51 vs. o que era UC-52:** o motor de custeio (FIFO ponderado, custo real por procedimento, "ticket médio de custo" por linha de produto — sem margem) não tem tela própria, nem ator/gatilho de UI — é puramente um service consumido por outra tela; seguindo o mesmo precedente já registrado neste mapa para o UC-51 anterior (Módulo Procedimentos, absorvido em UC-19/RN-08 na v3.9), o `uml-use-case-writer` decidiu **não criar um arquivo `UC-51-*.md` próprio para o motor de custeio** — ele passa a ser documentado como Regras de Negócio detalhadas dentro do que hoje é "UC-52 — Expansão de Relatórios Gerenciais"; **(4) Ator confirmado** para o UC de Relatórios + Custeio: `clinic_admin` apenas. **Nenhum arquivo `UC-51-*.md` foi criado** (segunda vez consecutiva que a numeração "UC-51" é reservada e descartada antes de virar arquivo). **Renumeração sequencial sem lacunas aplicada** às reservas seguintes (mesma regra já estabelecida no projeto): UC-52→**UC-51** (Expansão de Relatórios Gerenciais, escopo agora também incluindo o motor de custeio FIFO ponderado, o custo real por procedimento e o "ticket médio de custo" — sem margem — além do "Histórico do lote" já absorvido na v3.9), UC-53→**UC-52** (Motor de Recomendação/Projeção de Reposição), UC-54→**UC-53** (Trilha de Auditoria Completa), UC-55→**UC-54** (Backup Geográfico Automatizado). Nenhum dos 4 tem arquivo `.md` próprio ainda — apenas a numeração da tabela de reserva foi reescrita, sem renomear nenhum arquivo físico (confirmado, por conferência da listagem de arquivos `UC-*.md` existentes, que a nova reserva UC-51 a UC-54 não conflita com nenhum UC já escrito, `UC-01` a `UC-50`). Adicionado à Seção 7.2 o item "Módulo 03 — Investimentos & ROI: remover/ajustar a promessa de 'Margem por categoria (HA, toxina, bioestimulador…)' em `sections-product.jsx`" (landing-copy fix, 4→5 itens nesta subseção). **Sinalização registrada para o `uml-use-case-writer`:** quando o novo UC-51 (ex-UC-52, "Expansão de Relatórios Gerenciais") for de fato escrito/atualizado, deve citar explicitamente que o motor de custeio foi cogitado como UC próprio duas vezes (a mais recente sendo este "UC-51 Investimentos & ROI") e foi absorvido ali por não ter ator/gatilho/tela próprios — mesmo padrão já usado para o UC-51 anterior (Módulo Procedimentos → UC-19/RN-08); e o próprio UC-19 v1.1 (RN-08, sinalizado desde a v3.9 com a numeração antiga "UC-53" para o painel de histórico do lote) precisa ser corrigido para "UC-51" (não mais "UC-52", já que a numeração mudou de novo nesta versão) — este agente não edita arquivos `UC-*.md`. **Resumo Executivo (Seção 1) sem alteração na tabela de severidade** — os 12 itens da Seção 7 permanecem 12 no total, apenas redistribuídos entre as subseções (4 a implementar, antes 5; 5 correção de landing, antes 4; 3 adiados, inalterado); nenhum destes itens tem severidade classificável, mesmo tratamento das versões anteriores. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.10 e bullet sobre os gaps da landing page ajustado para citar a nova numeração UC-51 a UC-54 e o segundo descarte/absorção consecutivo de "UC-51".

> **Nota desta atualização (v3.9, 20/07/2026):** reconciliação da Seção 7.1 após a elicitação real do que era a reserva "UC-51 (Módulo Procedimentos)" — ao investigar para escrever o UC, o `uml-use-case-writer` e o usuário constataram que o "Módulo 02 — Procedimentos" da landing page já é, na prática, a tela `/clinic/requests` (UC-16/UC-17, "Procedimento Programado"/"Procedimento Efetuado"), já existente e em uso; o gap real era muito menor do que o presumido na auditoria da v3.8. Decisões finais tomadas com o usuário: **(a)** "tipo de procedimento categorizado" (full face, mid face, contorno, lábios, glabela) — decisão de **não implementar**, movido de "a implementar" para item de correção de texto da landing (Seção 7.2, baixa prioridade); **(b)** identificador amigável de sessão (`SES-XXXXXX`, não sequencial, derivado do próprio ID do Firestore, sem contador transacional) — decisão já tomada e **documentada como RN-08 em [UC-19](UC-19-concluir-ou-cancelar-procedimento-agendado.md)** (v1.0 → v1.1); implementação de código ainda pendente, mas a decisão e o desenho já estão fechados lá, fora do escopo desta Seção 7; **(c)** painel "Histórico do lote" (cruzamento produto+lote+período, cross-reference de sessões que usaram o mesmo lote) — remanejado para dentro do escopo do que era "UC-53 — Expansão de Relatórios Gerenciais". **Nenhum arquivo `UC-51-*.md` foi criado** — o `uml-use-case-writer` decidiu, por julgamento profissional (escopo final sem ator/gatilho/fluxo próprio), absorver a mudança como RN-08 dentro do UC-19 já existente, em vez de criar um UC novo — mesmo precedente já registrado no projeto de não deixar UCs vagos quando o conteúdo é absorvido por um UC existente. **Renumeração sequencial sem lacunas aplicada** às reservas seguintes (regra já estabelecida no projeto): UC-52→**UC-51** (Investimentos & ROI), UC-53→**UC-52** (Expansão de Relatórios Gerenciais, escopo agora incluindo "Histórico do lote"), UC-54→**UC-53** (Motor de Recomendação/Projeção de Reposição), UC-55→**UC-54** (Trilha de Auditoria Completa), UC-56→**UC-55** (Backup Geográfico Automatizado). Nenhum dos 5 tem arquivo `.md` próprio ainda — apenas a numeração da tabela de reserva foi reescrita, sem renomear nenhum arquivo físico (confirmado, por conferência da listagem de arquivos `UC-*.md` existentes, que a nova reserva UC-51 a UC-55 não conflita com nenhum UC já escrito, `UC-01` a `UC-50`). Novo UC-51 (ex-UC-52, Investimentos & ROI) passa a registrar explicitamente que **não depende mais de um "UC-51 Procedimentos" separado** — a base de dados para custeio continua sendo a `Solicitacao`/UC-16/UC-17 já existente, sem mudança estrutural. Novo UC-52 (ex-UC-53, Expansão de Relatórios Gerenciais) passa a registrar explicitamente que seu escopo **agora também inclui** o painel "Histórico do lote" absorvido do extinto UC-51. Adicionado à Seção 7.2 o item "Módulo 02 — Procedimentos: remover/ajustar a promessa de 'Modelos por tipo (full face, mid face, contorno, lábios, glabela…)' em `sections-product.jsx`" (landing-copy fix, 3→4 itens nesta subseção). **Sinalização registrada para o `uml-use-case-writer`:** o próprio [UC-19](UC-19-concluir-ou-cancelar-procedimento-agendado.md) v1.1, na "Observação" do RN-08, ainda cita a numeração antiga ("remanejado para UC-53") para o painel de histórico do lote — precisa ser atualizado para "UC-52" nesta próxima revisão do UC (ver seção de sinalização ao final desta execução; este agente não edita arquivos `UC-*.md`). **Resumo Executivo (Seção 1) sem alteração na tabela de severidade** — os 12 itens de Seção 7 permanecem 12 no total, apenas redistribuídos entre as subseções (5 a implementar, antes 6; 4 correção de landing, antes 3; 3 adiados, inalterado); nenhum destes itens tem severidade classificável, mesmo tratamento das versões anteriores. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.9 e bullet sobre os gaps da landing page ajustado para citar a nova numeração UC-51 a UC-55 e o descarte/absorção do antigo UC-51.

> **Nota desta atualização (v3.8, 20/07/2026):** criada a nova **Seção 7 — "Gaps: Landing Page vs. Sistema Real — Decisões de Produto"** (a antiga Seção 7, "Histórico de Versões", foi renumerada para Seção 8), consolidando uma auditoria — realizada e decidida com o usuário fora do fluxo usual `UC → mapa` — comparando as promessas comerciais da landing page (`public/landing/sections-hero.jsx`, `sections-product.jsx`, `sections-personas.jsx`, `sections-trust.jsx`, `sections-cta.jsx`) contra o comportamento real do sistema, verificado previamente por leitura direta do código. **Nenhum destes 12 itens é originado de um UC existente** — diferente de todos os achados das Seções 2 a 6, que rastreiam de volta a uma Seção/RN específica de um UC já escrito; por isso ficam isolados numa seção própria, e a Seção 1 (tabela de severidade) permanece inalterada, mesmo tratamento já dado às decisões de produto pendentes (Seção 4) e ao código morto (Seção 5). Detalhamento: (7.1) **6 itens com decisão de implementar**, numeração de UC reservada de forma sequencial e sem lacunas a partir do maior UC existente (UC-50) — **UC-51** (Módulo Procedimentos: tipos clínicos fixos, baixa automática por procedimento, identificador anônimo de sessão), **UC-52** (Módulo Investimentos & ROI: custeio FIFO por lote, custo direto por procedimento, ticket médio, margem por categoria — depende conceitualmente de UC-51 existir primeiro), **UC-53** (Expansão de Relatórios Gerenciais: PDFs executivos, fechamento mensal de 1 página, mix de produtos por trimestre), **UC-54** (Motor de Recomendação/Projeção de Reposição), **UC-55** (Trilha de Auditoria Completa: rastreamento de leitura + exportação) e **UC-56** (Backup Geográfico Automatizado — sinalizado como trabalho de infraestrutura/DevOps, fora do padrão usual de bugfix/feature de UI). **Nenhum dos 6 UCs foi escrito nesta execução** — apenas o número foi reservado; a redação fully-dressed (Cockburn) pelo `uml-use-case-writer` e o planejamento de implementação pelo `dev-task-manager` ficam para quando o usuário decidir priorizar cada um (nenhum dos dois agentes foi acionado). (7.2) **3 itens com decisão de corrigir apenas o texto da landing page** (baixa prioridade, tarefa de copy, sem UC): alertas de vencimento "60/30/15 dias" (sistema real usa um único threshold configurável, `expiry_warning_days`); multi-unidade/multi-sede (conceito inexistente no modelo de dados, `Tenant`); onboarding "via planilha" (contradiz a decisão de produto já registrada no `CLAUDE.md` — importação é exclusivamente via XML NF-e SEFAZ v4.00). (7.3) **3 itens com decisão adiada**, registrados apenas como pendência em aberto, sem UC e sem correção de texto: alegações específicas de criptografia (rotação trimestral de chaves, restore mensal testado); app nativo iOS/Android via Capacitor + modo offline 24h; integrações/API REST documentada + conectores nativos. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: número de versão/data e um novo bullet informativo sobre os 12 gaps da landing page — a tabela de severidade em si não mudou, portanto não houve alteração dos seus números.

> **Nota desta auditoria (v3.7, 20/07/2026 — Modo A, reconstrução completa a partir da releitura dos 50 UCs):** pedida explicitamente pelo usuário após um merge de duas branches divergentes — `fix/uc09-uc11-uc42-alta-prioridade` (aberta a partir de `develop`, antes da PR #237 ser mesclada em `gscandelari_setup`; commits `16877f1` `fix: tres itens de alta severidade (UC-09, UC-11, UC-42)` e `182c973` `docs: atualiza UC-09, UC-11 e UC-42 e sincroniza mapa de bugs (v3.6)`) e `gscandelari_setup` (já contendo a v3.6 deste mapa, com as correções de UC-32/UC-04/UC-39 da PR #237). O merge (`git merge origin/gscandelari_setup` na branch de fix) foi resolvido com `git checkout --theirs` como placeholder tanto para este mapa quanto para o `README.md`, deixando ambos temporariamente refletindo **apenas** o lado `gscandelari_setup` — sem as 4 correções feitas em `fix/uc09-uc11-uc42-alta-prioridade` — mesmo padrão de "branches divergentes" já visto e reconciliado neste mapa (UC-22 v2.8→v2.9; UC-29/UC-34 v1.9→v2.1; UC-36 v2.2→v2.3; UC-40/UC-44 v3.3→v3.4; UC-21/UC-13 v3.4→v3.5). Todos os 50 UCs foram relidos nesta auditoria: cabeçalho Status/Versão de cada um conferido via `grep` direto (12 dos 50 não "Aprovado", número inalterado — ver nota abaixo), e cada arquivo com marcador `CORRIGIDO`/`RESOLVIDO`/`[Corrigido]`/`[Resolvido]` (23 arquivos) lido/grepado por completo e comparado contra o que já constava neste mapa. **Divergência confirmada, restrita a 4 itens, todos do commit único `16877f1`** (já documentados pelo `uml-use-case-writer` nos UCs de origem, commit `182c973`): `UC-09-RN-02` e `UC-09-RN-03` (critério de "documento pendente" divergente entre `usePendingTerms`/`TermsInterceptor` e as duas páginas de aceite — unificado, eliminando o loop de redirecionamento dos antigos Fluxos de Exceção 8a/8b, agora `[Corrigido]`; UC-09 atualizado para v1.4), `UC-11-RN-07` (gravação não atômica de `nf_imports` + itens de inventário, com `status: "success"` fixado antes da escrita — `handleSaveNF` passou a usar `writeBatch`, mesmo padrão de UC-10; UC-11 atualizado para v1.1) e `UC-42-RN-02` (`checkExpiredProducts` nunca persistia a notificação de produto vencido, placeholder substituído pelo novo helper `createExpiredProductNotification`; UC-42 atualizado para v1.3) — todos os 4 verificados por leitura direta do texto de cada UC (trechos de código citados nas próprias Seções 6/9/14, com referência ao commit `16877f1`) nesta execução, e movidos de "Aberto" para "Corrigido e Documentado". **Nenhuma outra divergência de conteúdo foi encontrada** nos 50 UCs — em particular, os itens já corrigidos pela PR #237 (`UC-32-RN-01`, `UC-04-RN-09`, `UC-39-RN-02`) já estavam corretamente refletidos no placeholder v3.6 mantido pelo merge, sem necessidade de nova alteração; as pequenas variações adicionais de versão observadas nos cabeçalhos (`UC-29` v1.2.1→v1.2.2, `UC-36` v1.1→v1.1.1) correspondem à formalização, já feita pelo `uml-use-case-writer`, do achado compartilhado `UC-29-RN-09 / UC-36-RN-10` (tela de login não traduz `auth/user-disabled`) — que já estava rastreado neste mapa como item Média/Aberto na Seção 3 (Autenticação/Acesso) antes desta auditoria, sem mudança de status ou de conteúdo do achado em si; e as revisões pontuais de cross-reference em `UC-01` (v2.0.1), `UC-05` (v2.2), `UC-15` (v1.0.1) e `UC-23` (v1.1) são apenas atualizações de referências cruzadas entre UCs já mapeados, sem nenhum achado novo ou correção de bug. **Verificação aritmética completa desta auditoria** (contagem direta de todas as linhas das Seções 2 a 5, comparada com os totais da Seção 1): Seção 2 — Crítica: 1 Descartado + 5 Corrigido e Documentado = 6; Alta: 5 Aberto + 1 Em Correção + 17 Corrigido e Documentado + 1 Descartado = 24 (24 linhas na tabela, conferido uma a uma); Seção 3 — Média: 36 Aberto + 1 Em Correção + 1 Corrigido e Documentado + 1 Descartado = 39 (39 linhas, recontadas por subseção/módulo); Baixa: 66 Aberto + 16 Corrigido e Documentado + 1 Descartado = 83 (83 linhas, recontadas por subseção/módulo); Total geral 6+24+39+83 = 152, batendo com o Total da Seção 1; Seção 4 com 9 linhas (8 UC + 1 ADR), batendo com a nota do cabeçalho; Seção 5 com 16 itens de código morto/rotas órfãs, batendo com a nota do cabeçalho. **Nenhuma divergência aritmética encontrada.** Resumo Executivo (Seção 1) ajustado a partir da v3.6: Alta — Aberto 9→5, Corrigido e Documentado 13→17 (Total 24, inalterado); Total geral — Aberto 111→107, Corrigido e Documentado 35→39 (Total 152, inalterado). Itens atualizados nas Seções 1, 2 e 6. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado com os novos números.

> **Nota desta atualização (v3.6, 20/07/2026 — Modo B):** três itens de Alta severidade verificados corrigidos em código, todos no commit único `f6e9161` (`fix: tres itens de alta severidade (UC-32, UC-04, UC-39)`), com os UCs de origem já atualizados pelo `uml-use-case-writer`: `UC-32-RN-01` (`handleSubmit` em `admin/products/[id]/page.tsx` passa a calcular `fragmentacaoAlterada` e só reenvia `fragmentavel`/`unidades_por_embalagem` quando esses campos realmente mudaram — UC-32 v1.0.3), `UC-04-RN-09` — ex-`Q3`, renumerado de pergunta em aberto para Regra de Negócio (`ProtectedRoute.checkAccess` passa a verificar `claims.requirePasswordChange === true` e redirecionar para `/change-password`, fechando o bypass para sessões já autenticadas navegando direto — UC-04 v1.1.3; a sub-parte de "clínica suspensa" do achado original **não foi corrigida por não ser mais um gap real** — já coberta por `SuspensionInterceptor.tsx`, confirmado nesta execução, não presumido) e `UC-39-RN-02` (decisão do PO pela opção "a" — texto do diálogo "Adicionar Novo Usuário" corrigido para não mais prometer envio de credenciais por e-mail, em vez de implementar esse envio por questão de segurança — UC-39 v1.2.1; decisão pendente `UC-39-Decisão` removida da Seção 4 por estar resolvida). Todos os três movidos de "Aberto" para "Corrigido e Documentado" diretamente. **Divergência verificada antes da atualização:** branch `fix/uc32-uc04-uc39-alta-prioridade` criada a partir de `develop` imediatamente após o sync anterior — `git diff develop f6e9161 -- _MAPA-DE-BUGS-E-MELHORIAS.md` não retornou diferenças; nenhuma reconciliação foi necessária. Resumo Executivo (Seção 1) ajustado: Alta — Aberto 12→9, Corrigido e Documentado 10→13 (Total 24, inalterado); Total geral — Aberto 114→111, Corrigido e Documentado 32→35 (Total 152, inalterado). Decisões de produto pendentes 10→9 (8 UC + 1 ADR). Itens atualizados nas Seções 1, 2 e 6. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado com os novos números.

> **Nota desta auditoria (v3.5, 19/07/2026 — Modo A, reconstrução completa a partir da releitura dos 50 UCs):** pedida explicitamente pelo usuário logo após o merge de duas branches que haviam atualizado `_MAPA-DE-BUGS-E-MELHORIAS.md` de forma independente a partir da mesma base **v3.2** — uma branch (`fix/uc02-access-request-approve-auth`, PR #231) corrigiu `UC-02`/`UC-03` e chegou à v3.3 mantida pelo merge como placeholder; a outra (`fix/uc40-users-list-rule`) corrigiu `UC-40`/`UC-44` e, em paralelo, chegou a uma v3.3 diferente (commit `339fa86`), já reconciliada na auditoria anterior (v3.4). O usuário sinalizou que **uma terceira branch**, com o commit `91eb37e` (`fix: dois itens de alta severidade (UC-21, UC-13)`), corrigiu `UC-21-RN-05` e `UC-13-RN-07` e também não estava refletida no placeholder — mesmo padrão de reconciliação já usado neste mapa para UC-22 (v2.8→v2.9) e para UC-40/UC-44 (v3.3→v3.4). Todos os 50 UCs foram relidos nesta auditoria (cabeçalho Status/Versão de cada um conferido via leitura direta; histórico de versões de todo UC com versão acima de `1.0`/`x.0` — 25 arquivos — lido por completo; achados com marcador `[CORRIGIDO]`/`[RESOLVIDO]`/`[Corrigido]`/`[Resolvido]` buscados nos 50 arquivos e comparados um a um contra o que já constava neste mapa) — **nenhuma divergência de conteúdo além de UC-21/UC-13** foi encontrada; em particular, `UC-02` (v2.1), `UC-03` (v1.3), `UC-40` (v1.1) e `UC-44` (v1.1) já estavam corretamente refletidos no placeholder mantido pelo merge, sem necessidade de nova alteração. Dois itens de Alta severidade, ambos do commit único `91eb37e`, confirmado via `git log -1 --stat 91eb37e` nesta execução (toca `src/app/(clinic)/clinic/inventory/[id]/page.tsx` e `src/app/api/tenants/create/route.ts`) e já documentados pelo `uml-use-case-writer` (UC-21 v1.1, UC-13 v1.1), movidos de "Aberto" para "Corrigido e Documentado": `UC-21-RN-05` (senha inicial do admin de clínica passa a exigir troca obrigatória no primeiro acesso — `requirePasswordChange: true`) e `UC-13-RN-07` (falha na verificação de reservas ativas antes de desativar item de estoque deixa de ser fail-open e passa a bloquear a desativação) — detalhes completos na Seção 2 e na nova entrada da Seção 6. **Verificação aritmética desta auditoria:** contagem direta das linhas das Seções 2 a 5 confere com os totais desta tabela (Crítica: 1 Descartado + 5 Corrigido e Documentado = 6; Alta: 12 Aberto + 1 Em Correção + 10 Corrigido e Documentado + 1 Descartado = 24; Média: 36 Aberto + 1 Em Correção + 1 Corrigido e Documentado + 1 Descartado = 39; Baixa: 66 Aberto + 16 Corrigido e Documentado + 1 Descartado = 83; Total geral 152; Seção 4 com 10 linhas — 9 UC + 1 ADR; Seção 5 com 16 itens de código morto/rotas órfãs). Resumo Executivo ajustado a partir da v3.4: Alta — Aberto 14→12, Corrigido e Documentado 8→10 (Total 24, inalterado); Total geral — Aberto 116→114, Corrigido e Documentado 30→32 (Total 152, inalterado).

> **Nota desta auditoria (v3.4, 19/07/2026 — Modo A, reconstrução completa a partir da releitura dos 50 UCs):** pedida explicitamente pelo usuário após o merge de duas branches que haviam atualizado `_MAPA-DE-BUGS-E-MELHORIAS.md` de forma independente a partir da mesma base **v3.2** — uma branch (`fix/uc02-access-request-approve-auth`, PR #231) corrigiu `UC-02`/`UC-03` e chegou à sua própria v3.3; a outra (`fix/uc40-users-list-rule`) corrigiu `UC-40`/`UC-44` e, em paralelo, chegou a uma v3.3 **diferente** (commit `339fa86`). O merge (`ad5e692`) manteve como placeholder a versão vinda de `gscandelari_setup` (a v3.3 com o fix de UC-02/UC-03 já refletido), que ficou **desatualizada especificamente para UC-40/UC-44** — mesmo padrão de reconciliação já usado neste mapa para UC-22 (v2.8→v2.9) e para o par UC-29/UC-34 (v1.9→v2.1). Todos os 50 UCs foram relidos nesta auditoria (cabeçalho Status/Versão de todos os 50 conferido via leitura direta; achados com marcador `[CORRIGIDO]`/`[RESOLVIDO]` de todos os 50 localizados e comparados um a um contra o que já constava neste mapa) — **nenhuma divergência de conteúdo além de UC-40/UC-44** foi encontrada; em particular, `UC-02` (v2.1) e `UC-03` (v1.3) já estavam corretamente refletidos no placeholder mantido pelo merge, sem necessidade de nova alteração. Três itens de Alta severidade, todos do commit único `eb12c91` (`fix: dois itens de alta severidade (UC-40, UC-44)`, confirmado via `git log`/`git show` nesta execução — toca `firestore.rules` e `NotificationBell.tsx`) e já documentados pelo `uml-use-case-writer` (UC-40 v1.1, UC-44 v1.1), movidos de "Aberto" para "Corrigido e Documentado": `UC-40-RN-04`, `UC-44-RN-01`, `UC-44-RN-02` — detalhes completos na Seção 2 e na nova entrada da Seção 6. **Verificação aritmética desta auditoria:** contagem direta das linhas das Seções 2 a 5 confere com os totais desta tabela (Crítica: 1 Descartado + 5 Corrigido e Documentado = 6; Alta: 14 Aberto + 1 Em Correção + 8 Corrigido e Documentado + 1 Descartado = 24; Média: 36 Aberto + 1 Em Correção + 1 Corrigido e Documentado + 1 Descartado = 39; Baixa: 66 Aberto + 16 Corrigido e Documentado + 1 Descartado = 83; Total geral 152; Seção 4 com 10 linhas — 9 UC + 1 ADR; Seção 5 com 16 itens de código morto/rotas órfãs). Resumo Executivo ajustado a partir da v3.3 mantida pelo merge: Alta — Aberto 17→14, Corrigido e Documentado 5→8 (Total 24, inalterado); Total geral — Aberto 119→116, Corrigido e Documentado 27→30 (Total 152, inalterado).

> **Nota desta atualização (v3.3, 19/07/2026):** correção de segurança verificada em código: `UC-02-RNF-01` (Alta) movido de "Aberto" para "Corrigido e Documentado" — `POST /api/access-requests/[id]/approve` não tinha nenhuma verificação de autenticação (qualquer requisição HTTP não autenticada podia criar `tenant` + usuário `clinic_admin` reais, e `approved_by_uid`/`approved_by_name` vinham livremente do corpo da requisição); corrigido exigindo `Authorization: Bearer {idToken}` + `adminAuth.verifyIdToken` + checagem de `is_system_admin` (401/403), commit `33c5ef3`. `uml-use-case-writer` já atualizou UC-02 para v2.1. Durante a investigação desta correção, foi descoberto e corrigido no mesmo commit um achado gêmeo na rota irmã de rejeição, `POST /api/access-requests/[id]/reject` (UC-03) — mesmo padrão já usado neste mapa para achados nascidos e corrigidos no mesmo ciclo (`UC-29-RN-07`, `UC-22-RN-02`/`UC-22-RNF-05`, `UC-32-RN-06b`): nunca esteve "Aberto" neste mapa, registrado como `UC-03-RNF-04` apenas na Seção 6, **sem impacto nos contadores da Seção 1** (severidade classificada como Média — o vetor real de exploração pela UI nunca existiu, já que a rejeição usada pela tela é inteiramente client-side e protegida pela regra do Firestore `isSystemAdmin()`, mas a rota de API em si era diretamente explorável, pois o Admin SDK bypassa completamente as regras do Firestore). `uml-use-case-writer` já atualizou UC-03 para v1.3 (nova RNF-04, já nascendo `[CORRIGIDO]`). Resumo Executivo ajustado: Alta — Aberto 18→17, Corrigido e Documentado 4→5 (Total 24, inalterado); Total geral — Aberto 120→119, Corrigido e Documentado 26→27 (Total 152, inalterado).

> **Nota desta atualização (v3.2, 18/07/2026):** dois itens de Alta severidade (UC-50-RN-03, UC-39-RN-03) confirmados corrigidos em código no commit `467f462` e já documentados pelo `uml-use-case-writer` (UC-50 v1.1, UC-39 v1.2) — movidos para "Corrigido e Documentado". Um terceiro achado (UC-46-RN-03/RN-04) teve **decisão de produto tomada** (remover `/clinic/consultant/transfer` e substituir por um fluxo novo de convite/transferência, spec completa em `ONLY_FOR_DEVS/TO_DO/FEAT-unificacao-vinculo-transferencia-consultor.md` v1.1), mas **o código ainda não foi alterado** — tratado como "Em Correção" (não "Corrigido"), e a linha `UC-46-Decisão` foi removida da Seção 4 por não ser mais uma decisão em aberto, e sim uma implementação pendente de uma decisão já tomada (ver Seção 6).

> **Nota desta auditoria (v2.9, 18/07/2026):** esta versão é o resultado de uma reconstrução completa do mapa a partir da releitura dos 50 UCs, motivada por um merge de duas branches que haviam atualizado `_MAPA-DE-BUGS-E-MELHORIAS.md` de forma independente a partir da mesma base (v2.8) — a versão mantida como placeholder no merge estava desatualizada especificamente para **UC-22**, que teve **duas rodadas inteiras de correção** (v2.0, commit `90b8a6f`, e v2.0.1, commit `529adc3`) não refletidas neste mapa até agora. Todos os demais 49 UCs foram reconferidos e não apresentaram nenhuma divergência de conteúdo em relação ao que já estava registrado aqui. **Correção adicional de consistência encontrada nesta auditoria, sem relação com o merge:** a contagem de "decisões de produto pendentes" citada no cabeçalho e nesta nota vinha sendo carregada como um delta acumulado entre versões (12→13→14 ao longo do Histórico de Versões — hoje Seção 8, à época desta nota ainda Seção 7) sem recontagem das linhas reais da tabela da Seção 4; a contagem correta, por contagem direta das linhas existentes nesta versão, é **11** (10 UCs + 1 ADR) — corrigida aqui. Detalhes em [Seção 6](#6-itens-resolvidos) e no Histórico de Versões (hoje Seção 8).

**Nota sobre Status dos UCs de origem:** 12 dos 53 UCs mapeados não estão com status "Aprovado" — são, em si, um sinal de pendência: **UC-05, UC-39 e UC-40** estão "Em Revisão" (achados relevantes ainda sem decisão de produto); **UC-42, UC-43, UC-44, UC-45, UC-46, UC-47, UC-48, UC-49 e UC-50** estão em "Rascunho" (módulos de Notificações, Onboarding, Portal do Consultor e Inventário, investigados mas ainda não formalmente aprovados). **UC-51, UC-52 e UC-53** (novos, Seção 7.1) já nascem com status "Aprovado" — não entram nesta contagem de pendência.

### Destaques (itens mais urgentes)

1. ~~**[UC-36-RN-02 / UC-36-RN-03 — Crítica]** Editar "Função" ou "Status" de um usuário de clínica no Portal Admin (`admin/users`) **não tem nenhum efeito real de acesso** — não sincroniza custom claims nem desabilita a conta no Firebase Auth. Um System Admin pode acreditar que rebaixou ou desativou um usuário e nada muda de fato.~~ **[CORRIGIDO E DOCUMENTADO 17/07/2026]** Nova rota `PUT /api/users/[id]` sincroniza custom claims (`role`/`active`) e desabilita a conta via `adminAuth.updateUser({disabled})`, com checagem de "último clinic_admin ativo" (commits `2c7b023`, `e0f0e74`, `69bc944`); UC-36 atualizado para v1.1 — ver Seção 6.
2. ~~**[UC-29-RN-01 — Crítica]** "Suspender" um Consultor Rennova (`admin/consultants`) é puramente cosmético — não bloqueia login nem revoga acesso às clínicas vinculadas.~~ **[CORRIGIDO E DOCUMENTADO 16/07/2026]** Botão "Suspender" reconectado à rota `DELETE`, com correção de claims e restauração simétrica na reativação (commits `b08fdeb`, `dbf128f`, `d167883`); achado adicional RN-07 (filtro "Suspensos") corrigido no mesmo ciclo (commit `3ab00b6`). UC-29 atualizado pelo `uml-use-case-writer` para v1.2.1 — ver Seção 6.
3. ~~**[UC-43-RN-01/02 — Crítica]** A tela de Preferências de Notificação (`clinic/settings`) usa `updateDoc` em vez de `setDoc` para inicializar configurações — pode estar **permanentemente quebrada** para qualquer tenant novo, bloqueando também UC-42 (verificação de alertas).~~ **[CORRIGIDO E DOCUMENTADO 16/07/2026]** `updateDoc` substituído por `setDoc` em `notificationService.ts` (commit `a38e581`); UC-43 atualizado pelo `uml-use-case-writer` para v1.2.1 — ver Seção 6.
4. ~~**[UC-14-RN-05 — Crítica]** A ferramenta de Auditoria de Inventário (`clinic/inventory/audit`) tem uma condição de corrida real entre leitura e escrita, sem transação — pode sobrescrever silenciosamente reservas de estoque legítimas.~~ **[DESCARTADO 15/07/2026]** Decisão de produto: a ferramenta inteira será removida, não corrigida — ver Seção 6.
5. ~~**[UC-34-RN-03 — Crítica]** Exclusão de Documento Legal é definitiva (hard delete) e não verifica se o documento já foi aceito por usuários — compromete a trilha de auditoria de compliance.~~ **[CORRIGIDO E DOCUMENTADO 16/07/2026]** `handleDelete` em `admin/legal-documents/page.tsx` agora consulta `user_document_acceptances` por `document_id` antes do `deleteDoc` e bloqueia a exclusão com mensagem explicativa se houver qualquer aceite registrado (commit `4561a2a`); UC-34 atualizado para v1.0.1 e UC-09 para v1.3.1 (referência cruzada) — ver Seção 6.
6. ~~**[UC-35-RN-04/05/06 — Alta]** A tela "Configurações do Sistema" (`admin/settings`) é uma funcionalidade inteira desconectada: modo de manutenção, permissão de registro e tempo de sessão não têm nenhum efeito real no restante da plataforma (achados também refletidos em UC-01 e UC-04).~~ **[CORRIGIDO E DOCUMENTADO 24/07/2026]** `ProtectedRoute.tsx` agora lê `maintenance_mode` e redireciona não-admins para a nova tela `/maintenance`; `POST /api/access-requests` bloqueia (403) novas solicitações quando `registration_enabled` é falso; `useSessionTimeout.ts` passa a ler `session_timeout_minutes`, com fallback de 15 minutos (commit `66c75fa`). UC-35 atualizado para v2.0 (major bump) e UC-04 para v1.1.4 (achado cross-referenciado RN-07 marcado `[CORRIGIDO]`) — ver Seção 6. **Com este item, não restam itens Abertos de severidade Crítica ou Alta neste mapa.**

---

## 2. Itens Críticos e de Alta Severidade

| ID | UC de Origem | Categoria | Severidade | Resumo | Status | Módulo |
|---|---|---|---|---|---|---|
| UC-14-RN-05 | [UC-14](UC-14-auditar-e-corrigir-inconsistencias-de-inventario.md) | Bug funcional | Crítica | Condição de corrida entre a leitura da auditoria e a escrita da correção de inventário, sem transação — pode sobrescrever silenciosamente reservas legítimas feitas no intervalo. | Descartado (remover funcionalidade) | Inventário |
| UC-29-RN-01 | [UC-29](UC-29-editar-suspender-e-reativar-consultor.md) | Achado de segurança | Crítica | "Suspender" um consultor não bloqueia login nem revoga acesso às clínicas vinculadas — é puramente cosmético no Firestore, sem tocar Auth/claims. | Corrigido e Documentado (16/07/2026) | Admin — Consultores |
| UC-34-RN-03 | [UC-34](UC-34-editar-publicar-e-excluir-documento-legal.md) | Débito técnico / Compliance | Crítica | Exclusão definitiva (hard delete) de documento legal sem checar `user_document_acceptances` — compromete a trilha de auditoria de compliance. | Corrigido e Documentado (16/07/2026) | Admin — Documentos Legais |
| UC-36-RN-02 | [UC-36](UC-36-editar-usuario-e-alterar-status-cross-tenant.md) | Achado de segurança | Crítica | Editar "Função" de um usuário de clínica não sincroniza custom claims — mudança puramente cosmética no Firestore, sem efeito real de permissões. | Corrigido e Documentado (17/07/2026) | Admin — Usuários |
| UC-36-RN-03 | [UC-36](UC-36-editar-usuario-e-alterar-status-cross-tenant.md) | Achado de segurança | Crítica | Definir "Status: Inativo" não desabilita a conta no Firebase Auth nem existe rota alternativa correta — usuário "desativado" continua acessando normalmente. | Corrigido e Documentado (17/07/2026) | Admin — Usuários |
| UC-43-RN-01/02 | [UC-43](UC-43-configurar-preferencias-de-notificacao.md) | Bug funcional | Crítica | `initializeNotificationSettings` e o fallback de salvamento usam `updateDoc` em vez de `setDoc` — a tela pode estar permanentemente quebrada para qualquer tenant cujo documento de configurações nunca foi criado. | Corrigido e Documentado (16/07/2026) | Notificações e Alertas |
| UC-02-RNF-01 | [UC-02](UC-02-aprovar-solicitacao-de-acesso.md) | Achado de segurança | Alta | ~~A rota de aprovação de solicitação de acesso não valida Bearer token — depende apenas do controle client-side do layout.~~ **[CORRIGIDO E DOCUMENTADO 19/07/2026]** `POST /api/access-requests/[id]/approve` agora exige `Authorization: Bearer {idToken}`, verificado via `adminAuth.verifyIdToken`, com checagem de `is_system_admin` (401/403); `approved_by_uid`/`approved_by_name` passam a vir do token verificado, não mais do corpo da requisição (commit `33c5ef3`); UC-02 atualizado para v2.1 — ver Seção 6. | Corrigido e Documentado (19/07/2026) | Admin — Acesso |
| UC-04-RN-09 (ex-Q3) | [UC-04](UC-04-fazer-login-com-redirecionamento-por-papel.md) | Achado de segurança | Alta | ~~`ProtectedRoute` não verifica `requirePasswordChange` nem status de clínica suspensa para uma sessão já autenticada navegando direto — bypass da troca de senha obrigatória e do bloqueio de clínica suspensa.~~ **[CORRIGIDO E DOCUMENTADO 20/07/2026]** `ProtectedRoute.checkAccess` agora verifica `claims.requirePasswordChange === true` e redireciona para `/change-password` (mais um guard de renderização equivalente), fechando o bypass para sessões já autenticadas navegando direto (commit `f6e9161`). A sub-parte de "clínica suspensa" do achado original **não exigiu correção de código**: já estava coberta por `SuspensionInterceptor.tsx` — confirmado durante a investigação e registrado como constatação, não como fix novo. UC-04 atualizado para v1.1.3 (nova RN-09) — ver Seção 6. | Corrigido e Documentado (20/07/2026) | Autenticação |
| UC-09-RN-02 | [UC-09](UC-09-aceitar-termos-legais.md) | Bug funcional | Alta | ~~Critério de "pendente" diverge entre `TermsInterceptor` (por versão) e as telas de aceite (qualquer versão aceita) — gera loop de redirecionamento ao reeditar um documento legal já aceito.~~ **[CORRIGIDO E DOCUMENTADO 20/07/2026]** As duas páginas de aceite passaram a montar um `Map<document_id, document_version>` (em vez de `Set<document_id>`) e a calcular pendência com o mesmo critério de `usePendingTerms` (commit `16877f1`); UC-09 atualizado para v1.4 — ver Seção 6. | Corrigido e Documentado (20/07/2026) | Autenticação |
| UC-09-RN-03 | [UC-09](UC-09-aceitar-termos-legais.md) | Bug funcional | Alta | ~~Divergência de filtro (`required_for_registration`/`required_for_existing_users`) entre `usePendingTerms` e as páginas de aceite — mesmo efeito de loop de redirecionamento do RN-02.~~ **[CORRIGIDO E DOCUMENTADO 20/07/2026]** As duas páginas passaram a buscar todos os documentos `status: "ativo"` e filtrar client-side pela união das duas flags, igual a `usePendingTerms` (commit `16877f1`); UC-09 atualizado para v1.4 — ver Seção 6. | Corrigido e Documentado (20/07/2026) | Autenticação |
| UC-11-RN-07 | [UC-11](UC-11-inserir-nota-fiscal-manualmente.md) | Bug funcional | Alta | ~~`nf_imports` criado com `status: "success"` fixo antes da gravação (não atômica) dos itens de inventário — falha parcial não é revertida nem refletida no status.~~ **[CORRIGIDO E DOCUMENTADO 20/07/2026]** `handleSaveNF` passou a usar `writeBatch(db)`, gravando a NF e todos os itens de inventário atomicamente (mesmo padrão de UC-10) — qualquer falha rejeita o batch inteiro, sem gravação parcial (commit `16877f1`); UC-11 atualizado para v1.1 — ver Seção 6. | Corrigido e Documentado (20/07/2026) | Inventário |
| UC-13-RN-07 | [UC-13](UC-13-desativar-item-de-estoque-com-verificacao-de-reservas-ativas.md) | Bug funcional | Alta | ~~Falha ao verificar reservas ativas é tratada como "sem reservas" — item pode ser desativado sem redistribuição mesmo tendo reservas reais não verificadas.~~ **[CORRIGIDO E DOCUMENTADO 19/07/2026]** `checkInventoryItemReservations` passou a ser fail-closed: se a verificação falhar, `impactedProcedimentos` permanece `null`, ambos os botões de ação ("Confirmar desativação"/"Forçar exclusão") ficam ocultos, e a UI exibe um alerta de erro com botão "Tentar novamente" (commit `91eb37e`); UC-13 atualizado para v1.1 — ver Seção 6. | Corrigido e Documentado (19/07/2026) | Inventário |
| UC-14-Q1 | [UC-14](UC-14-auditar-e-corrigir-inconsistencias-de-inventario.md) | Achado de segurança | Alta | Ferramenta de correção em lote do inventário (potencialmente destrutiva) não tem nenhuma restrição de role — qualquer usuário do tenant pode executá-la. | Descartado (remover funcionalidade) | Inventário |
| UC-21-RN-03 | [UC-21](UC-21-cadastrar-nova-clinica.md) | Achado de segurança | Alta | ~~Senha inicial do admin de clínica é escolhida manualmente pelo System Admin e enviada em texto plano por e-mail.~~ **[CORRIGIDO E DOCUMENTADO 24/07/2026]** E-mail de boas-vindas do admin de clínica (template padrão e `getPreviewEmail()`, em `admin/tenants/new/page.tsx`) confirmado sem a senha em texto plano no corpo, com defesa contra reintrodução manual do placeholder; texto de ajuda da UI também corrigido (commit `66c75fa`). `requirePasswordChange: true` já garantia a troca obrigatória no primeiro acesso desde a correção do RN-05 (v1.1, 19/07/2026). UC-21 atualizado para v1.2 — ver Seção 6. | Corrigido e Documentado (24/07/2026) | Admin — Clínicas |
| UC-21-RN-05 | [UC-21](UC-21-cadastrar-nova-clinica.md) | Achado de segurança | Alta | ~~Nenhum mecanismo técnico força a troca da senha inicial apesar do aviso na tela — `requirePasswordChange` nunca é setada por este fluxo.~~ **[CORRIGIDO E DOCUMENTADO 19/07/2026]** `POST /api/tenants/create` agora define `requirePasswordChange: true` nas custom claims e no documento `users/{userId}` do admin de clínica criado, alinhando este fluxo ao padrão já usado em UC-02/UC-28/UC-39 (commit `91eb37e`); UC-21 atualizado para v1.1 — ver Seção 6. | Corrigido e Documentado (19/07/2026) | Admin — Clínicas |
| UC-22-RN-05 | [UC-22](UC-22-editar-desativar-e-reativar-clinica.md) | Achado de segurança | Alta | Mecanismo rico de suspensão de clínica (motivo, e-mail de contato, cascata de desativação de usuários) está implementado mas totalmente órfão — nunca acionável por nenhuma tela. | Corrigido e Documentado (18/07/2026) | Admin — Clínicas |
| UC-22-RN-06 | [UC-22](UC-22-editar-desativar-e-reativar-clinica.md) | Achado de segurança | Alta | Regra do Firestore permite que o próprio tenant (`clinic_admin`/`clinic_user`) edite os dados cadastrais da própria clínica diretamente, sem passar pelas telas administrativas. | Corrigido e Documentado (18/07/2026) | Admin — Clínicas |
| UC-28-RN-04 | [UC-28](UC-28-cadastrar-consultor.md) | Achado de segurança | Alta | ~~Senha temporária do consultor recém-criado é enviada em texto plano por e-mail.~~ **[CORRIGIDO E DOCUMENTADO 24/07/2026]** `generateConsultantWelcomeEmail` (`src/app/api/consultants/route.ts`) perdeu o parâmetro `tempPassword` — a senha deixou de ser incluída no HTML do e-mail de boas-vindas do consultor (commit `66c75fa`). `requirePasswordChange: true` já garantia a troca obrigatória no primeiro acesso. UC-28 atualizado para v1.1 — ver Seção 6. | Corrigido e Documentado (24/07/2026) | Admin — Consultores |
| UC-32-RN-01 | [UC-32](UC-32-editar-ativar-e-desativar-produto-no-catalogo-master.md) | Bug funcional | Alta | ~~Formulário de edição sempre reenvia `fragmentavel`, disparando incondicionalmente a checagem de "produto em uso" — bloqueia a edição inteira (mesmo campos não relacionados) sempre que o produto já está no inventário de alguma clínica.~~ **[CORRIGIDO E DOCUMENTADO 20/07/2026]** `handleSubmit` (`admin/products/[id]/page.tsx`) passa a calcular `fragmentacaoAlterada` e só inclui `fragmentavel`/`unidades_por_embalagem` no payload de `updateMasterProduct` quando esses campos realmente mudaram em relação ao produto carregado — editar apenas outros campos (nome, categoria, status) deixa de acionar o bloqueio de "produto em uso" (commit `f6e9161`). UC-32 atualizado para v1.0.3 — ver Seção 6. | Corrigido e Documentado (20/07/2026) | Admin — Catálogo de Produtos |
| UC-35-RN-04 | [UC-35](UC-35-editar-configuracoes-globais-do-sistema.md) | Divergência texto/UI vs lógica | Alta | ~~`maintenance_mode`/`maintenance_message` não são lidos por nenhuma outra parte do sistema — ativar "Modo de Manutenção" não bloqueia nada.~~ **[CORRIGIDO E DOCUMENTADO 24/07/2026]** `ProtectedRoute.tsx` passou a ler `system_settings/global.maintenance_mode` e redireciona qualquer usuário que não seja `system_admin` para a nova tela `/maintenance` (commit `66c75fa`). UC-35 atualizado para v2.0 (major bump — a premissa central do documento deixou de ser verdadeira) — ver Seção 6. | Corrigido e Documentado (24/07/2026) | Admin — Configurações |
| UC-35-RN-05 | [UC-35](UC-35-editar-configuracoes-globais-do-sistema.md) | Divergência texto/UI vs lógica | Alta | ~~`registration_enabled` não é lido por `/register` nem por `POST /api/access-requests` — desligar "Permitir novos registros" não bloqueia nenhum cadastro (cross-ref [UC-01-RN-10](UC-01-solicitar-acesso-ao-sistema.md)).~~ **[CORRIGIDO E DOCUMENTADO 24/07/2026]** `POST /api/access-requests` agora rejeita (403) novas solicitações quando `system_settings/global.registration_enabled === false` (commit `66c75fa`). **Ressalva registrada no próprio UC-35 como melhoria futura, não como gap:** apenas validação de servidor foi implementada, sem checagem espelhada de UX em `/register`. UC-35 atualizado para v2.0 — ver Seção 6. | Corrigido e Documentado (24/07/2026) | Admin — Configurações |
| UC-35-RN-06 | [UC-35](UC-35-editar-configuracoes-globais-do-sistema.md) | Divergência texto/UI vs lógica | Alta | ~~`session_timeout_minutes` não é lido por `useSessionTimeout.ts`, que usa um valor fixo de 15 minutos hardcoded (cross-ref [UC-04-RN-09](UC-04-fazer-login-com-redirecionamento-por-papel.md), ex-Q3).~~ **[CORRIGIDO E DOCUMENTADO 24/07/2026]** `useSessionTimeout.ts` passou a ler `system_settings/global.session_timeout_minutes` uma vez por sessão, com fallback de 15 minutos caso o campo não exista ou seja inválido (commit `66c75fa`). UC-35 atualizado para v2.0 — ver Seção 6. **Efeito colateral em UC-04:** o mesmo commit fecha o achado cross-referenciado `UC-04-RN-07` (nunca catalogado como item próprio neste mapa — ver Seção 6), com UC-04 atualizado para v1.1.4. | Corrigido e Documentado (24/07/2026) | Admin — Configurações |
| UC-39-RN-02 | [UC-39](UC-39-criar-usuario-diretamente-para-clinica-via-painel-admin.md) | Divergência texto/UI vs lógica | Alta | ~~O texto do diálogo "Adicionar Novo Usuário" promete envio de credenciais por e-mail, mas isso nunca ocorre — o e-mail disparado é genérico e nunca inclui a senha.~~ **[CORRIGIDO E DOCUMENTADO 20/07/2026]** Decisão do PO: opção (a) por questões de segurança — texto do `DialogDescription` corrigido para deixar explícito que a senha definida não é enviada por e-mail e deve ser comunicada por fora do sistema, em vez de implementar envio de senha em texto plano por e-mail (commit `f6e9161`). UC-39 atualizado para v1.2.1 — ver Seção 6. | Corrigido e Documentado (20/07/2026) | Admin — Usuários |
| UC-39-RN-03 | [UC-39](UC-39-criar-usuario-diretamente-para-clinica-via-painel-admin.md) | Achado de segurança | Alta | ~~Usuário criado diretamente pelo System Admin nunca recebe a claim `requirePasswordChange` — nunca é obrigado a trocar a senha escolhida pelo admin.~~ **[CORRIGIDO E DOCUMENTADO 18/07/2026]** `POST /api/users/create` agora define `requirePasswordChange: true` nas custom claims e no documento `users/{uid}` (commit `467f462`); UC-39 atualizado para v1.2 — ver Seção 6. | Corrigido e Documentado (18/07/2026) | Admin — Usuários |
| UC-40-RN-04 | [UC-40](UC-40-criar-usuario-para-a-propria-clinica.md) | Achado de segurança | Alta | ~~A regra `allow list` do Firestore para `users/{userId}` não filtra por `tenant_id` na própria regra — depende apenas de convenção no código-cliente para isolamento multi-tenant.~~ **[CORRIGIDO E DOCUMENTADO 19/07/2026]** A regra passou a exigir também `resource.data.tenant_id == request.auth.token.tenant_id`, forçando a própria query de listagem a estar filtrada por `tenant_id` (commit `eb12c91`); UC-40 atualizado para v1.1 — ver Seção 6. | Corrigido e Documentado (19/07/2026) | Clínica — Usuários |
| UC-42-RN-02 | [UC-42](UC-42-executar-verificacoes-de-alertas-manualmente.md) | Bug funcional | Alta | ~~`checkExpiredProducts` nunca persiste uma notificação real (trecho substituído por um placeholder) — reporta sucesso e conta produtos "criados" sem gravar nada.~~ **[CORRIGIDO E DOCUMENTADO 20/07/2026]** Novo helper `createExpiredProductNotification` (`notificationService.ts`, mesmo padrão de `createExpiringProductNotification`/`createLowStockNotification`) passou a ser chamado por `checkExpiredProducts`, substituindo o placeholder (commit `16877f1`); UC-42 atualizado para v1.3 — ver Seção 6. | Corrigido e Documentado (20/07/2026) | Notificações e Alertas |
| UC-44-RN-01 | [UC-44](UC-44-consultar-e-gerenciar-notificacoes-recebidas.md) | Bug funcional | Alta | ~~O hook `useNotifications` expõe um estado de erro, mas `NotificationBell` nunca o lê — toda falha de ação (marcar como lida, excluir, limpar) é silenciosa para o usuário.~~ **[CORRIGIDO E DOCUMENTADO 19/07/2026]** `NotificationBell` passou a ler `error` do hook e exibir `toast({variant: 'destructive'})` para as quatro ações de escrita (commit `eb12c91`). Falhas do listener em tempo real (`onSnapshot`) permanecem fora do escopo — não corrigidas. UC-44 atualizado para v1.1 — ver Seção 6. | Corrigido e Documentado (19/07/2026) | Notificações e Alertas |
| UC-44-RN-02 | [UC-44](UC-44-consultar-e-gerenciar-notificacoes-recebidas.md) | Achado de segurança | Alta | ~~Regra do Firestore restringe exclusão de notificação a `clinic_admin`, mas a UI mostra os botões de exclusão identicamente para `clinic_user` — falha de permissão silenciosa e sem explicação.~~ **[CORRIGIDO E DOCUMENTADO 19/07/2026]** Botões de exclusão individual e "Limpar notificações lidas" agora só renderizam para `clinic_admin` (`canDelete`), alinhando a UI à regra do Firestore (commit `eb12c91`). UC-44 atualizado para v1.1 — ver Seção 6. | Corrigido e Documentado (19/07/2026) | Notificações e Alertas |
| UC-46-RN-03 | [UC-46](UC-46-visualizar-consultor-vinculado-a-clinica.md) | Bug funcional | Alta | `/clinic/consultant/transfer` (vincular consultor por código, pelo lado da clínica) está completamente quebrada — a API sempre rejeita qualquer usuário de clínica com 403. **Decisão de produto já tomada** (remover e substituir por um fluxo novo de convite/transferência com aprovação) — spec completa e pronta em `ONLY_FOR_DEVS/TO_DO/FEAT-unificacao-vinculo-transferencia-consultor.md` (v1.1), **ainda não implementada**; o código desta página continua quebrado nesta data. UC-46 atualizado para v1.1 (comportamento as-is inalterado) — ver Seção 6. | Em Correção | Gestão de Clínica / Consultores |
| UC-50-RN-03 | [UC-50](UC-50-consultar-inventario-e-detalhe-do-item-de-estoque.md) | Bug funcional | Alta | ~~Dashboard e sino de notificações navegam para `/clinic/inventory` com `?filter=expired`/`?filter=low-stock`, mas `InventoryView.applyFilter` só reconhece `expiring`/`low_stock`/`out_of_stock` — o filtro pretendido nunca é aplicado, silenciosamente (lista mostra "Todos").~~ **[CORRIGIDO E DOCUMENTADO 18/07/2026]** `applyFilter` ganhou branch dedicado para `'expired'` e o link de estoque baixo do Dashboard foi corrigido para `?filter=low_stock` (commit `467f462`); UC-50 atualizado para v1.1 — ver Seção 6. | Corrigido e Documentado (18/07/2026) | Inventário |

---

## 3. Itens de Média e Baixa Severidade

Agrupados por módulo/portal.

### Autenticação / Acesso

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-07-RN-03 | [UC-07](UC-07-recuperar-senha-esquecida.md) | Achado de segurança | Média | Mensagem "Usuário não encontrado" ao recuperar senha permite enumeração de contas cadastradas. | Aberto |
| UC-06-Q1 | [UC-06](UC-06-trocar-senha-obrigatoria-no-primeiro-acesso.md) | Bug funcional | Média | Falha silenciosa ao limpar `requirePasswordChange` pode gerar impasse: usuário preso trocando uma senha que já é a definitiva. | Aberto |
| UC-09-Fluxo8d | [UC-09](UC-09-aceitar-termos-legais.md) | Bug funcional | Média | Variante B (onboarding) de aceite de termos não redireciona usuário deslogado — tela fica carregando indefinidamente. | Aberto |
| UC-01-RN-09 | [UC-01](UC-01-solicitar-acesso-ao-sistema.md) | Débito técnico | Baixa | Sem checagem de duplicidade de solicitação pendente para o mesmo e-mail em `POST /api/access-requests`. | Aberto |
| UC-01-RN-04-05-06 | [UC-01](UC-01-solicitar-acesso-ao-sistema.md) | Débito técnico | Baixa | Validação de frontend mais permissiva que a de backend (nome, e-mail, telefone) — gera erro de submissão só percebido no servidor. | Aberto |
| UC-03-Q1 | [UC-03](UC-03-rejeitar-solicitacao-de-acesso.md) | Achado de UX | Baixa | Placeholder do campo "Motivo da rejeição" cita CPF/CNPJ, schema desatualizado desde UC-01 v2.0 (cosmético). | Aberto |
| UC-04-Q1 | [UC-04](UC-04-fazer-login-com-redirecionamento-por-papel.md) | Débito técnico | Baixa | Redirecionamento de `clinic_consultant` no login faz um salto duplo (`/dashboard` → `/consultant/dashboard`), inconsistente com a lógica já correta de `ProtectedRoute`. | Corrigido e Documentado (18/07/2026) |
| UC-04-Q2 | [UC-04](UC-04-fazer-login-com-redirecionamento-por-papel.md) | Achado de UX | Baixa | Tela `/waiting-approval` reutilizada tanto para "aguardando aprovação" quanto para "conta desativada", com a mesma mensagem em ambos os casos. | Aberto |
| UC-06-RN-02 | [UC-06](UC-06-trocar-senha-obrigatoria-no-primeiro-acesso.md) | Débito técnico | Baixa | Validação de força de senha nesta tela é mais fraca (só comprimento) que a `validatePassword` compartilhada. | Aberto |
| UC-07-RN-04 | [UC-07](UC-07-recuperar-senha-esquecida.md) | Achado de UX | Baixa | `/forgot-password` não verifica se o usuário já está autenticado, diferente de `/login`/`/register`. | Aberto |
| UC-08-RNF-02 | [UC-08](UC-08-system-admin-envia-link-de-redefinicao-de-senha.md) | Achado de UX | Baixa | Diálogo de confirmação do admin usa `confirm()` nativo em vez do padrão `Dialog` do sistema. | Corrigido e Documentado (18/07/2026) |
| UC-08-RNF-03 | [UC-08](UC-08-system-admin-envia-link-de-redefinicao-de-senha.md) | Débito técnico | Baixa | Apenas a solicitação de redefinição mais recente é registrada — sem histórico completo de reenvios. | Aberto |
| UC-09-RN-07 | [UC-09](UC-09-aceitar-termos-legais.md) | Débito técnico | Baixa | `ip_address` nunca é de fato capturado nos registros de aceite, apesar de existir no schema. | Aberto |
| UC-09-RNF-03 | [UC-09](UC-09-aceitar-termos-legais.md) | Achado de UX | Baixa | Mensagens de erro do Firestore exibidas cruas ao usuário, sem tradução. | Aberto |
| UC-29-RN-09 / UC-36-RN-10 | [UC-29](UC-29-editar-suspender-e-reativar-consultor.md) / [UC-36](UC-36-editar-usuario-e-alterar-status-cross-tenant.md) | Achado de UX | Média | Tela de login (`login/page.tsx`) não trata o código `auth/user-disabled` do Firebase Auth: `signIn` (`src/hooks/useAuth.ts:75-82`) retorna `error.message` bruto do SDK em vez de `error.code`; `translateFirebaseError` (`login/page.tsx:136-153`) não tem `case` para `user-disabled` e cai no `default`, exibindo ao usuário a mensagem crua em inglês (`Firebase: The user account has been disabled by an administrator. (auth/user-disabled)`). Achado descoberto na validação manual pós-fix de UC-29/UC-36 — agora que "Suspender" (UC-29) e "Status: Inativo" (UC-36) realmente desabilitam a conta (`disabled: true`), esse code path passou a ser efetivamente alcançável. Correção sugerida (ainda não implementada): adicionar `case 'user-disabled'` em `translateFirebaseError` com mensagem em PT-BR orientando o usuário a contatar o administrador. Numeração confirmada nesta auditoria por leitura direta de UC-29 (RN-09) e UC-36 (RN-10) — deixa de ser provisória. | Aberto |

### Inventário

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-11-Q1 | [UC-11](UC-11-inserir-nota-fiscal-manualmente.md) | Achado de segurança | Média | Ator real da tela de inserção manual inclui `clinic_user` (não só `clinic_admin` como esperado) — sem checagem de role própria, diferente de UC-10. | Aberto |
| UC-11-RN-09-10-11 | [UC-11](UC-11-inserir-nota-fiscal-manualmente.md) | Débito técnico | Média | Divergências de schema entre itens de inventário criados via XML (UC-10) e manual (UC-11): `created_by` (uid vs. e-mail), `dt_validade` (Timestamp vs. string), `nf_id` vs. `nf_import_id`. | Aberto |
| UC-12-RN-02 | [UC-12](UC-12-resolver-produtos-pendentes-de-cadastro.md) | Bug funcional | Média | "Marcar Resolvido" não valida se o produto foi de fato cadastrado no catálogo master — é limpeza de fila, não uma validação. | Aberto |
| UC-13-RN-09 | [UC-13](UC-13-desativar-item-de-estoque-com-verificacao-de-reservas-ativas.md) | Achado de segurança | Média | Restrição "só `clinic_admin` desativa item" é aplicada só na UI, sem regra dedicada no Firestore. | Aberto |
| UC-14-RN-04 | [UC-14](UC-14-auditar-e-corrigir-inconsistencias-de-inventario.md) | Débito técnico | Média | Definição de "reserva ativa" diverge entre esta auditoria (`agendada`+`aprovada`) e UC-13 (só `agendada`). | Descartado (remover funcionalidade) |
| UC-15-RN-05 | [UC-15](UC-15-configurar-limite-de-estoque-baixo-por-produto.md) | Débito técnico | Média | Nenhum agendamento automático roda as verificações de alerta de estoque baixo — depende inteiramente de acionamento manual (UC-42). | Aberto |
| UC-15-RN-07 | [UC-15](UC-15-configurar-limite-de-estoque-baixo-por-produto.md) | Achado de segurança | Média | Sem regra dedicada no Firestore para `stock_limits` — restrição de role é só na UI. | Aberto |
| UC-15-RN-08 | [UC-15](UC-15-configurar-limite-de-estoque-baixo-por-produto.md) | Bug funcional | Média | `checkLowStock` não filtra `active: true`, somando quantidade residual de lotes já desativados (UC-13). | Aberto |
| UC-50-RN-01 | [UC-50](UC-50-consultar-inventario-e-detalhe-do-item-de-estoque.md) | Divergência texto/UI vs lógica | Média | Card "Total de Produtos" mostra números diferentes com o mesmo rótulo entre a lista de inventário (conta documentos de lote) e a tela de Relatórios/UC-47 (conta produtos distintos consolidados). | Aberto |
| UC-50-RN-02 | [UC-50](UC-50-consultar-inventario-e-detalhe-do-item-de-estoque.md) | Divergência texto/UI vs lógica | Média | No detalhe do item, "Valor Total em Estoque" mostra o valor de apenas um lote, não do produto consolidado entre todos os lotes — rótulo não deixa claro esse escopo. | Aberto |
| UC-50-RN-07 | [UC-50](UC-50-consultar-inventario-e-detalhe-do-item-de-estoque.md) | Divergência texto/UI vs lógica | Média | Badge de status de estoque (Normal/Baixo/Esgotado) calculado de forma agregada por produto na lista, mas por lote individual no detalhe — podem divergir para o mesmo lote. | Aberto |
| UC-13-RN-04 | [UC-13](UC-13-desativar-item-de-estoque-com-verificacao-de-reservas-ativas.md) | Débito técnico | Baixa | Ordem de alocação entre solicitações concorrentes na redistribuição forçada não segue a data do procedimento. | Aberto |
| UC-13-RN-08 | [UC-13](UC-13-desativar-item-de-estoque-com-verificacao-de-reservas-ativas.md) | Achado de UX | Baixa | Nenhuma notificação aos responsáveis por procedimentos alterados/cancelados automaticamente pela redistribuição forçada. | Aberto |
| UC-11-RN-06 | [UC-11](UC-11-inserir-nota-fiscal-manualmente.md) | Achado de UX | Baixa | Checagem de duplicidade de NF só ocorre no último passo do wizard, diferente de UC-10 (checa antes do upload). | Aberto |
| UC-12-RN-01 | [UC-12](UC-12-resolver-produtos-pendentes-de-cadastro.md) | Achado de UX | Baixa | Botão "Cadastrar Produto" (fila de pendências) não leva nenhum prefill de código/nome para a tela de cadastro. | Aberto |
| UC-12-RN-03 | [UC-12](UC-12-resolver-produtos-pendentes-de-cadastro.md) | Débito técnico | Baixa | Pendência removida sem resolução real gera pendência nova (não reaproveitada) no próximo reenvio de XML, sem histórico. | Aberto |
| UC-14-RN-03 | [UC-14](UC-14-auditar-e-corrigir-inconsistencias-de-inventario.md) | Débito técnico | Baixa | `Math.max(0, ...)` pode mascarar inconsistências mais graves (valores que deveriam ser negativos). | Descartado (remover funcionalidade) |
| UC-15-RN-03 | [UC-15](UC-15-configurar-limite-de-estoque-baixo-por-produto.md) | Divergência texto/UI vs lógica | Baixa | UI usa fallback simples (10) enquanto a notificação automática usa fallback em 3 níveis — podem divergir visivelmente. | Aberto |
| UC-15-RN-06 | [UC-15](UC-15-configurar-limite-de-estoque-baixo-por-produto.md) | Achado de UX | Baixa | Validação inválida e falha de gravação do limite de estoque são silenciosas, sem feedback ao usuário. | Aberto |
| UC-50-RN-04 | [UC-50](UC-50-consultar-inventario-e-detalhe-do-item-de-estoque.md) | Achado de UX | Baixa | Texto de estado vazio da lista ("Faça upload de uma DANFE para adicionar produtos") referencia o fluxo de importação por PDF/DANFE, removido definitivamente do sistema (substituído por importação via XML). | Aberto |

### Procedimentos

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-16-RN-08 | [UC-16](UC-16-registrar-procedimento-programado.md) | Bug funcional | Média | Aplicar um protocolo substitui a lista de produtos já selecionados sem aviso, descartando produtos adicionados manualmente antes. | Aberto |
| UC-16-RN-09 | [UC-16](UC-16-registrar-procedimento-programado.md) | Bug funcional | Média | Aplicação de protocolo não valida nem avisa sobre estoque insuficiente, diferente da adição manual de produtos. | Aberto |
| UC-18-RN-04 | [UC-18](UC-18-editar-procedimento-agendado.md) | Débito técnico | Média | Editar produtos de uma solicitação agendada não grava em `status_history` nem `inventory_activity` — gap de auditoria. | Aberto |
| UC-20-RN-07 | [UC-20](UC-20-gerenciar-protocolos.md) | Achado de segurança | Média | Sem regra dedicada no Firestore para `protocolos` — restrição de role (`clinic_admin`) é só na UI. | Aberto |
| UC-16-RN-02 | [UC-16](UC-16-registrar-procedimento-programado.md) | Débito técnico | Baixa | Alocação FEFO duplicada — implementações independentes em UC-13 e UC-16, sem código compartilhado. | Aberto |
| UC-17-RN-05 | [UC-17](UC-17-registrar-procedimento-efetuado.md) | Achado de UX | Baixa | Aplicação de protocolo bypassa a exigência de seleção manual de lote deste modo, misturando critérios no mesmo procedimento. | Aberto |
| UC-18-RN-02 | [UC-18](UC-18-editar-procedimento-agendado.md) | Débito técnico | Baixa | Algoritmo de ajuste de reservas na edição é "liberar tudo e recriar tudo", não incremental — confuso para auditoria manual dos dados brutos. | Aberto |
| UC-18-RNF-02 | [UC-18](UC-18-editar-procedimento-agendado.md) | Débito técnico | Baixa | Dados de edição de procedimento trafegam via query string — risco teórico de limite de tamanho de URL. | Aberto |
| UC-19-RN-06 | [UC-19](UC-19-concluir-ou-cancelar-procedimento-agendado.md) | Achado de UX | Baixa | Nenhuma confirmação antes de concluir/cancelar um procedimento (ação irreversível), diferente do padrão de UC-13. | Aberto |
| UC-20-RN-05 | [UC-20](UC-20-gerenciar-protocolos.md) | Achado de UX | Baixa | Protocolos podem incluir produtos sem estoque disponível, causando omissão silenciosa ao aplicar (mesmo gap de UC-16). | Aberto |
| UC-20-RN-06 | [UC-20](UC-20-gerenciar-protocolos.md) | Débito técnico | Baixa | Sem validação de nome duplicado entre protocolos do mesmo tenant. | Aberto |

### Administração do Sistema — Clínicas / Consultores

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-23-RN-03 | [UC-23](UC-23-vincular-alterar-remover-consultor-da-clinica.md) | Débito técnico | Média | Sincronização de custom claims do consultor ocorre fora da transação atômica do Firestore — risco de inconsistência em falha parcial. | Aberto |
| UC-24-RN-02 | [UC-24](UC-24-vincular-se-automaticamente-a-clinica-sem-consultor.md) | Bug funcional | Média | Falha ao sincronizar custom claims do consultor é absorvida silenciosamente — usuário vê "sucesso" mesmo com claims não sincronizadas. | Aberto |
| UC-26-RN-03 | [UC-26](UC-26-aprovar-pedido-de-transferencia-de-clinica.md) | Bug funcional | Média | Sincronização de claims sem `try/catch` pode retornar erro 500 ao usuário mesmo com os dados já alterados no Firestore. | Aberto |
| UC-28-RN-05 | [UC-28](UC-28-cadastrar-consultor.md) | Débito técnico | Média | Sem rollback se a falha ocorrer após a criação do usuário no Firebase Auth — pode deixar usuário órfão. | Aberto |
| UC-29-RN-03 | [UC-29](UC-29-editar-suspender-e-reativar-consultor.md) | Achado de UX | Média | Editar o e-mail de login de um consultor não notifica ninguém — pode travar o acesso dele sem explicação. | Aberto |
| UC-21-RN-07 | [UC-21](UC-21-cadastrar-nova-clinica.md) | Achado de UX | Baixa | Impossível retroceder no wizard de cadastro de clínica sem cancelar o processo inteiro. | Aberto |
| UC-21-RN-09 | [UC-21](UC-21-cadastrar-nova-clinica.md) | Débito técnico | Baixa | E-mail de notificação interna de novo tenant é hardcoded; campo `plan_id` nunca é definido. | Aberto |
| UC-22-RN-07 | [UC-22](UC-22-editar-desativar-e-reativar-clinica.md) | Achado de UX | Baixa | Atalho de "Desativar" existe na listagem de clínicas, mas não há atalho equivalente de "Reativar". | Corrigido e Documentado (18/07/2026) |
| UC-22-RN-08 | [UC-22](UC-22-editar-desativar-e-reativar-clinica.md) | Achado de UX | Baixa | Feedback de sucesso/erro inconsistente entre as duas telas de desativação de clínica (mensagem inline vs. `alert()`/recarga silenciosa). | Corrigido e Documentado (18/07/2026) |
| UC-22-RNF-01 | [UC-22](UC-22-editar-desativar-e-reativar-clinica.md) | Débito técnico | Baixa | Nenhum motivo ou auditoria registrado ao desativar/reativar uma clínica pelo mecanismo simples. | Corrigido e Documentado (18/07/2026) |
| UC-24-RN-06 | [UC-24](UC-24-vincular-se-automaticamente-a-clinica-sem-consultor.md) | Achado de segurança | Baixa | Regra do Firestore permite que um `clinic_admin` escreva diretamente em `consultant_claims` (risco teórico, hoje inatingível). | Aberto |
| UC-25-RN-02 | [UC-25](UC-25-solicitar-transferencia-de-clinica-ja-vinculada.md) | Débito técnico | Baixa | Nenhum prazo de expiração para um pedido de transferência de clínica pendente. | Aberto |
| UC-25-RN-03 | [UC-25](UC-25-solicitar-transferencia-de-clinica-ja-vinculada.md) | Achado de UX | Baixa | Notificação ao consultor atual sobre um pedido de transferência é só por e-mail, sem notificação in-app nem link direto ao pedido. | Aberto |
| UC-26-RN-01 | [UC-26](UC-26-aprovar-pedido-de-transferencia-de-clinica.md) | Achado de UX | Baixa | Nenhuma confirmação antes de aprovar um pedido de transferência de clínica (ação irreversível, único clique). | Aberto |
| UC-27-RN-03 | [UC-27](UC-27-rejeitar-pedido-de-transferencia-de-clinica.md) | Achado de UX | Baixa | Assimetria de confirmação: aprovar transferência (mais impactante) não pede confirmação, rejeitar pede. | Aberto |
| UC-28-RN-02 | [UC-28](UC-28-cadastrar-consultor.md) | Débito técnico | Baixa | Janela de corrida teórica na geração do código único de 6 dígitos do consultor (checagem e escrita não atômicas). | Aberto |
| UC-29-RN-05 | [UC-29](UC-29-editar-suspender-e-reativar-consultor.md) | Débito técnico | Baixa | Formulário de edição do consultor sempre reenvia o e-mail, disparando chamada desnecessária ao Firebase Auth a cada salvamento. | Aberto |
| UC-30-RN-03 | [UC-30](UC-30-definir-senha-consultor-manualmente.md) | Achado de UX | Baixa | UI ignora a mensagem de sucesso diferenciada retornada pela API ao definir senha do consultor. | Corrigido e Documentado (18/07/2026) |
| UC-30-RN-06 | [UC-30](UC-30-definir-senha-consultor-manualmente.md) | Achado de segurança | Baixa | `error.message` bruto do Firebase Admin SDK exposto em respostas de erro 500. | Corrigido e Documentado (18/07/2026) |
| UC-30-RN-07 | [UC-30](UC-30-definir-senha-consultor-manualmente.md) | Débito técnico | Baixa | Campos de auditoria gravados (`passwordSetByAdminAt` etc.) não estão declarados na interface `Consultant`. | Aberto |
| UC-30-RN-08 | [UC-30](UC-30-definir-senha-consultor-manualmente.md) | Débito técnico | Baixa | Sem histórico auditável de definições manuais de senha de consultor — apenas a mais recente é registrada. | Aberto |

### Administração do Sistema — Catálogo / Documentos Legais / Configurações

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-31-RN-02 | [UC-31](UC-31-cadastrar-produto-no-catalogo-master.md) | Débito técnico | Média | Nenhuma validação server-side de formato no cadastro de produto master — só client SDK + regra do Firestore. | Aberto |
| UC-32-RN-02 | [UC-32](UC-32-editar-ativar-e-desativar-produto-no-catalogo-master.md) | Bug funcional | Média | Ativar/Desativar produto direto na listagem não tem confirmação nem checa se o produto está em uso. | Aberto |
| UC-32-RN-03 | [UC-32](UC-32-editar-ativar-e-desativar-produto-no-catalogo-master.md) | Bug funcional | Média | Flag `active` de produto tem efeito inconsistente entre UC-10 (ignorado) e UC-11 (filtrado) — desativar não bloqueia os dois caminhos de entrada de estoque. | Aberto |
| UC-33-RN-03 | [UC-33](UC-33-cadastrar-documento-legal.md) | Débito técnico | Média | Nenhuma validação server-side de formato no cadastro de documento legal. | Aberto |
| UC-33-RN-04 | [UC-33](UC-33-cadastrar-documento-legal.md) | Achado de segurança | Média | Regra do Firestore permite leitura de documentos legais em qualquer status (inclusive "rascunho") a qualquer usuário autenticado. | Aberto |
| UC-34-RN-05 | [UC-34](UC-34-editar-publicar-e-excluir-documento-legal.md) | Bug funcional | Média | Nenhum aviso ao editar versão/obrigatoriedade de documento já ativo e potencialmente já aceito — pode disparar o loop de redirecionamento do UC-09. | Aberto |
| UC-31-RN-01 | [UC-31](UC-31-cadastrar-produto-no-catalogo-master.md) | Débito técnico | Baixa | Checagem de duplicidade de código de produto master não é atômica (janela de corrida teórica). | Aberto |
| UC-32-RN-06 | [UC-32](UC-32-editar-ativar-e-desativar-produto-no-catalogo-master.md) | Achado de UX | Baixa | Sem opção de limpar a categoria de um produto já categorizado, via UI. | Corrigido e Documentado (18/07/2026) |
| UC-33-RN-01 | [UC-33](UC-33-cadastrar-documento-legal.md) | Débito técnico | Baixa | Sem verificação de duplicidade de `slug` entre documentos legais. | Aberto |
| UC-33-RN-02 | [UC-33](UC-33-cadastrar-documento-legal.md) | Débito técnico | Baixa | Sem verificação de duplicidade de "Ordem de Exibição" entre documentos legais. | Aberto |
| UC-34-RN-01 | [UC-34](UC-34-editar-publicar-e-excluir-documento-legal.md) | Achado de UX | Baixa | Slug sempre recalculado ao editar o título, sobrescrevendo qualquer customização manual anterior. | Aberto |
| UC-34-RN-02 | [UC-34](UC-34-editar-publicar-e-excluir-documento-legal.md) | Débito técnico | Baixa | `published_at` é sobrescrito a cada salvamento com status ativo e nunca é lido por nenhuma tela do sistema. | Aberto |
| UC-35-RN-01 | [UC-35](UC-35-editar-configuracoes-globais-do-sistema.md) | Débito técnico | Baixa | Documento de configurações globais é sobrescrito por inteiro (`setDoc` sem `merge`), sem detecção de conflito concorrente. | Aberto |
| UC-35-RN-07 | [UC-35](UC-35-editar-configuracoes-globais-do-sistema.md) | Achado de segurança | Baixa | Regra do Firestore permite leitura de `system_settings/global` a qualquer usuário autenticado, não só `system_admin`. | Aberto |

### Administração do Sistema — Usuários / Autoatendimento

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-36-RN-07 | [UC-36](UC-36-editar-usuario-e-alterar-status-cross-tenant.md) | Débito técnico | Média | Nenhuma validação impede que uma clínica fique sem nenhum `clinic_admin` ativo após uma edição de função/status. | Corrigido e Documentado (17/07/2026) |
| UC-39-RN-05 | [UC-39](UC-39-criar-usuario-diretamente-para-clinica-via-painel-admin.md) | Bug funcional | Média | API de criação de usuário não verifica se o tenant está ativo antes de permitir a criação. | Aberto |
| UC-36-RN-09 | [UC-36](UC-36-editar-usuario-e-alterar-status-cross-tenant.md) | Achado de UX | Baixa | Sem confirmação dedicada para mudança de status de usuário (diferente do padrão de UC-22/UC-29). | Aberto |
| UC-36-RNF-02 | [UC-36](UC-36-editar-usuario-e-alterar-status-cross-tenant.md) | Débito técnico | Baixa | Nenhuma auditoria de quem editou o quê em um usuário (só `updated_at` é gravado). | Aberto |
| UC-37-RN-03 | [UC-37](UC-37-definir-senha-usuario-manualmente.md) | Achado de UX | Baixa | UI ignora a mensagem de sucesso diferenciada retornada pela API ao definir senha de usuário. | Corrigido e Documentado (18/07/2026) |
| UC-37-RN-06 | [UC-37](UC-37-definir-senha-usuario-manualmente.md) | Achado de segurança | Baixa | `error.message` bruto do SDK exposto em erros 500 (mesmo padrão de UC-30). | Corrigido e Documentado (18/07/2026) |
| UC-37-RN-07 | [UC-37](UC-37-definir-senha-usuario-manualmente.md) | Achado de UX | Baixa | Checkbox "Solicitar troca de senha" vem desmarcada por padrão aqui, mas marcada por padrão no equivalente de consultores (UC-30) — divergência não explicada. | Aberto |
| UC-37-RN-08 | [UC-37](UC-37-definir-senha-usuario-manualmente.md) | Débito técnico | Baixa | Campos de auditoria gravados não estão declarados na interface `User`. | Aberto |
| UC-37-RN-09 | [UC-37](UC-37-definir-senha-usuario-manualmente.md) | Débito técnico | Baixa | Sem histórico auditável de definições manuais de senha de usuário — apenas a mais recente é registrada. | Aberto |
| UC-38-RN-01 | [UC-38](UC-38-editar-perfil-e-trocar-senha-do-system-admin.md) | Débito técnico | Baixa | Senha mínima de 8 caracteres para `system_admin` diverge do padrão de 6 usado no resto do sistema. | Aberto |
| UC-38-RN-03 | [UC-38](UC-38-editar-perfil-e-trocar-senha-do-system-admin.md) | Achado de UX | Baixa | Sem bloqueio para nova senha igual à senha atual (diferente de UC-06). | Corrigido e Documentado (18/07/2026) |
| UC-38-RN-04 | [UC-38](UC-38-editar-perfil-e-trocar-senha-do-system-admin.md) | Débito técnico | Baixa | Tratamento de erro de reautenticação incompleto — não trata `auth/invalid-credential`, só `auth/wrong-password`. | Corrigido e Documentado (18/07/2026) |
| UC-38-RN-07 | [UC-38](UC-38-editar-perfil-e-trocar-senha-do-system-admin.md) | Débito técnico | Baixa | Nenhuma auditoria registrada quando o próprio `system_admin` troca a senha. | Aberto |
| UC-39-RN-01 | [UC-39](UC-39-criar-usuario-diretamente-para-clinica-via-painel-admin.md) | Débito técnico | Baixa | Nenhuma validação client-side no diálogo de criação de usuário do Portal Admin. | Aberto |
| UC-39-RN-08 | [UC-39](UC-39-criar-usuario-diretamente-para-clinica-via-painel-admin.md) | Débito técnico | Baixa | Nenhum campo de auditoria distingue se um usuário foi criado via System Admin ou via aprovação de solicitação. | Aberto |

### Clínica — Usuários / Autoatendimento

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-40-RN-02 | [UC-40](UC-40-criar-usuario-para-a-propria-clinica.md) | Achado de segurança | Baixa | Variável `isAdmin` declarada mas nunca usada para gating em `UsersTab` — pode piscar conteúdo de admin para `clinic_user` na página órfã. | Aberto |
| UC-40-RN-07 | [UC-40](UC-40-criar-usuario-para-a-propria-clinica.md) | Achado de segurança | Baixa | Nenhuma restrição para um `clinic_admin` criar outro `clinic_admin` para a própria clínica. | Aberto |
| UC-41-RN-03 | [UC-41](UC-41-editar-perfil-e-trocar-senha-do-usuario-de-clinica.md) | Achado de UX | Baixa | Sem bloqueio para nova senha igual à atual (mesmo achado de UC-38). | Corrigido e Documentado (18/07/2026) |
| UC-41-RN-04 | [UC-41](UC-41-editar-perfil-e-trocar-senha-do-usuario-de-clinica.md) | Débito técnico | Baixa | Tratamento de erro de reautenticação incompleto (mesmo achado de UC-38). | Corrigido e Documentado (18/07/2026) |
| UC-41-RN-07 | [UC-41](UC-41-editar-perfil-e-trocar-senha-do-usuario-de-clinica.md) | Débito técnico | Baixa | Nenhuma auditoria registrada quando o usuário de clínica troca a própria senha. | Aberto |
| UC-41-RNF-04 | [UC-41](UC-41-editar-perfil-e-trocar-senha-do-usuario-de-clinica.md) | Achado de UX | Baixa | Falha ao carregar o histórico de aceites de termos é silenciosa (só `console.error`). | Aberto |

### Onboarding de Clínica

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-45-RN-02 | [UC-45](UC-45-completar-configuracao-inicial-da-clinica.md) | Achado de segurança | Média | Sem distinção de role entre `clinic_admin`/`clinic_user` para editar os dados cadastrais da clínica em `/clinic/setup`. | Aberto |
| UC-45-RN-04 | [UC-45](UC-45-completar-configuracao-inicial-da-clinica.md) | Achado de UX | Média | Sem flag de "onboarding concluído" — `/clinic/setup` permanece acessível indefinidamente como uma segunda via de edição de dados cadastrais. | Aberto |
| UC-45-RN-05 | [UC-45](UC-45-completar-configuracao-inicial-da-clinica.md) | Débito técnico | Baixa | Campo legado `cnpj` nunca é atualizado por este fluxo, pode divergir de `document_number`. | Aberto |
| UC-45-RN-06 | [UC-45](UC-45-completar-configuracao-inicial-da-clinica.md) | Débito técnico | Baixa | Validação de CPF só verifica quantidade de dígitos, sem checagem de dígito verificador (assimétrico com CNPJ). | Aberto |

### Notificações e Alertas

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-42-RN-03 | [UC-42](UC-42-executar-verificacoes-de-alertas-manualmente.md) | Bug funcional | Média | Nenhuma das três funções de verificação de alerta filtra `active: true` no inventário. | Aberto |
| UC-42-RN-05 | [UC-42](UC-42-executar-verificacoes-de-alertas-manualmente.md) | Débito técnico | Média | Nenhum agendamento automático roda as verificações de alerta — depende inteiramente de acionamento manual (cross-ref UC-15). | Aberto |
| UC-42-RN-07 | [UC-42](UC-42-executar-verificacoes-de-alertas-manualmente.md) | Achado de UX | Baixa | Deduplicação de alertas reseta ao marcar como lida — gera notificação duplicada mesmo sem mudança na condição de origem. | Aberto |
| UC-43-RN-05 | [UC-43](UC-43-configurar-preferencias-de-notificacao.md) | Débito técnico | Baixa | `email_notifications` é persistido mas não tem nenhum consumidor funcional (UI sempre desabilitada, "em breve"). | Aberto |
| UC-44-RN-04 | [UC-44](UC-44-consultar-e-gerenciar-notificacoes-recebidas.md) | Divergência texto/UI vs lógica | Baixa | `notification_sound` (configurado em UC-43) não controla o som real tocado pelo `NotificationBell` (fixo `true`). | Aberto |
| UC-44-RN-05 | [UC-44](UC-44-consultar-e-gerenciar-notificacoes-recebidas.md) | Débito técnico | Baixa | Limite fixo de 50 notificações exibidas, sem paginação. | Aberto |
| UC-44-RN-07 | [UC-44](UC-44-consultar-e-gerenciar-notificacoes-recebidas.md) | Achado de UX | Baixa | Exclusão de notificação individual não pede confirmação, diferente da limpeza em lote. | Corrigido e Documentado (18/07/2026) |

### Gestão de Clínica / Consultores (Consulta)

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-46-RN-04 | [UC-46](UC-46-visualizar-consultor-vinculado-a-clinica.md) | Achado de segurança | Média | Gate de role invertido em `/clinic/consultant/transfer`: bloqueia `clinic_admin`, mas permite `clinic_user` acessar o formulário quebrado. **Decisão de produto já tomada** (mesma spec de UC-46-RN-03 — remover a página), **ainda não implementada**; comportamento as-is inalterado. Ver Seção 6. | Em Correção |
| UC-46-RN-01 | [UC-46](UC-46-visualizar-consultor-vinculado-a-clinica.md) | Débito técnico | Baixa | Duplicação de código entre `ClinicConsultantPage` e `ConsultantTab` — mesma consulta, implementações independentes. | Aberto |
| UC-46-RN-02 | [UC-46](UC-46-visualizar-consultor-vinculado-a-clinica.md) | Achado de UX | Baixa | Falhas ao buscar o consultor vinculado são indistinguíveis do estado "sem consultor" — sem mensagem de erro. | Aberto |

### Relatórios

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-47-RN-02 | [UC-47](UC-47-gerar-relatorios-de-estoque-vencimento-e-consumo.md) | Bug funcional | Média | Itens de inventário com data de validade em formato não reconhecido são silenciosamente excluídos do Relatório de Vencimento, subestimando o "Valor em Risco". | Aberto |
| UC-47-RN-01 | [UC-47](UC-47-gerar-relatorios-de-estoque-vencimento-e-consumo.md) | Achado de UX | Baixa | Relatório "Produtos Vencendo" também inclui produtos já vencidos — nome pode confundir o usuário. | Aberto |
| UC-47-RNF-01 | [UC-47](UC-47-gerar-relatorios-de-estoque-vencimento-e-consumo.md) | Achado de UX | Baixa | Feedback de erro via `alert()` nativo, inconsistente com o padrão de toast do resto do sistema. | Corrigido e Documentado (18/07/2026) |

### Portal do Consultor

| ID | UC de Origem | Categoria | Severidade | Resumo | Status |
|---|---|---|---|---|---|
| UC-48-RN-02 | [UC-48](UC-48-consultar-clinicas-vinculadas-e-estoque.md) | Divergência texto/UI vs lógica | Média | Card "Estoque Baixo" no detalhe da clínica diz "5 unidades ou menos", mas o cálculo real usa limiar `<= 10`. | Aberto |
| UC-48-RN-03 | [UC-48](UC-48-consultar-clinicas-vinculadas-e-estoque.md) | Débito técnico | Baixa | Terceiro mecanismo de limiar de "estoque baixo" (hardcoded, 10), totalmente desconectado de UC-15/UC-43. | Aberto |
| UC-49-RN-03 | [UC-49](UC-49-visualizar-perfil-proprio-do-consultor.md) | Achado de UX | Baixa | Falha ao carregar o perfil do consultor é silenciosa, sem estado de erro visível. | Aberto |

---

## 4. Decisões de Produto Pendentes

Itens que não são bugs — são escolhas de escopo/comportamento a serem decididas antes de virarem tarefa de correção.

> ⚠️ **Passo 0 obrigatório antes de iniciar qualquer nova implementação:** o item `ADR-QA-AUTOMATION` (última linha da tabela abaixo) deve ser revisado com o usuário antes de começar a próxima tarefa de código — pedido explícito do usuário em 17/07/2026, ainda sem decisão tomada.

| ID | UC de Origem | Categoria | Resumo | Opções Levantadas | Módulo |
|---|---|---|---|---|---|
| UC-02-Q1 | [UC-02](UC-02-aprovar-solicitacao-de-acesso.md) | Decisão de produto pendente | `document_type`/`document_number`/`address` do tenant são fallback fixo de código desde a mudança de formulário de UC-01, sem fonte real de dado. | (a) manter fallback fixo; (b) adicionar formulário na tela de aprovação; (c) remover os campos do modelo `Tenant`; (d) reintroduzir a coleta em UC-01. | Admin — Acesso |
| UC-05-Decisão | [UC-05](UC-05-vincular-se-automaticamente-a-clinica-sem-consultor.md) | Decisão de produto pendente | Botão "Aprovar Solicitação" pela própria clínica (`clinic_admin`) nunca funcionou — 3 problemas confirmados em cadeia (função depreciada, contagem de vagas errada, nenhuma solicitação real chega a existir). | (a) remover a tela/botão de aprovação do `clinic_admin`, mantendo só a rejeição; (b) implementar de fato o mecanismo, se houver motivo de negócio não explicado. | Admin da Clínica |
| UC-10-RN-02 | [UC-10](UC-10-importar-nfe-via-upload-de-xml.md) | Decisão de produto pendente | Regras de bloqueio por duplicidade de NF-e (v1) foram explicitamente sinalizadas pelo próprio usuário como sujeitas a revisão. | Revisão de negócio a definir; não deve ser tratada como regra definitiva. | Inventário |
| UC-25-Decisão | [UC-25](UC-25-solicitar-transferencia-de-clinica-ja-vinculada.md) | Decisão de produto pendente | O backend de "Solicitar Transferência de Clínica" (ramo "CASO 2" da API) está pronto, mas nenhuma tela expõe um botão que o dispare — afeta também UC-26 e UC-27, que ficam sempre com a fila vazia. | (a) implementar o botão/fluxo de UI faltante; (b) remover o ramo da API e as rotas de aprovação/rejeição associadas, hoje inatingíveis. | Portal do Consultor |
| UC-35-Decisão | [UC-35](UC-35-editar-configuracoes-globais-do-sistema.md) | Decisão de produto pendente | As três configurações da tela "Configurações do Sistema" (manutenção, registro, timeout — RN-04/05/06, listadas como Alta na Seção 2) não têm efeito real hoje. | (a) implementar os três consumidores reais; (b) remover/ocultar a tela até a decisão de implementar ser tomada. | Admin — Configurações |
| UC-40-RN-08 | [UC-40](UC-40-criar-usuario-para-a-propria-clinica.md) | Decisão de produto pendente | `clinic_admin` não tem, hoje, nenhuma forma de editar, desativar/reativar ou redefinir/definir senha de um usuário já existente da própria clínica — apenas criar e listar. | (a) implementar uma tela análoga a UC-36/UC-37, escopada ao próprio tenant do `clinic_admin`; (b) manter como decisão consciente (contato com suporte/System Admin para qualquer correção). | Clínica — Usuários |
| UC-49-RN-01 | [UC-49](UC-49-visualizar-perfil-proprio-do-consultor.md) | Decisão de produto pendente | Diferente de UC-38 (System Admin) e UC-41 (usuário de clínica), o Consultor não tem nenhum mecanismo de autoatendimento para editar nome/senha — só orientação para contatar o suporte. | Confirmar se é intencional (delegar sempre ao suporte/System Admin) ou lacuna de funcionalidade a implementar. | Portal do Consultor |
| UC-50-RN-05 | [UC-50](UC-50-consultar-inventario-e-detalhe-do-item-de-estoque.md) | Decisão de produto pendente | A lista de inventário só exibe itens `active: true`; o detalhe (`getInventoryItem`) não filtra por `active` — um item desativado nunca aparece na lista, mas seu detalhe continua acessível por URL direta (link salvo, notificação antiga). | (a) manter como está (comportamento aceitável, acesso só por URL direta); (b) bloquear/redirecionar o acesso ao detalhe de um item inativo. | Inventário |
| **`ADR-QA-AUTOMATION`** | N/A — proposta de processo (não originada de UC) | Decisão de produto pendente / infraestrutura | 🔔 **REVISAR ANTES DE INICIAR A PRÓXIMA IMPLEMENTAÇÃO** (pedido explícito do usuário, 17/07/2026). ADR em `ONLY_FOR_DEVS/TO_DO/ADR-automacao-qa-playwright-firebase-emulator.md` (v1.0, status "Planejamento — PAUSADO deliberadamente, decisão não tomada") propõe um "Agente de QA" que leria a seção "STEP 4 — Validação Manual" de specs concluídos em `TASK_COMPLETED/` e geraria/rodaria testes Playwright equivalentes contra o Firebase Emulator Suite, substituindo a justificativa de "mock frágil do Admin SDK" usada nos 4 bugfixes críticos recentes (UC-29, UC-34, UC-36, UC-43) para não escrever testes automatizados. `@playwright/test` já instalado e `emulators` já configurados em `firebase.json`, mas falta toda a infraestrutura: sem `playwright.config.ts`, sem testes E2E, sem script `test:e2e`, sem job de CI. | 7 perguntas em aberto registradas na Seção 10.1 do ADR — nenhuma decisão tomada ainda; ficam a critério do usuário na retomada do assunto. | Infraestrutura / QA — Processo (cross-cutting, não vinculado a um módulo funcional específico) |

---

## 5. Código Morto / Rotas Órfãs Confirmadas

Candidatos a limpeza (`chore:`), não a "correção" propriamente dita — confirmados por leitura de código e/ou busca exaustiva no repositório.

- **`accessRequestService.createAccessRequest()`** (client-side) — nunca chamada por nenhuma tela; faria correspondência de solicitação por CNPJ/documento com tenant existente. Confirmado em [UC-01](UC-01-solicitar-acesso-ao-sistema.md) (seção 14) e [UC-05](UC-05-vincular-se-automaticamente-a-clinica-sem-consultor.md) (RN-03).
- **Fluxo inteiro "Aprovar Solicitação pela Própria Clínica"** (`clinic/access-requests`, botão "Aprovar") — `approveAccessRequest()` depreciada, sempre falha; candidato a remoção junto com a decisão de produto do UC-05.
- ~~**Rota órfã `/clinic/inventory/audit`** — sem nenhum link de navegação em todo o sistema (mesmo padrão do antigo `/activate`, já removido). Confirmado em [UC-14](UC-14-auditar-e-corrigir-inconsistencias-de-inventario.md).~~ **[DESCARTADO 15/07/2026]** Toda a ferramenta será removida — ver Seção 6.
- **`src/lib/services/tenantService.ts`** — service legado (baseado em Cloud Functions `httpsCallable`) que chama uma função `"createTenant"` inexistente em `functions/src/`. Confirmado em [UC-21](UC-21-cadastrar-nova-clinica.md) (RNF-03).
- **`consultantService.ts`: `transferConsultant`, `removeConsultant`, `searchConsultants`** — reimplementam client-side a mesma lógica das rotas de API realmente usadas, mas nunca são chamadas. Confirmado em [UC-23](UC-23-vincular-alterar-remover-consultor-da-clinica.md) (RN-06).
- **`POST /api/consultants/claims/[id]/approve` e `/reject`** — implementam um fluxo antigo de aprovação manual de vínculo consultor-clínica, superado pelo auto-link; código morto duplo (sem gatilho de UI e sem dado de entrada possível, já que nada cria uma claim `pending`). Confirmado em [UC-24](UC-24-vincular-se-automaticamente-a-clinica-sem-consultor.md) (RN-05).
- **`consultantService.ts`: `createConsultant`** — nunca chamada; a tela usa exclusivamente a API route. Confirmado em [UC-28](UC-28-cadastrar-consultor.md) (RN-06).
- ~~**`DELETE /api/consultants/[id]`** — implementaria a desativação real do consultor (desabilita Auth + zera claims), mas nenhuma tela a chama. Confirmado em [UC-29](UC-29-editar-suspender-e-reativar-consultor.md) (RN-02).~~ **[CORRIGIDO E DOCUMENTADO 16/07/2026]** Deixou de ser código morto — reconectada ao botão "Suspender" (commit `dbf128f`). Ver Seção 6.
- **`masterProductService.ts`: `deleteMasterProduct`** — hard delete de produto do catálogo master, nunca chamado por nenhuma tela. Confirmado em [UC-32](UC-32-editar-ativar-e-desativar-produto-no-catalogo-master.md) (RN-04).
- **Ramo de UI/lógica "consultor" no diálogo "Editar Usuário"** (`admin/users`) — inalcançável, já que consultores são filtrados antes de compor a lista exibida por essa tela. Confirmado em [UC-36](UC-36-editar-usuario-e-alterar-status-cross-tenant.md) (RN-04).
- **Rota órfã `/clinic/users`** — sem nenhum link de navegação; a única via real é a aba "Usuários" de `/clinic/my-clinic`. Confirmado em [UC-40](UC-40-criar-usuario-para-a-propria-clinica.md) (RN-09).
- **Rota órfã `/clinic/alerts`** — sem nenhum link de navegação em todo o sistema. Confirmado em [UC-42](UC-42-executar-verificacoes-de-alertas-manualmente.md) (RN-06).
- **Rota órfã `/clinic/settings`** — sem nenhum link de navegação em todo o sistema. Confirmado em [UC-43](UC-43-configurar-preferencias-de-notificacao.md) (RN-06). **[DECISÃO 15/07/2026]** Navegação propositalmente mantida sem link por ora — ligar o menu antes de decidir se os alertas serão automatizados criaria uma promessa de UI que a automação (ainda manual, UC-42) não cumpre. Ver Seção 6 e o novo achado sobre a landing page prometer "alertas automáticos de 60/30/15 dias".
- **`notificationService.ts`: `createRequestApprovedNotification`, `createRequestRejectedNotification`** — funções completas, com ícone próprio já implementado em `NotificationBell`, mas nunca chamadas. Os tipos `request_created` e `new_user` também nunca geram nenhuma notificação real em todo o código-fonte. Confirmado em [UC-44](UC-44-consultar-e-gerenciar-notificacoes-recebidas.md) (RN-03).
- **`reportService.ts`: `exportToCSV`** — função de exportação alternativa, nunca chamada por `ReportsView` nem por nenhum outro ponto do código. Confirmado em [UC-47](UC-47-gerar-relatorios-de-estoque-vencimento-e-consumo.md) (RN-05).
- **Chamada `fetch` sem uso do resultado** em `ClinicDetailPage` (`GET /api/tenants/{id}/consultant`, dentro de `loadTenantData`) — resquício de código sem efeito observável. Confirmado em [UC-48](UC-48-consultar-clinicas-vinculadas-e-estoque.md) (RN-01).

---

## 6. Itens Resolvidos

| ID | UC de Origem | Status | Data | Decisão |
|---|---|---|---|---|
| UC-14-RN-05, UC-14-Q1, UC-14-RN-04, UC-14-RN-03 | [UC-14](UC-14-auditar-e-corrigir-inconsistencias-de-inventario.md) | Descartado (remover funcionalidade) | 15/07/2026 | A Auditoria de Inventário (`/clinic/inventory/audit`) foi criada como ferramenta de correção manual durante experimentação do desenvolvedor. Decisão do PO: com o projeto em pré-MVP, a ferramenta será **removida do sistema** em vez de corrigida — futuras correções de dados não passarão por ela. Ação pendente: remover `src/app/(clinic)/clinic/inventory/audit/`, e uma vez removida, o `uml-use-case-writer` deve marcar UC-14 como **Obsoleto** (não apenas atualizar uma RN) e o `uc-issues-tracker` deve mover estes itens de "Descartado" para "Corrigido e Documentado". |

🔔 **Ação de código pendente (não implementada nesta sessão):** deletar a rota `/clinic/inventory/audit` e qualquer referência a ela. Isso é trabalho de implementação, não de documentação — recomenda-se um spec `CR-remove-auditoria-inventario` via `doc-writer` quando for priorizado.

| UC-29-RN-01, UC-29-RN-02 | [UC-29](UC-29-editar-suspender-e-reativar-consultor.md) | Corrigido e Documentado | 16/07/2026 | Decisão do PO (15/07/2026): o botão "Suspender" (`admin/consultants`) passa a chamar `DELETE /api/consultants/[id]` (já implementada, antes órfã) em vez de `PUT` com `{status: 'suspended'}` — isso desabilita a conta no Firebase Auth e zera o custom claim `active`, corrigindo o comportamento cosmético anterior. Não foi escolhido reforçar o `PUT` (opção b) para não duplicar lógica de desativação em duas rotas. |

✅ **Concluído (16/07/2026):** implementado nos commits `b08fdeb` (corrige o bug de claims da rota `DELETE` — antes espalhava o documento Firestore do consultor como se fossem as claims do usuário, destruindo `is_consultant`/`role`/`tenant_id`/`consultant_id`; passou a buscar as claims reais via `adminAuth.getUser` antes de sobrescrever), `dbf128f` (reconecta o botão "Suspender" da listagem à rota `DELETE`) e `d167883` (decisão do PO pela Opção A: reforça simetricamente também a reativação, `PUT` com `status: 'active'`, restaurando `disabled: false` e o claim `active: true` preservando as demais claims). Branch `bugfix/suspend-consultant-reconnect-delete`. Verificado nesta execução por leitura direta do código-fonte: `src/app/api/consultants/[id]/route.ts` (handlers `DELETE` e `PUT`) e `src/app/(admin)/admin/consultants/page.tsx` (`handleSuspend` chama `DELETE`, `handleReactivate` chama `PUT`) confirmam o comportamento descrito. Durante a mesma revisão, o `uml-use-case-writer` encontrou e o mesmo ciclo corrigiu um achado adicional, **UC-29-RN-07** (nunca esteve "Aberto" neste mapa): a aba "Suspensos" da listagem (`admin/consultants`) parou de encontrar consultores suspensos pelo novo fluxo real, pois `GET /api/consultants` ainda filtrava apenas por `status === 'suspended'` (valor legado), não pelo novo valor gravado `'inactive'`. Corrigido no commit `3ab00b6` — o filtro passou a usar `where('status', 'in', ['suspended', 'inactive'])`; confirmado por leitura de `src/app/api/consultants/route.ts` (linhas 140-144) nesta execução. O `uml-use-case-writer` já atualizou [UC-29](UC-29-editar-suspender-e-reativar-consultor.md) para **v1.2.1**, documentando RN-01, RN-02 e RN-07 como resolvidos; **RN-08** (ordem de escrita assimétrica Firestore/Auth entre `DELETE` e `PUT`-reativação, achado novo decorrente da correção) permanece em aberto, não tratado neste ciclo.

| UC-34-RN-03 | [UC-34](UC-34-editar-publicar-e-excluir-documento-legal.md) | Corrigido e Documentado | 16/07/2026 | Decisão do PO: opção (a) da seção 14 do UC-34 — bloquear a exclusão de um documento legal (`handleDelete`, `admin/legal-documents`) sempre que existir pelo menos um registro em `user_document_acceptances` referenciando aquele `document_id`, com mensagem clara ao admin. Não escolhida a opção (b) (permitir só desativar, nunca excluir) para preservar a possibilidade de excluir documentos que nunca foram aceitos por ninguém (ex.: rascunhos, erros de cadastro). |

✅ **Concluído (16/07/2026):** correção aplicada em `src/app/(admin)/admin/legal-documents/page.tsx` — commit `4561a2a` (branch `bugfix/block-legal-document-deletion-with-acceptances`). Verificado nesta execução por leitura direta do código-fonte: `handleDelete` executa uma query em `user_document_acceptances` filtrando `where('document_id', '==', documentToDelete.id)` com `limit(1)` antes do `deleteDoc`; se `acceptancesSnap` não estiver vazio, a exclusão é bloqueada com toast explicativo ("Este documento já foi aceito por usuários e não pode ser excluído...") e a função retorna sem chamar `deleteDoc`. O `uml-use-case-writer` já atualizou [UC-34](UC-34-editar-publicar-e-excluir-documento-legal.md) para **v1.0.1** e, por referência cruzada, [UC-09](UC-09-aceitar-termos-legais.md) para **v1.3.1**, documentando a correção e marcando o achado como resolvido na seção 14 de ambos.

| UC-36-RN-02, UC-36-RN-03, UC-36-RN-07 | [UC-36](UC-36-editar-usuario-e-alterar-status-cross-tenant.md) | Corrigido e Documentado | 17/07/2026 | Decisão do PO (15/07/2026): opção (a) da seção 14 do UC-36 — construir `PUT /api/users/[id]` (rota nova) que chame `adminAuth.setCustomUserClaims` (sincroniza "Função") e `adminAuth.updateUser({disabled: !active})` (torna "Status: Inativo" real), migrando `admin/users` de `updateDoc` direto para essa rota. Empacotado junto (RN-07): antes de rebaixar ou desativar um `clinic_admin`, a rota checa se ele é o único `clinic_admin` ativo do tenant; se for, exige confirmação explícita do System Admin (HTTP 409 `LAST_ADMIN_CONFIRMATION_REQUIRED` + `confirm()` + reenvio com `confirmLastAdmin: true`) antes de prosseguir — não é um bloqueio automático, o admin pode confirmar que quer mesmo deixar a clínica sem admin local. |

✅ **Concluído (17/07/2026):** correção verificada nesta execução por leitura direta do código-fonte. `src/app/api/users/[id]/route.ts` (rota `PUT`, nova) confirmado implementando: validação de Bearer token + claim `is_system_admin` (401/403), defesa em profundidade contra edição de `system_admin`/`clinic_consultant` (403), checagem `willLoseAdminStatus`/`otherActiveAdmins` com resposta `409 LAST_ADMIN_CONFIRMATION_REQUIRED` quando aplicável (RN-07), `adminAuth.getUser` + `adminAuth.setCustomUserClaims(userId, {...currentClaims, role, active})` preservando `tenant_id` (RN-02), e `adminAuth.updateUser(userId, {disabled: !active})` (RN-03). `src/app/(admin)/admin/users/page.tsx` (`handleSaveUser`) confirmado migrado: ramo não-consultor agora faz `fetch('/api/users/{uid}', {method: 'PUT', ...})` com `Authorization: Bearer {token}` e trata o `409` reenviando com `confirmLastAdmin: true` após `confirm()`. Branch `bugfix/sync-user-claims-on-edit`, commits `2c7b023` (`fix(admin): add PUT /api/users/[id] to sync auth claims and disabled status`), `e0f0e74` (`fix(admin): require confirmation before removing a clinic's last active admin`), `69bc944` (`fix(admin): migrate user edit dialog to sync claims via api route`) — todos confirmados via `git log` nesta execução. O `uml-use-case-writer` já atualizou [UC-36](UC-36-editar-usuario-e-alterar-status-cross-tenant.md) para **v1.1**, marcando RN-02/RN-03/RN-07 como `[Corrigido — nota histórica]`, reescrevendo o fluxo principal (seção 6) e adicionando o fluxo de exceção 8c (confirmação de "último admin"). RN-09 (confirmação dedicada de status, achado de UX) permanece em aberto na Seção 3 — fora do escopo desta correção.

**Nota sobre o ciclo de revisão dos itens críticos (histórico, mantida apenas para rastreabilidade):** esta observação foi escrita num ponto intermediário do ciclo de correções (logo após a resolução de UC-36-RN-02/RN-03), quando UC-29-RN-01 e UC-34-RN-03 ainda não tinham confirmação de correção em código. Como as entradas desta própria Seção 6 já demonstram (ver acima), **UC-29** e **UC-34** foram confirmados como "Corrigido e Documentado" em 16/07/2026 — antes mesmo da resolução de UC-36 (17/07/2026). Com isso, os 6 itens críticos originalmente priorizados estão todos encerrados: 5 "Corrigido e Documentado" (UC-29, UC-34, UC-36 ×2, UC-43) + 1 "Descartado" (UC-14). Nenhum item de severidade Crítica permanece "Em Correção".

| UC-43-RN-01/02 | [UC-43](UC-43-configurar-preferencias-de-notificacao.md) | Corrigido e Documentado | 16/07/2026 | Decisão do PO (15/07/2026): corrigir apenas o bug (`updateDoc` → `setDoc` em `initializeNotificationSettings` e no fallback de `saveNotificationSettings`), sem religar o link de navegação de `/clinic/settings` por ora. Motivo: ligar o menu antes de decidir sobre automação criaria uma segunda promessa de UI não cumprida (ver achado da landing page abaixo). |

**[ACHADO MAPEADO — 15/07/2026]** A landing page (`ONLY_FOR_DEVS/new_landing_page/sections-product.jsx`, linhas 96 e 99, módulo "Inventário inteligente") promete **"Alertas automáticos de vencimento"** e **"Validade com alertas de 60/30/15 dias"**. Nenhuma das duas promessas corresponde ao sistema real. Mapeamento formal concluído pelo `uml-use-case-writer`: **UC-42 (v1.1→1.2) e UC-43 (v1.1→1.2)** ganharam uma nova RN-08 (idêntica, cruzando um UC ao outro) documentando a divergência, e um item correspondente na seção 14 de ambos. Decisão de produto pendente registrada formalmente na **Seção 4** (`UC-42-RN-08 / UC-43-RN-08`).

**Achados relacionados, fora do escopo desta rodada (identificados pelo `uml-use-case-writer`, não verificados a fundo):** `ONLY_FOR_DEVS/new_landing_page/sections-personas.jsx`, linha 7 ("...e te avisa quando vai vencer") e linha 171 ("Alertas que abrem conversas... você é avisado antes do cliente") — ambas sugerem a mesma promessa de notificação proativa/automática em outra seção da landing page. Não investigadas nem mapeadas; ficam como sugestão para o PO decidir se merecem a mesma revisão.

✅ **Concluído (16/07/2026):** correção aplicada em `src/lib/services/notificationService.ts` — commit `a38e581` (branch `bugfix/notification-settings-setdoc`), trocando `updateDoc` por `setDoc` em `initializeNotificationSettings` (criação do documento de configurações) e no fallback de `saveNotificationSettings` (bloco `catch` para `error.code === 'not-found'`). Verificado nesta execução por leitura direta do código-fonte — ambos os pontos confirmados usando `setDoc`. O `uml-use-case-writer` já atualizou [UC-43](UC-43-configurar-preferencias-de-notificacao.md) para **v1.2.1**, documentando a correção e marcando o achado como resolvido na seção 14.

| UC-42-RN-08 / UC-43-RN-08 | [UC-42](UC-42-executar-verificacoes-de-alertas-manualmente.md) / [UC-43](UC-43-configurar-preferencias-de-notificacao.md) | Corrigido e Documentado | 15/07/2026 | Decisão do PO: opção (b) — ajustar a copy da landing page (`sections-product.jsx`, linhas 96 e 99) para refletir o comportamento real (verificação configurável, sob demanda), em vez de prometer automação e escalonamento 60/30/15. Motivo: pré-MVP, prioridade em estabilizar o backlog de bugs já mapeado antes de abrir escopo de feature nova (Scheduled Function + lógica de deduplicação por estágio). A automação real (opção a) **não foi descartada** — vira item de roadmap pós-MVP, fora do escopo deste mapa de bugs até ser formalmente priorizada. Copy corrigida: "Alertas automáticos de vencimento" → "Alertas de vencimento configuráveis"; "Validade com alertas de 60/30/15 dias" → "Validade com alertas configuráveis por antecedência". |

✅ **Concluído:** `ONLY_FOR_DEVS/new_landing_page/sections-product.jsx` (linhas 96 e 99) editado nesta sessão. UC-42/UC-43 permanecem com a RN-08 documentando o histórico da divergência (não removida — registra que a promessa existiu e foi corrigida).

| UC-04-Q1, UC-08-RNF-02, UC-30-RN-06, UC-37-RN-06, UC-47-RNF-01 | [UC-04](UC-04-fazer-login-com-redirecionamento-por-papel.md) / [UC-08](UC-08-system-admin-envia-link-de-redefinicao-de-senha.md) / [UC-30](UC-30-definir-senha-consultor-manualmente.md) / [UC-37](UC-37-definir-senha-usuario-manualmente.md) / [UC-47](UC-47-gerar-relatorios-de-estoque-vencimento-e-consumo.md) | Corrigido e Documentado | 18/07/2026 | Lote de 5 correções de severidade Baixa, todas no commit único `53df743` (`fix: lote de correções de baixa severidade (UC-04, UC-08, UC-30, UC-37, UC-47)`). |

✅ **Concluído (18/07/2026):** correções verificadas nesta execução por leitura direta do código-fonte e dos UCs de origem já atualizados pelo `uml-use-case-writer`:
- **UC-04-Q1** — `redirectByRole` (`src/app/(auth)/login/page.tsx`, linhas 87-97) passou a incluir um branch direto `else if (claims.role === 'clinic_consultant') { router.push('/consultant/dashboard'); }`, eliminando o salto duplo via `/dashboard`. UC-04 atualizado para **v1.1.2** (nova RN-08 documentando a correção).
- **UC-08-RNF-02** — diálogo de confirmação do admin (`src/app/(admin)/admin/users/page.tsx`, `confirmResetPassword`) migrado de `confirm()` nativo para `AlertDialog` padrão do sistema (`src/components/ui/alert-dialog.tsx`), aplicado nas duas variantes (usuários e consultores). UC-08 atualizado para **v1.2.1**.
- **UC-30-RN-06** — `src/app/api/consultants/[id]/set-password/route.ts` (bloco `catch`, linhas 78-80) não propaga mais `error.message` bruto do Firebase Admin SDK; retorna sempre a mensagem genérica "Erro ao definir senha. Tente novamente.", com o erro real preservado apenas em log de servidor. UC-30 atualizado para **v1.1.1**.
- **UC-37-RN-06** — mesma correção aplicada à rota irmã de usuários de clínica, `src/app/api/users/[id]/set-password/route.ts` (linhas 79-81), no mesmo commit. UC-37 atualizado para **v1.0.1**.
- **UC-47-RNF-01** — `ReportsView` (`src/components/reports/ReportsView.tsx`) passou a usar `useToast` (`src/hooks/use-toast.ts`) em vez de `alert()` nativo para o feedback de erro ao gerar relatório, alinhando ao padrão de toast do restante do sistema. UC-47 atualizado para **v1.0.1**.

Todos os 5 UCs de origem já foram atualizados pelo `uml-use-case-writer` para as versões acima, com o achado correspondente marcado como resolvido na seção 9 (e, quando aplicável, seção 14) de cada um — não há sinalização de documentação pendente para este lote.

| UC-38-RN-04, UC-41-RN-04, UC-30-RN-03, UC-37-RN-03 | [UC-38](UC-38-editar-perfil-e-trocar-senha-do-system-admin.md) / [UC-41](UC-41-editar-perfil-e-trocar-senha-do-usuario-de-clinica.md) / [UC-30](UC-30-definir-senha-consultor-manualmente.md) / [UC-37](UC-37-definir-senha-usuario-manualmente.md) | Corrigido e Documentado | 18/07/2026 | Segundo lote de correções de severidade Baixa, commit único `ec31c27` (`fix: segundo lote de correções de baixa severidade (UC-22, UC-30, UC-37, UC-38, UC-41)`). |

✅ **Concluído (18/07/2026):** correções verificadas nesta execução por leitura direta do código-fonte e dos UCs de origem já atualizados pelo `uml-use-case-writer`:
- **UC-38-RN-04** — `handleUpdatePassword` (`src/app/(admin)/admin/profile/page.tsx:91`) passou a tratar `error.code === 'auth/wrong-password' || error.code === 'auth/invalid-credential'`, antes só reconhecia `auth/wrong-password` explicitamente. Alinha este formulário ao padrão já usado em UC-06. UC-38 atualizado para **v1.0.1**.
- **UC-41-RN-04** — mesma correção aplicada à rota irmã de usuário de clínica, `src/app/(clinic)/clinic/profile/page.tsx:148`, no mesmo commit. UC-41 atualizado para **v1.0.1**.
- **UC-30-RN-03** — `src/app/(admin)/admin/consultants/[id]/page.tsx` (`handleSetPassword`, linha 239) passou a gravar `setSetPasswordSuccessMessage(data.message || 'Senha definida com sucesso!')` a partir da resposta real da API (antes ignorada), e o bloco de sucesso (linha 475) já exibia `{setPasswordSuccessMessage || 'Senha definida com sucesso!'}` — a UI agora reflete a mensagem diferenciada retornada pelo backend. UC-30 atualizado para **v1.1.2**.
- **UC-37-RN-03** — mesma correção aplicada à rota irmã de usuários de clínica, `src/app/(admin)/admin/users/page.tsx` (linhas 279 e 742), no mesmo commit. UC-37 atualizado para **v1.0.2**.

Todos os 4 UCs de origem já foram atualizados pelo `uml-use-case-writer` para as versões acima, com o achado correspondente marcado como resolvido na seção 9 de cada um — não há sinalização de documentação pendente para estes 4 itens.

**Item adicional do mesmo commit (`ec31c27`), resolução parcial — não movido para "Corrigido e Documentado":**

| UC-22-RN-08 | [UC-22](UC-22-editar-desativar-e-reativar-clinica.md) | Aberto (parcialmente corrigido) | 18/07/2026 | Do mesmo commit `ec31c27`, apenas a parte de **tratamento de erro** do achado foi corrigida: `handleDeactivate` em `admin/tenants/page.tsx` deixou de usar `alert(err.message \|\| 'Erro ao desativar clínica')` e passou a usar `setError(err.message \|\| 'Erro ao desativar clínica')` (com `setError('')` no início da função), reaproveitando o state `error` já existente no arquivo — alinhando esta tela ao padrão de feedback inline já usado na tela de detalhe. |

⚠️ **Verificado, mas não encerrado (18/07/2026):** confirmado por leitura direta do código-fonte (`src/app/(admin)/admin/tenants/page.tsx`, função `handleDeactivate`, linhas 51-65) que a parte de erro está de fato corrigida (usa `setError`, não `alert()`). Porém a parte original do achado que cobria a **ausência de mensagem de sucesso** ao desativar pela listagem **não foi tratada**: após `deactivateTenant(tenantId)` bem-sucedido, o código só chama `loadTenants()` — nenhum `setSuccess`/toast/mensagem é exibido ao usuário, diferente da tela de detalhe (que mostra "Clínica desativada com sucesso!"). O UC-22 já foi atualizado pelo `uml-use-case-writer` para **v1.2.1**, documentando a correção parcial: RN-08 marcado `[Parcialmente corrigido]`, RNF-04 marcado `[Parcialmente resolvido]`, e o item 5 da Seção 14 do UC-22 atualizado para `[PARCIALMENTE RESOLVIDO]`, com a parte de sucesso explicitamente registrada como pendência aberta.

🔁 **[SUPERADO — ver entrada abaixo]** Este achado parcial (`ec31c27`, v1.2.1) foi superado por uma mudança de mecanismo inteira em UC-22, não por uma correção pontual adicional: o commit `90b8a6f` (v2.0) **removeu por completo** o "mecanismo simples" (`deactivateTenant`/`reactivateTenant`, incluindo o `handleDeactivate` da listagem referenciado acima) e reconectou o mecanismo rico de suspensão (`SuspendTenantDialog`/`ReactivateTenantDialog`) como único caminho de UI, com feedback via `toast` padronizado em ambas as telas — tornando a inconsistência original irrelevante, pois o código que a continha deixou de existir. O commit `529adc3` (v2.0.1) fechou as duas lacunas residuais abertas por essa troca de mecanismo. Resultado: `UC-22-RN-08` está, na prática, resolvido — não pela correção direta do `handleDeactivate` antigo, mas pela substituição do mecanismo que o continha.

| UC-22-RN-02, UC-22-RN-05, UC-22-RN-06, UC-22-RN-07, UC-22-RN-08, UC-22-RNF-01, UC-22-RNF-05 | [UC-22](UC-22-editar-desativar-e-reativar-clinica.md) | Corrigido e Documentado | 18/07/2026 | Decisão do PO (registrada desde v1.0 do UC-22): Opção (a) da decisão pendente `UC-22-Decisão` (Seção 4) — conectar o mecanismo rico de suspensão de clínica em vez de removê-lo. Empacotado em duas rodadas: **v2.0 (commit `90b8a6f`)** removeu `deactivateTenant`/`reactivateTenant` e reconectou `SuspendTenantDialog`/`ReactivateTenantDialog` (`POST`/`DELETE /api/tenants/[id]/suspend`) como único caminho de UI em ambas as telas (detalhe e listagem), com motivo formal, detalhes obrigatórios, e-mail de contato, cascata real sobre todos os usuários da clínica (claims + `users/{id}.active`) e auditoria completa (RN-03/04/05/07 e RNF-01 corrigidos nesta rodada) — corrigindo também um bug de reconstrução de claims do zero (mesmo padrão do bugfix de UC-29). **v2.0.1 (commit `529adc3`)** fechou as duas lacunas residuais apontadas na revisão da v2.0: (1) removeu o campo "Status" redundante do formulário de edição cadastral, que ainda permitia ao próprio `system_admin` alternar `tenants/{id}.active` sem passar pelo mecanismo rico (achado elevado na própria revisão da v2.0, catalogado como RN-02) — RN-06 e RN-08 ficam então integralmente fechados (sem caminho de UI residual); (2) adicionou `adminAuth.updateUser(userId, { disabled })` à cascata de suspensão/reativação, bloqueando/restaurando de fato a conta no Firebase Auth (achado novo da própria v2.0, catalogado como RNF-05), alinhando UC-22 ao mesmo padrão de UC-29/UC-36. |

✅ **Concluído (18/07/2026) — achado extraído da documentação do UC-22 (v2.0.1), não verificado em código nesta execução (Modo A):** o próprio texto de UC-22 v2.0.1 documenta, com referência a commits e trechos de código, todas as correções acima; este mapa não releu o código-fonte da aplicação diretamente nesta rodada, apenas a documentação já produzida e versionada pelo `uml-use-case-writer`. `UC-22-RN-05` (mecanismo rico órfão) e `UC-22-RN-06` (regra do Firestore permitindo edição direta pelo próprio tenant) — ambos Alta severidade — movidos de "Aberto" para "Corrigido e Documentado". `UC-22-RN-07` (sem atalho de "Reativar" na listagem) e `UC-22-RNF-01` (sem auditoria no mecanismo simples) — ambos Baixa — movidos de "Aberto" para "Corrigido e Documentado". `UC-22-RN-08` (feedback inconsistente) — Baixa, antes tratado como "Aberto" mesmo após a correção parcial de `ec31c27` — agora também movido para "Corrigido e Documentado", pelo motivo explicado no bloco "SUPERADO" acima. Dois achados adicionais (`UC-22-RN-02` e `UC-22-RNF-05`) nasceram e já foram corrigidos dentro do mesmo ciclo de revisão do UC-22 (entre v2.0 e v2.0.1) — seguindo o mesmo precedente já usado para `UC-29-RN-07` neste mapa, **não foram contabilizados como itens "Aberto" na Seção 1** em nenhum momento, apenas registrados aqui, em Seção 6, para rastreabilidade histórica. A decisão de produto `UC-22-Decisão` (Seção 4) foi removida por estar resolvida. Nenhuma pendência conhecida resta sobre UC-22 — confirmado pela leitura da Seção 14 do próprio documento ("Não restam pendências conhecidas sobre o mecanismo de editar/suspender/reativar clínica neste UC").

| UC-32-RN-06, UC-38-RN-03, UC-41-RN-03, UC-44-RN-07 | [UC-32](UC-32-editar-ativar-e-desativar-produto-no-catalogo-master.md) / [UC-38](UC-38-editar-perfil-e-trocar-senha-do-system-admin.md) / [UC-41](UC-41-editar-perfil-e-trocar-senha-do-usuario-de-clinica.md) / [UC-44](UC-44-consultar-e-gerenciar-notificacoes-recebidas.md) | Corrigido e Documentado | 18/07/2026 | Terceiro lote de correções de severidade Baixa, todas no commit único `2ddebd6` (`fix: terceiro lote de correções de baixa severidade (UC-32, UC-38, UC-41, UC-44)`). |

✅ **Concluído (18/07/2026):** correções verificadas nesta execução por leitura direta do código-fonte e dos UCs de origem já atualizados pelo `uml-use-case-writer`:
- **UC-38-RN-03** — `handleUpdatePassword` (`src/app/(admin)/admin/profile/page.tsx`) passou a bloquear explicitamente a nova senha sendo igual à senha atual (`if (newPassword === currentPassword) { setPasswordError('A nova senha deve ser diferente da senha atual'); return; }`), logo após a checagem de confirmação de senha e antes de `setPasswordLoading(true)`, alinhando este formulário a UC-06. UC-38 atualizado para **v1.0.2**.
- **UC-41-RN-03** — mesma correção aplicada à rota irmã de usuário de clínica, `src/app/(clinic)/clinic/profile/page.tsx`, no mesmo commit. UC-41 atualizado para **v1.0.2**.
- **UC-32-RN-06** — o `<Select>` de categoria em `admin/products/[id]/page.tsx` passou a oferecer a opção "Sem categoria" (`<SelectItem value="none">`), com o payload de `updateMasterProduct` enviando `category: category || null`. **Bug adicional descoberto e corrigido no mesmo commit (causa raiz de por que a limpeza não funcionava de fato):** `updateMasterProduct` (`masterProductService.ts`) ignorava `category === undefined`, então o campo `category` nunca era de fato removido no Firestore mesmo com a UI antiga tentando limpar; corrigido para usar `deleteField()` quando `category === null`, com o tipo `UpdateMasterProductData.category` ampliado para `MasterProductCategory | null` (`src/types/masterProduct.ts`). UC-32 atualizado para **v1.0.1**.
- **UC-44-RN-07** — `handleDelete` (`src/components/notifications/NotificationBell.tsx`) passou a exigir confirmação nativa (`confirm('Deseja realmente excluir esta notificação?')`) antes de excluir uma notificação individual, mesmo padrão nativo já usado por `handleClearAll` (limpeza em lote) no mesmo componente. UC-44 atualizado para **v1.0.1** (permanece com Status "Rascunho" no cabeçalho — a correção não alterou esse status).

Todos os 4 UCs de origem já foram atualizados pelo `uml-use-case-writer` para as versões acima, com o achado correspondente marcado como resolvido na seção 9 (e, quando aplicável, seção 14) de cada um — não há sinalização de documentação pendente para este lote.

| UC-32-RN-06b | [UC-32](UC-32-editar-ativar-e-desativar-produto-no-catalogo-master.md) | Corrigido e Documentado | 18/07/2026 | Achado nascido e corrigido no mesmo ciclo, durante a validação manual da correção do RN-06 — mesmo precedente já usado para `UC-29-RN-07` e `UC-22-RN-02`/`UC-22-RNF-05` neste mapa: nunca esteve "Aberto", não recebe linha própria nas Seções 2/3 e não é contabilizado nos totais da Seção 1. |

✅ **Concluído (18/07/2026):** durante a validação manual da correção do RN-06 (opção "Sem categoria" no `<Select>` de categoria do formulário de edição, commit `2ddebd6`), o usuário notou que a tela de **listagem** de produtos (`src/app/(admin)/admin/products/page.tsx`) tinha a mesma lacuna original do RN-06 no seu próprio filtro de categoria: oferecia apenas "Todas as categorias" mais cada categoria específica, sem opção para filtrar produtos sem categoria definida — a v1.0 do UC-32 já registrava essa ausência em "ambas as telas" (edição e listagem), mas apenas a tela de edição foi corrigida no commit `2ddebd6`. Corrigido no commit `199d09b` (`fix(admin): adiciona filtro "Sem categoria" na listagem de produtos master`): o estado `categoryFilter` foi ampliado de `useState<MasterProductCategory | 'all'>('all')` para `useState<MasterProductCategory | 'all' | 'none'>('all')`; a função `filterProducts` ganhou o branch `if (categoryFilter === 'none') { filtered = filtered.filter((p) => !p.category); }` antes do `else if (categoryFilter !== 'all')`; e o `<Select>` ganhou `<SelectItem value="none">Sem categoria</SelectItem>` logo após "Todas as categorias". Diferente da correção do formulário de edição — que exigiu `deleteField()` no backend para de fato limpar o campo no Firestore —, este filtro é puramente client-side sobre os `products` já carregados em memória, então não havia bug equivalente a corrigir aqui. Correção verificada por leitura direta do código-fonte nesta execução. O `uml-use-case-writer` já atualizou [UC-32](UC-32-editar-ativar-e-desativar-produto-no-catalogo-master.md) para **v1.0.2**, documentando o achado como sub-nota do próprio RN-06 (Seção 9) e adicionando o commit `199d09b` à Seção 13 (Referências) — não abriu um RN novo, por ser a mesma área e o mesmo padrão do achado original. Severidade Baixa (Achado de UX), mesma classificação do RN-06 original — Módulo Admin — Catálogo de Produtos. **Sem impacto nos contadores da Seção 1** (mesmo tratamento dado a `UC-29-RN-07` e `UC-22-RN-02`/`UC-22-RNF-05`).

| UC-50-RN-03, UC-39-RN-03 | [UC-50](UC-50-consultar-inventario-e-detalhe-do-item-de-estoque.md) / [UC-39](UC-39-criar-usuario-diretamente-para-clinica-via-painel-admin.md) | Corrigido e Documentado | 18/07/2026 | Lote de 2 correções de severidade Alta, ambas no commit único `467f462` (`fix: dois itens de alta severidade (UC-50, UC-39)`). |

✅ **Concluído (18/07/2026):** correções verificadas nesta execução por leitura direta do código-fonte e dos UCs de origem já atualizados pelo `uml-use-case-writer`:
- **UC-50-RN-03** — `applyFilter` (`src/components/inventory/InventoryView.tsx:117`) confirmado com um branch dedicado `if (filter === 'expired') { ... }`, semanticamente distinto de `'expiring'` (lógica original preservada, ver UC-47-RN-01); o chip de filtro rápido ganhou a opção "Vencidos" (linha 419, lista `['all', 'expired', 'expiring', 'low_stock', 'out_of_stock']`). O link de estoque baixo do Dashboard (`src/app/(clinic)/clinic/dashboard/page.tsx:403`) confirmado corrigido de `?filter=low-stock` para `?filter=low_stock`. UC-50 atualizado para **v1.1** (Fluxo de Exceção 8c e RN-03 marcados `[CORRIGIDO]`; item 3 da Seção 14 marcado `[RESOLVIDO]`).
- **UC-39-RN-03** — `POST /api/users/create` (`src/app/api/users/create/route.ts`, linhas 108 e 119) confirmado gravando `requirePasswordChange: true` tanto nas custom claims (`adminAuth.setCustomUserClaims`) quanto no documento `users/{uid}`, alinhando este fluxo de criação de usuário pelo System Admin ao padrão já usado em UC-02/UC-28 — o usuário criado passa a ser obrigado a trocar a senha no primeiro acesso, acionando UC-06. UC-39 atualizado para **v1.2** (RN-03 marcada `[CORRIGIDO]`; item 2 da Seção 14 marcado `[RESOLVIDO]`).

Commit `467f462` confirmado via `git log` nesta execução. Ambos os UCs de origem já foram atualizados pelo `uml-use-case-writer` — não há sinalização de documentação pendente para este lote.

| UC-46-RN-03, UC-46-RN-04 | [UC-46](UC-46-visualizar-consultor-vinculado-a-clinica.md) | Em Correção (decisão de produto tomada, implementação pendente) | 18/07/2026 | Decisão do PO: opção equivalente a "remover e substituir" da decisão pendente `UC-46-Decisão` (removida desta Seção 4) — `/clinic/consultant/transfer` será removida e substituída por um fluxo novo de convite (clínica → consultor) e transferência (consultor → clínica com consultor já vinculado) com aprovação, unificado sob a coleção `consultant_transfer_requests` com campo discriminador `type`. Spec completa e pronta para implementação: `ONLY_FOR_DEVS/TO_DO/FEAT-unificacao-vinculo-transferencia-consultor.md` (v1.1). **Diferente dos demais itens desta seção, este NÃO está corrigido**: nenhuma linha de código foi alterada nesta data — tratado como "Em Correção" (mesmo status usado historicamente para decisão tomada + implementação ainda pendente, ver UC-29/UC-34/UC-36/UC-43 no histórico desta seção), não "Corrigido e Documentado". |

⏳ **Aguardando implementação (18/07/2026):** confirmado por leitura de `src/app/(clinic)/clinic/consultant/transfer/page.tsx` (arquivo ainda existe, comportamento inalterado — continua chamando `POST /api/tenants/{id}/consultant`, que continua rejeitando qualquer usuário de clínica com 403) que o código não mudou. O `uml-use-case-writer` já atualizou [UC-46](UC-46-visualizar-consultor-vinculado-a-clinica.md) para **v1.1**, documentando a decisão tomada (Seção 14, item 1) e anotando RN-03/RN-04 e o Fluxo de Exceção 8b com a mesma nota de status, sem alterar a descrição do comportamento as-is. 🔔 Quando a spec for implementada, uma futura execução deste mapa deve: mover `UC-46-RN-03`/`UC-46-RN-04` para "Corrigido e Documentado"; verificar se a implementação também resolveu UC-46-RN-01 (duplicação `ClinicConsultantPage`/`ConsultantTab`) e UC-46-RN-02 (erro silencioso), já que a spec toca `ConsultantTab.tsx` mas não confirma a unificação completa (ver Seção 5.5 da spec); e verificar impacto em UC-23 a UC-27 (a spec lista mudanças em UC-25/26/27 e mantém UC-23/UC-24 inalterados).

| UC-02-RNF-01 | [UC-02](UC-02-aprovar-solicitacao-de-acesso.md) | Corrigido e Documentado | 19/07/2026 | Correção de segurança direta (não é uma decisão de produto com opções alternativas — ausência de autenticação era claramente um bug): `POST /api/access-requests/[id]/approve` passou a exigir `Authorization: Bearer {idToken}`, verificado via `adminAuth.verifyIdToken`, com checagem de `decodedToken.is_system_admin` (401 se token ausente/inválido, 403 se não for `system_admin`); `approved_by_uid`/`approved_by_name` passam a vir do token verificado, não mais do corpo da requisição, antes controlado livremente pelo client (permitindo também forjar a autoria da aprovação). |

✅ **Concluído (19/07/2026):** correção verificada nesta execução por leitura do código relevante já transcrito e referenciado em [UC-02](UC-02-aprovar-solicitacao-de-acesso.md) v2.1 (`approve/route.ts`) — confirmado que a rota extrai o token do header `Authorization: Bearer`, verifica via `adminAuth.verifyIdToken` e retorna 401/403 conforme o caso, **antes** de qualquer criação de `tenant`/usuário (nenhuma operação executa sem autenticação válida). Commit `33c5ef3`. `uml-use-case-writer` já atualizou UC-02 para **v2.1** (RN-01, RN-04, RNF-01, Fluxo Principal — passo 5 reescrito e novo passo 6 de verificação, com renumeração subsequente —, nova exceção 8a, Pós-condições 4.2 e seção 14 marcada como resolvida).

**Achado gêmeo, nascido e corrigido no mesmo commit — não contabilizado na Seção 1 (mesmo precedente de `UC-29-RN-07`, `UC-22-RN-02`/`UC-22-RNF-05`, `UC-32-RN-06b`):**

| UC-03-RNF-04 | [UC-03](UC-03-rejeitar-solicitacao-de-acesso.md) | Corrigido e Documentado | 19/07/2026 | Descoberto durante a investigação da correção de `UC-02-RNF-01`: a rota irmã `POST /api/access-requests/[id]/reject` tinha exatamente o mesmo problema — nenhuma verificação de autenticação, com `rejected_by_uid`/`rejected_by_name` vindos livremente do corpo da requisição. Corrigida no mesmo commit `33c5ef3`, com o mesmo padrão (Bearer token + `verifyIdToken` + checagem de `is_system_admin`). |

✅ **Concluído (19/07/2026), verificado via [UC-03](UC-03-rejeitar-solicitacao-de-acesso.md) v1.3 (RNF-04):** diferente de `UC-02-RNF-01`, esta rota **não é usada pelo fluxo real da UI** — a rejeição (tanto pelo System Admin quanto pelo Clinic Admin) sempre passa por `rejectAccessRequest()` (`updateDoc` client-side, RN-02, já protegido pela regra do Firestore `isSystemAdmin()` em `access_requests`, `firestore.rules` linhas 94-100). A correção foi, portanto, defesa em profundidade: como o Admin SDK usado pela rota de API bypassa completamente as regras do Firestore, a exposição era real e diretamente explorável (qualquer requisição HTTP sem autenticação podia rejeitar qualquer solicitação pendente e forjar a autoria), mesmo sem uso pela UI hoje. **Severidade classificada como Média** (não Alta, como `UC-02-RNF-01`): o impacto de uma rejeição indevida fica limitado a negar/vandalizar solicitações pendentes (sem criação de tenant, usuário ou exposição de dado sensível), diferente do impacto de uma aprovação forjada (criação real de tenant + usuário `clinic_admin` — o achado mais grave). `uml-use-case-writer` já atualizou [UC-03](UC-03-rejeitar-solicitacao-de-acesso.md) para **v1.3**, com a nova RNF-04 já nascendo `[CORRIGIDO]`. Nunca esteve "Aberto" neste mapa — sem linha própria nas Seções 2/3, **sem impacto nos contadores da Seção 1**.

| UC-40-RN-04, UC-44-RN-01, UC-44-RN-02 | [UC-40](UC-40-criar-usuario-para-a-propria-clinica.md) / [UC-44](UC-44-consultar-e-gerenciar-notificacoes-recebidas.md) | Corrigido e Documentado | 19/07/2026 | Lote de 3 correções de severidade Alta, todas no commit único `eb12c91` (`fix: dois itens de alta severidade (UC-40, UC-44)`). |

✅ **Concluído (19/07/2026):** correções verificadas nesta execução por leitura direta do código-fonte (`firestore.rules`, `NotificationBell.tsx`) e dos UCs de origem já atualizados pelo `uml-use-case-writer`:
- **UC-40-RN-04** — `firestore.rules` (`match /users/{userId}`, regra `allow list`) confirmado com a cláusula adicional `&& resource.data.tenant_id == request.auth.token.tenant_id`, forçando a própria query de listagem a estar filtrada por `tenant_id` (o Firestore valida `list` pela consulta como um todo, não documento a documento — sem esse filtro, a operação inteira é rejeitada). Fecha o risco teórico de listagem cross-tenant por um `clinic_admin` manipulando o SDK diretamente, sem depender apenas de convenção no código-cliente. UC-40 atualizado para **v1.1** (RN-04 e RNF-01 marcados `[CORRIGIDO]`/`[Mitigado]`; item 1 da Seção 14 marcado `[RESOLVIDO]`).
- **UC-44-RN-01** — `src/components/notifications/NotificationBell.tsx` confirmado importando `useToast`, desestruturando `error` de `useNotifications(...)` e disparando `toast({ title: error, variant: 'destructive' })` em um `useEffect([error, toast])` — as quatro ações de escrita (`markAsRead`, `markAllAsRead`, `deleteNotification`, `deleteReadNotifications`) deixam de falhar silenciosamente. **Nota de escopo, já registrada no próprio UC-44 (RNF-02, Fluxo de Exceção 8c):** falhas do listener em tempo real (`onSnapshot`) não setam o estado `error` do hook e continuam sem nenhum feedback ao usuário — não corrigidas nesta rodada, risco de suporte residual explicitamente documentado como pendente. UC-44 atualizado para **v1.1** (RN-01 marcado `[CORRIGIDO]`; item 2 da Seção 14 marcado `[RESOLVIDO parcialmente]`).
- **UC-44-RN-02** — mesmo arquivo, `canDelete = claims?.role === 'clinic_admin'` confirmado condicionando a renderização do ícone de lixeira individual e do bloco "Limpar notificações lidas" — um `clinic_user` deixa de ver controles que sempre falhavam por permissão, alinhando a UI à regra do Firestore (`firestore.rules`, `tenants/{tenantId}/notifications`, inalterada nesta correção) que já restringia `delete` a `clinic_admin`. UC-44 atualizado para **v1.1** (RN-02 marcado `[CORRIGIDO]`; item 1 da Seção 14 marcado `[RESOLVIDO]`).

Commit `eb12c91` confirmado via `git log`/`git show` nesta execução (branch `fix/uc40-users-list-rule`, já mesclada em `ad5e692`). Ambos os UCs de origem já foram atualizados pelo `uml-use-case-writer` — não há sinalização de documentação pendente para este lote. A ressalva de escopo de UC-44-RN-01 (falhas do listener `onSnapshot` fora do escopo) já estava prevista na redação original do achado e permanece registrada como tal no próprio UC-44 (RNF-02) — não configura um achado novo não coberto por este mapa.

| UC-21-RN-05, UC-13-RN-07 | [UC-21](UC-21-cadastrar-nova-clinica.md) / [UC-13](UC-13-desativar-item-de-estoque-com-verificacao-de-reservas-ativas.md) | Corrigido e Documentado | 19/07/2026 | Lote de 2 correções de severidade Alta, ambas no commit único `91eb37e` (`fix: dois itens de alta severidade (UC-21, UC-13)`). |

✅ **Concluído (19/07/2026):** correções verificadas nesta execução por leitura direta do código-fonte (`git show 91eb37e`) e dos UCs de origem já atualizados pelo `uml-use-case-writer`:
- **UC-21-RN-05** — `POST /api/tenants/create` (`src/app/api/tenants/create/route.ts`) confirmado gravando `requirePasswordChange: true` tanto nas custom claims quanto no documento `users/{userId}` do admin de clínica recém-criado, alinhando este fluxo ao mesmo padrão já usado em UC-02/UC-28/UC-39. UC-06 (Trocar Senha Obrigatória no Primeiro Acesso) passa a se aplicar a usuários criados por este caminho, o que não ocorria antes da correção. UC-21 atualizado para **v1.1** (RN-05 marcada `[CORRIGIDO]`; item 2 da Seção 14 marcado `[RESOLVIDO]`).
- **UC-13-RN-07** — `src/app/(clinic)/clinic/inventory/[id]/page.tsx` confirmado com o novo estado `checkError`: se `checkInventoryItemReservations` falhar, `impactedProcedimentos` permanece `null` e ambos os botões de ação ("Confirmar desativação"/"Forçar exclusão") ficam ocultos — a checagem prévia deixa de ser fail-open (tratar erro como "sem reservas") e passa a ser fail-closed, com um novo botão "Tentar novamente" na UI. UC-13 atualizado para **v1.1** (RN-07 marcada `[CORRIGIDO]`; item 2 da Seção 14 marcado `[RESOLVIDO]`).

Commit `91eb37e` confirmado via `git log -1 --stat 91eb37e` nesta execução. Ambos os UCs de origem já foram atualizados pelo `uml-use-case-writer` — não há sinalização de documentação pendente para este lote.

| UC-32-RN-01, UC-04-RN-09 (ex-Q3), UC-39-RN-02 | [UC-32](UC-32-editar-ativar-e-desativar-produto-no-catalogo-master.md) / [UC-04](UC-04-fazer-login-com-redirecionamento-por-papel.md) / [UC-39](UC-39-criar-usuario-diretamente-para-clinica-via-painel-admin.md) | Corrigido e Documentado | 20/07/2026 | Lote de 3 correções de severidade Alta, todas no commit único `f6e9161` (`fix: tres itens de alta severidade (UC-32, UC-04, UC-39)`). |

✅ **Concluído (20/07/2026):** correções verificadas nesta execução por leitura direta do diff do commit (`git show f6e9161`) e dos UCs de origem já atualizados pelo `uml-use-case-writer`:
- **UC-32-RN-01** — `handleSubmit` (`src/app/(admin)/admin/products/[id]/page.tsx`) confirmado calculando `fragmentacaoAlterada = product != null && (fragmentavel !== product.fragmentavel || (fragmentavel && Number(unidadesPorEmbalagem) !== product.unidades_por_embalagem))`, e o payload de `updateMasterProduct` passa a incluir `fragmentavel`/`unidades_por_embalagem` apenas via spread condicional (`...(fragmentacaoAlterada && {...})`) — a checagem de "produto em uso" deixa de ser disparada incondicionalmente, e editar apenas campos não relacionados (nome, categoria, status) de um produto já em uso deixa de ser bloqueado. UC-32 atualizado para **v1.0.3** (RN-01 marcada `[CORRIGIDO]`, RNF-01 `[Mitigado]`, novo Fluxo 8a documentando o comportamento remanescente quando a fragmentação de fato muda).
- **UC-04-RN-09 (ex-Q3)** — `ProtectedRoute.tsx` (`checkAccess`) confirmado com o bloco `if (user && claims && claims.requirePasswordChange === true) { router.push('/change-password'); return; }`, mais um guard de renderização equivalente (`if (claims.requirePasswordChange === true) { return null; }`) — fecha o bypass de troca de senha obrigatória para uma sessão já autenticada navegando direto a qualquer rota protegida (antes essa checagem só existia no submit do `/login`). **Nota de escopo, confirmada nesta execução:** a sub-parte de "clínica suspensa" do achado original (Q3) **não foi corrigida agora porque não havia gap real** — já estava coberta por `SuspensionInterceptor.tsx` para as rotas de clínica; verificado por leitura direta do código antes de decidir o escopo do fix, não presumido. UC-04 atualizado para **v1.1.3**, com o achado renumerado de `Q3` para **RN-09** (formalizado como Regra de Negócio, não mais pergunta em aberto) e cross-referenciado em `RNF-02`.
- **UC-39-RN-02** — `DialogDescription` do diálogo "Adicionar Novo Usuário" (`src/app/(admin)/admin/tenants/[id]/page.tsx`) confirmado alterado de "O usuário receberá as credenciais por email" para um texto que deixa explícito que a senha não é enviada por e-mail e deve ser comunicada por fora do sistema. Decisão do PO: opção (a) da decisão pendente `UC-39-Decisão` (Seção 4, removida por estar resolvida) — corrigir o texto da UI em vez de implementar envio de senha em texto plano por e-mail (risco de segurança). UC-39 atualizado para **v1.2.1** (RN-02 e RNF-02 marcados `[CORRIGIDO]`/`[Mitigado]`).

Commit `f6e9161` confirmado via `git show` nesta execução. Todos os 3 UCs de origem já foram atualizados pelo `uml-use-case-writer` — não há sinalização de documentação pendente para este lote.

| UC-09-RN-02, UC-09-RN-03, UC-11-RN-07, UC-42-RN-02 | [UC-09](UC-09-aceitar-termos-legais.md) / [UC-11](UC-11-inserir-nota-fiscal-manualmente.md) / [UC-42](UC-42-executar-verificacoes-de-alertas-manualmente.md) | Corrigido e Documentado | 20/07/2026 | Lote de 4 correções de severidade Alta, todas no commit único `16877f1` (`fix: tres itens de alta severidade (UC-09, UC-11, UC-42)`), com os UCs de origem já atualizados pelo `uml-use-case-writer` no commit `182c973`. Branch `fix/uc09-uc11-uc42-alta-prioridade`, criada a partir de `develop` antes da PR #237 (`fix/uc32-uc04-uc39-alta-prioridade`) ser mesclada em `gscandelari_setup`; posteriormente sincronizada via `git merge origin/gscandelari_setup`, com o conflito deste mapa e do `README.md` resolvido como placeholder (`--theirs`) até esta auditoria reconciliar as duas linhas de trabalho. |

✅ **Concluído (20/07/2026):** correções verificadas nesta execução por leitura direta do texto de cada UC de origem (trechos de código citados nas próprias Seções 6/9/14 dos UCs, com referência ao commit `16877f1`):
- **UC-09-RN-02** — `/accept-terms` e `/clinic/setup/terms` passaram a montar um `Map<document_id, document_version>` (em vez de um `Set<document_id>`) e a calcular pendência com o mesmo critério de `usePendingTerms`/`TermsInterceptor` (`!acceptedVersion || acceptedVersion !== doc.version`), eliminando a divergência que causava o loop de redirecionamento (antigo Fluxo de Exceção 8a, agora histórico "[Corrigido]"). UC-09 atualizado para **v1.4** (RN-01/RN-02 e item 1 da Seção 14 marcados `[Resolvido]`).
- **UC-09-RN-03** — as duas páginas de aceite passaram a buscar todos os documentos `legal_documents` com `status == 'ativo'` (sem filtro `required_for_*` na própria query) e a filtrar client-side pela união `required_for_registration || required_for_existing_users`, igual a `usePendingTerms` — eliminando a segunda causa do mesmo loop (antigo Fluxo de Exceção 8b, agora histórico "[Corrigido]"). UC-09 atualizado para **v1.4** (RN-03 e item 2 da Seção 14 marcados `[Resolvido]`), no mesmo commit e mesma versão de UC-09 do item acima.
- **UC-11-RN-07** — `handleSaveNF` (`src/app/(clinic)/clinic/add-products/...`) passou a usar `writeBatch(db)`: a referência do documento da NF (`nf_imports`) e a de cada item de inventário são adicionadas ao mesmo batch via `batch.set()`, com um único `await batch.commit()` ao final — mesmo padrão de atomicidade já usado na importação via XML (UC-10). Uma falha em qualquer ponto agora rejeita o batch inteiro, sem gravação parcial com status `"success"` enganoso. UC-11 atualizado para **v1.1** (RN-07, RNF-01 e item 2 da Seção 14 marcados `[Resolvido]`/`[Corrigido]`).
- **UC-42-RN-02** — novo helper `createExpiredProductNotification` (`notificationService.ts`, mesmo padrão de `createExpiringProductNotification`/`createLowStockNotification`, `type: 'expired'`, `priority: 'urgent'`) passou a ser chamado por `checkExpiredProducts` (`alertTriggers.ts`), substituindo o placeholder (`getDocs` comentado) que apenas incrementava um contador e logava no console sem gravar nada. UC-42 atualizado para **v1.3** (RN-02 e item 2 da Seção 14 marcados `[Resolvido]`/`[Corrigido]`; RN-08 — divergência marketing x produto — não afetada por esta correção).

Todos os 3 UCs de origem já foram atualizados pelo `uml-use-case-writer` para as versões acima — não há sinalização de documentação pendente para este lote.

| UC-21-RN-03, UC-28-RN-04, UC-35-RN-04, UC-35-RN-05, UC-35-RN-06 | [UC-21](UC-21-cadastrar-nova-clinica.md) / [UC-28](UC-28-cadastrar-consultor.md) / [UC-35](UC-35-editar-configuracoes-globais-do-sistema.md) | Corrigido e Documentado | 24/07/2026 | Lote de 5 correções de severidade Alta, todas no commit único `66c75fa` (`fix: cinco itens de alta severidade (UC-21, UC-28, UC-35)`). |

✅ **Concluído (24/07/2026):** correções verificadas nesta execução por leitura direta do diff do commit (`git show --stat 66c75fa`) e dos UCs de origem já atualizados pelo `uml-use-case-writer`:
- **UC-21-RN-03** — e-mail de boas-vindas do admin de clínica (template padrão de `src/app/(admin)/admin/tenants/new/page.tsx` e a função `getPreviewEmail()`) confirmado sem a senha em texto plano no corpo, com defesa adicionada contra reintrodução manual do placeholder da senha; texto de ajuda da UI também corrigido para não sugerir que a senha será enviada por e-mail. `requirePasswordChange: true` já era gravado desde a correção do RN-05 (v1.1, 19/07/2026) — a troca obrigatória no primeiro acesso permanece garantida independentemente do conteúdo do e-mail. UC-21 atualizado para **v1.2** (RN-03 marcada `[CORRIGIDO]`).
- **UC-28-RN-04** — `src/app/api/consultants/route.ts` confirmado com `generateConsultantWelcomeEmail` sem o parâmetro `tempPassword` — a senha temporária do consultor deixou de ser incluída no HTML do e-mail de boas-vindas. `requirePasswordChange: true` já era gravado neste fluxo. UC-28 atualizado para **v1.1** (RN-04 marcada `[CORRIGIDO]`).
- **UC-35-RN-04** — `src/components/auth/ProtectedRoute.tsx` confirmado lendo `system_settings/global.maintenance_mode` e redirecionando qualquer usuário que não seja `system_admin` para a nova tela `src/app/maintenance/page.tsx`. "Modo de Manutenção" deixa de ser puramente cosmético. UC-35 atualizado para **v2.0** (major bump — a premissa central do documento, de que a tela é inteiramente desconectada, deixou de ser verdadeira; RN-04 marcada `[CORRIGIDO]`).
- **UC-35-RN-05** — `src/app/api/access-requests/route.ts` confirmado rejeitando (403) `POST` quando `system_settings/global.registration_enabled === false`. **Ressalva de escopo, já registrada no próprio UC-35 como melhoria futura, não como gap:** apenas a validação de servidor foi implementada — não há checagem espelhada de UX em `/register` (a tela continua exibindo o formulário normalmente, falhando apenas no submit). UC-35 atualizado para **v2.0** (RN-05 marcada `[CORRIGIDO]`).
- **UC-35-RN-06** — `src/hooks/useSessionTimeout.ts` confirmado lendo `system_settings/global.session_timeout_minutes` uma vez por sessão, com fallback de 15 minutos caso o campo não exista ou seja inválido — deixa de ser um valor fixo hardcoded. UC-35 atualizado para **v2.0** (RN-06 marcada `[CORRIGIDO]`). **Efeito colateral documentado em UC-04:** o mesmo commit fecha o achado cross-referenciado em `UC-04-RN-07` (que documentava esse hardcode como pré-condição do fluxo de timeout, cross-referenciando UC-35) — `UC-04-RN-07` também foi marcado `[CORRIGIDO]` pelo `uml-use-case-writer`, e UC-04 atualizado para **v1.1.4**. **Verificação de catalogação:** `UC-04-RN-07` nunca teve entrada própria na Seção 2/3 deste mapa — existia apenas como nota informativa cross-referenciada dentro do próprio texto de UC-04 (achado distinto de `UC-04-RN-09`, ex-`Q3`, já `Corrigido e Documentado` desde a v3.6, que trata do bypass de `requirePasswordChange`); como nunca foi um item catalogado como "Aberto", nenhuma linha nova foi criada nem movida no mapa para ele — apenas esta confirmação de estado.

Commit `66c75fa` confirmado via `git show --stat` nesta execução (branch `fix/uc21-uc28-uc35-alta-prioridade`, criada a partir de `develop` após o merge da branch de docs anterior, `docs/adr-backup-geografico-automatizado`). **Divergência verificada antes da atualização:** `git status` confirmado sem nenhuma alteração concorrente neste mapa ou no `README.md` — apenas os 4 UCs de origem e este mapa/README pendentes de edição nesta sessão; nenhuma reconciliação foi necessária. Todos os 5 UCs de origem já foram atualizados pelo `uml-use-case-writer` (UC-21, UC-28 e UC-35 diretamente; UC-04 como efeito colateral do fix de UC-35) — não há sinalização de documentação pendente para este lote.

---

## 7. Gaps: Landing Page vs. Sistema Real — Decisões de Produto

> **Natureza distinta desta seção:** os itens abaixo **não** nasceram de um Caso de Uso já mapeado — vieram de uma auditoria direta comparando as promessas comerciais da landing page (`public/landing/sections-hero.jsx`, `sections-product.jsx`, `sections-personas.jsx`, `sections-trust.jsx`, `sections-cta.jsx`) contra o que o sistema realmente entrega, com verificação prévia por leitura direta do código-fonte e decisão já tomada com o usuário, item a item. Por não terem origem em UC nem se encaixarem no conceito de "bug de código" com severidade classificável, ficam isolados nesta seção própria, fora da tabela de severidade da Seção 1 — mesmo tratamento dado às Decisões de Produto Pendentes (Seção 4) e ao Código Morto (Seção 5).

### 7.1 Itens a Implementar — 100% Documentados: UC-51, UC-52 e UC-53 Escritos e Aprovados; Backup Geográfico Documentado como ADR

Numeração reservada de forma sequencial e sem lacunas, a partir do maior UC existente (UC-50). **UC-51, UC-52 e UC-53 já foram escritos e aprovados** pelo `uml-use-case-writer` — ver [UC-51](UC-51-gerar-relatorios-gerenciais-avancados-e-custeio-por-procedimento.md), v1.1, [UC-52](UC-52-consultar-projecao-de-reposicao-de-estoque.md), v1.0, e [UC-53](UC-53-consultar-e-exportar-trilha-de-auditoria.md), v1.0, todos Status: Aprovado (decisões de design fechadas; código ainda não implementado) — desde a v3.11 (UC-51), a v3.12 (UC-52) e a v3.13 (UC-53) deste mapa, esses três itens deixaram de ser "apenas número reservado" e aguardam somente priorização/planejamento de implementação pelo `dev-task-manager`. **O item antes reservado como UC-54 (Backup Geográfico Automatizado) não virou um caso de uso formal** — a numeração "UC-54" foi liberada e não será reocupada por nenhum outro item desta seção. Na elicitação registrada na v3.14 deste mapa, ficou decidido junto ao usuário que este item deveria ser documentado como um **ADR** (Architecture Decision Record), não um UC Cockburn: não há ator humano nem tela envolvidos — é um processo de infraestrutura agendado (backup automatizado do Firestore + Cloud Storage via Cloud Scheduler/Cloud Functions) —, e havia decisões técnicas reais de arquitetura a formalizar, mesmo perfil do único outro ADR já existente no projeto, `ONLY_FOR_DEVS/TO_DO/ADR-automacao-qa-playwright-firebase-emulator.md` (este, porém, permanece "Planejamento — PAUSADO", com 7 perguntas em aberto — ver Seção 4 —, enquanto o novo ADR de backup já nasce com Status "Aprovado — decisões de arquitetura fechadas", apenas com a implementação de código não priorizada). Ver `ONLY_FOR_DEVS/TO_DO/ADR-backup-geografico-automatizado.md`, v1.1. **Com isso, os 6 itens originalmente reservados nesta subseção (UC-51 a UC-56 na numeração da v3.8) estão agora 100% documentados** — nenhum resta apenas como número reservado sem nenhum documento formal: os dois primeiros "UC-51" sucessivos (Módulo Procedimentos e Investimentos & ROI) foram absorvidos e documentados dentro de UC-19 (RN-08) e do próprio UC-51 final, respectivamente; os itens que assentaram como UC-51, UC-52 e UC-53 têm cada um seu arquivo próprio, escrito e aprovado; e o item que seria UC-54 (Backup Geográfico) está documentado no ADR aprovado citado acima.

> **Item descartado/absorvido (era "UC-51 — Módulo Procedimentos"):** ao investigar para escrever este UC, o `uml-use-case-writer` e o usuário constataram que o "Módulo 02 — Procedimentos" da landing page já é, na prática, a tela `/clinic/requests` (UC-16/UC-17), já existente e em uso — o gap real era muito menor do que o presumido na v3.8. Decisões finais: **(a)** "tipo de procedimento categorizado" (full face, mid face, contorno, lábios, glabela) — **não será implementado**, movido para item de correção de texto da landing (Seção 7.2); **(b)** identificador amigável de sessão (`SES-XXXXXX`, não sequencial, derivado do próprio ID do Firestore) — decisão já tomada e **documentada como RN-08 em [UC-19](UC-19-concluir-ou-cancelar-procedimento-agendado.md)** v1.1 (implementação de código ainda pendente); **(c)** painel "Histórico do lote" — remanejado para o escopo do que hoje é o novo UC-51 (ver tabela abaixo). Nenhum arquivo `UC-51-*.md` foi criado — a numeração foi liberada e as reservas seguintes foram renumeradas sem lacunas.

> **Item descartado/absorvido pela segunda vez (era "UC-51 — Investimentos & ROI"):** ao investigar para escrever este UC (segunda ocupação consecutiva do número "UC-51", já liberado uma vez pelo item acima), o `uml-use-case-writer` e o usuário decidiram, em conjunto: **(a) FIFO:** o sistema deve calcular custo médio ponderado por ordem de entrada (FIFO real) quando o mesmo produto tiver múltiplos lotes consumidos num período — lógica de cálculo nova, mas que não muda a escolha manual de lote em UC-16/UC-17 (o FIFO aqui é apenas o método de agregação do relatório); **(b) margem por categoria** — decisão de **não implementar** (não existe, e não será criado agora, nenhum campo de valor cobrado do paciente/valor de tabela no sistema); essa parte específica da promessa da landing foi movida para item de correção de texto da landing (Seção 7.2); **(c)** o motor de custeio (FIFO ponderado, custo real por procedimento, "ticket médio de custo" — sem margem) não tem tela própria, nem ator/gatilho de UI — é puramente um service consumido por outra tela; seguindo o mesmo precedente do item acima, o `uml-use-case-writer` decidiu **não criar um arquivo `UC-51-*.md` próprio** — o motor de custeio passa a ser documentado como Regras de Negócio dentro do que hoje é o novo UC-51 ("Expansão de Relatórios Gerenciais"); **(d)** ator confirmado para esse UC de Relatórios + Custeio: `clinic_admin` apenas. Nenhum arquivo `UC-51-*.md` foi criado nesta segunda rodada tampouco — a numeração foi liberada de novo e as reservas seguintes foram renumeradas sem lacunas.

> **Atualização (v3.11) — a terceira ocupação do número "UC-51" não foi descartada:** desta vez como "Expansão de Relatórios Gerenciais e Custeio por Procedimento", o `uml-use-case-writer` escreveu e o usuário aprovou o UC-51 formalmente — [UC-51](UC-51-gerar-relatorios-gerenciais-avancados-e-custeio-por-procedimento.md), v1.1, Status: Aprovado. É, portanto, o primeiro item desta subseção a sair do estado "apenas número reservado" e passar a "UC escrito e aprovado, aguardando priorização/planejamento de implementação" — ver linha atualizada na tabela abaixo. As duas notas acima (v3.9 e v3.10) permanecem como registro histórico de como se chegou a esse escopo final e não precisam de correção — apenas não são mais a última palavra sobre o estado do item.

> **Atualização (v3.12) — UC-52 escrito e aprovado, sem nenhuma rodada de descarte:** diferente do UC-51 (ocupado e liberado duas vezes antes de assentar), a reserva "UC-52 — Motor de Recomendação/Projeção de Reposição" permaneceu estável desde a v3.10 e foi diretamente escrita pelo `uml-use-case-writer` e aprovada pelo usuário como [UC-52](UC-52-consultar-projecao-de-reposicao-de-estoque.md), v1.0, Título final "Consultar Projeção de Reposição de Estoque", Status: Aprovado. Escopo fechado como projeção de **data estimada de esgotamento de estoque** por `codigo_produto` (não uma sugestão de quantidade a pedir, explicitamente fora de escopo — RN-10), calculada a partir da taxa de consumo histórico real (`Solicitacao concluida`, mesma fonte de UC-47) com janela em cascata 90→60→30 dias (RN-03, critério de suficiência de dados a validar tecnicamente, pendência não bloqueante). Consultor Rennova vinculado passa a ver a mesma projeção completa das clínicas vinculadas a ele — achado de arquitetura/segurança confirmado nesta escrita: a regra genérica já existente `tenants/{tenantId}/{document=**}` já concede essa leitura, **nenhuma mudança em `firestore.rules` é necessária** (RN-08), mesmo padrão já registrado em UC-48 (RN-06). Ver linha atualizada na tabela abaixo.

> **Atualização (v3.13) — UC-53 escrito e aprovado, sem nenhuma rodada de descarte:** assim como o UC-52, a reserva "UC-53 — Trilha de Auditoria Completa" permaneceu estável desde a v3.10 e foi diretamente escrita pelo `uml-use-case-writer` e aprovada pelo usuário como [UC-53](UC-53-consultar-e-exportar-trilha-de-auditoria.md), v1.0, Título final "Consultar e Exportar Trilha de Auditoria", Status: Aprovado. Escopo fechado: `system_admin` (visão cross-tenant) e `clinic_admin` (visão restrita ao próprio tenant) consultam e exportam (CSV e PDF) um histórico unificado de escritas administrativas sensíveis — criar/editar/ativar/desativar/suspender/mudar papel/definir senha/excluir — sobre Usuários e Consultores, Clínicas, Produtos Master, Documentos Legais e Configurações Globais, somado ao `inventory_activity` já existente (confirmado que já é gravado hoje, mas nunca foi exposto em nenhuma tela). Rastreia apenas escritas, nunca leituras/visualizações. Nova coleção `audit_log` (top-level, cross-tenant) proposta; log explícito adicionado rota a rota, mesmo padrão de `writeActivityLogs`, sem Cloud Function `onWrite` automática. Retenção indefinida, sem TTL. Trilha não é retroativa — só registra a partir da implementação. Ver linha atualizada na tabela abaixo.

> **Atualização (v3.14) — item ex-UC-54 descartado como UC e documentado como ADR:** diferente de todos os itens acima, a reserva "UC-54 — Backup Geográfico Automatizado" **não foi escrita como caso de uso**. Ao elicitar este item com o usuário, ficou definido que ele não tem ator humano nem tela — é um processo de infraestrutura agendado (backup automatizado do Firestore + Cloud Storage) — e que as decisões pendentes eram de arquitetura técnica, não de fluxo de UI/ator, exatamente o perfil de um ADR. Registrado como `ONLY_FOR_DEVS/TO_DO/ADR-backup-geografico-automatizado.md`, v1.1, Status: Aprovado — decisões de arquitetura fechadas (mecanismo `gcloud firestore export` + Cloud Scheduler/Cloud Function; réplica de `/danfe/{tenantId}/...` via Cloud Storage Transfer Service; região secundária `southamerica-west1`; retenção de 90 dias via lifecycle rule; restore mensal híbrido — automação + validação humana; custo recorrente de GCP aceito explicitamente pelo usuário, US$ 5-20/mês estimados). Implementação de código **não priorizada** — decisão explícita de "documentar agora, implementar depois". A numeração "UC-54" não será reutilizada para este item; a linha correspondente na tabela abaixo passa a refletir "Descartado como UC — documentado como ADR", sem nenhum arquivo `UC-54-*.md`. Nota cruzada: o outro ADR do projeto, `ADR-QA-AUTOMATION` (Seção 4), permanece pausado/sem decisão tomada — este novo ADR de backup, diferente daquele, já nasce fechado/aprovado, por isso não é tratado como uma "decisão pendente" na Seção 4.

| UC Reservado | Módulo/Funcionalidade Prometida | Gap Identificado (Landing vs. Sistema Real) | Estado Atual do Código | Observação |
|---|---|---|---|---|
| UC-51 (ex-UC-52) — **Escrito e Aprovado** ([UC-51-gerar-relatorios-gerenciais-avancados-e-custeio-por-procedimento.md](UC-51-gerar-relatorios-gerenciais-avancados-e-custeio-por-procedimento.md), v1.1) | Expansão de Relatórios Gerenciais (agora também incluindo o motor de custeio) | Landing promete PDFs executivos, fechamento mensal (1 página), mix de produtos por trimestre, custeio FIFO por lote, custo direto por procedimento e ticket médio por linha de produto (margem por categoria **fora de escopo**, ver Seção 7.2). | `reportService.ts` hoje só oferece 3 relatórios pontuais com export CSV/Excel (valor de estoque, vencimento, consumo — ver UC-47). Nenhum serviço de cálculo de custo por procedimento existe hoje — **código ainda não implementado**, mas o desenho já está formalmente documentado e aprovado. | **Status: UC escrito e aprovado, aguardando priorização/planejamento de implementação pelo `dev-task-manager`** — não é mais apenas número reservado. Escopo final fechado em 4 capacidades: (i) Custo por Procedimento (motor de custeio — RN-01/RN-02/RN-03, custo médio ponderado pelo **consumo real** de cada lote, não uma leitura contábil por camadas de entrada independente do lote debitado); (ii) Histórico do Lote (RN-05, restrito a Solicitações `concluida`), absorvido do primeiro UC-51 extinto (Módulo Procedimentos); (iii) Fechamento Executivo Mensal e (iv) Mix de Produtos por Trimestre — ambos exportáveis em PDF (biblioteca ainda não decidida, RNF-01, pendência técnica não bloqueante). Estende a tela `/clinic/reports` já existente (UC-47). Ator único confirmado: `clinic_admin`. Margem por categoria explicitamente fora de escopo (RN-04). O motor de custeio foi cogitado como UC próprio duas vezes e absorvido aqui ambas as vezes por não ter ator/gatilho/tela própria — mesmo padrão de "sem UC dedicado quando o conteúdo é puramente service" já usado no projeto. Base de dados para custeio continua sendo a `Solicitacao`/UC-16/UC-17 já existente, sem mudança estrutural. |
| UC-52 (ex-UC-53) — **Escrito e Aprovado** ([UC-52-consultar-projecao-de-reposicao-de-estoque.md](UC-52-consultar-projecao-de-reposicao-de-estoque.md), v1.0) | Motor de Recomendação/Projeção de Reposição (redefinido como "Consultar Projeção de Reposição de Estoque") | Landing promete "sugestão de reposição calculada pelo sistema" e "projeção de necessidade de reposição" baseada em consumo histórico. | Nenhum motor de recomendação/projeção existe hoje — busca por "reposição", "forecast", "reorder" no código só retorna a própria landing page. **Código ainda não implementado**, mas o desenho já está formalmente documentado e aprovado. | **Status: UC escrito e aprovado, aguardando priorização/planejamento de implementação pelo `dev-task-manager`** — não é mais apenas número reservado. Escopo fechado: projeção de **data estimada de esgotamento** por `codigo_produto` (RN-01/RN-02/RN-05/RN-06), calculada a partir da taxa de consumo histórico real (`Solicitacao concluida`, mesma fonte de UC-47), com janela de histórico em cascata 90→60→30 dias e critério de suficiência de dados a validar tecnicamente (RN-03, pendência não bloqueante). Cálculo 100% client-side, sob demanda ao carregar a tela, sem cron novo (RN-07). Novo card no Dashboard (clínica e Consultor) + nova tela "Projeções Gerais" em `/clinic/inventory/projections` e `/consultant/clinics/{tenantId}/projections`. Consultor Rennova vinculado vê a mesma projeção completa das clínicas vinculadas — confirmado que a regra genérica `tenants/{tenantId}/{document=**}` já permite essa leitura, nenhuma mudança de `firestore.rules` necessária (RN-08). "Sugestão de quantidade a pedir" (Módulo 05 da landing) explicitamente fora de escopo desta versão (RN-10). |
| UC-53 (ex-UC-54) — **Escrito e Aprovado** ([UC-53-consultar-e-exportar-trilha-de-auditoria.md](UC-53-consultar-e-exportar-trilha-de-auditoria.md), v1.0) | Trilha de Auditoria Completa (redefinida como "Consultar e Exportar Trilha de Auditoria") | Landing promete "quem viu, quem alterou, quando — exportável a qualquer momento". | Só existe `inventory_activity` (`writeActivityLogs`, `solicitacaoService.ts`), que registra apenas movimentações de estoque (não leituras/visualizações) e não tem exportação. **Código ainda não implementado**, mas o desenho já está formalmente documentado e aprovado. | **Status: UC escrito e aprovado, aguardando priorização/planejamento de implementação pelo `dev-task-manager`** — não é mais apenas número reservado. Escopo fechado: rastreia apenas **escritas administrativas sensíveis** (criar/editar/ativar/desativar/suspender/mudar papel/definir senha/excluir), nunca leituras/visualizações, sobre Usuários e Consultores, Clínicas, Produtos Master, Documentos Legais e Configurações Globais — somado ao `inventory_activity` já existente. Visão `system_admin` cross-tenant e `clinic_admin` restrita ao próprio tenant. Exportação em CSV e PDF. Nova coleção `audit_log` (top-level) proposta; log explícito rota a rota, sem Cloud Function `onWrite` automática. Retenção indefinida (sem TTL). Não é retroativa. |
| ~~UC-54~~ (ex-UC-55) — **Descartado como UC, documentado como ADR** (`ONLY_FOR_DEVS/TO_DO/ADR-backup-geografico-automatizado.md`, v1.1) | Backup Geográfico Automatizado | Landing promete backup geográfico diário, retenção de 90 dias e restore testado mensalmente. | Nenhuma Cloud Scheduled Function nem configuração de backup no repositório (`firebase.json`, `functions/src/`) hoje. **Código ainda não implementado**, mas o desenho já está formalmente documentado e aprovado. | **Status: descartado como caso de uso, documentado como ADR aprovado** — não há ator humano nem tela envolvidos (processo de infraestrutura agendado), por isso o `uml-use-case-writer` não escreveu um `UC-54-*.md`; as decisões técnicas de arquitetura foram formalizadas em um ADR (mesmo perfil do único outro ADR do projeto). Status do ADR: "Aprovado — decisões de arquitetura fechadas; implementação não priorizada (aguardando janela futura)". Escopo fechado: `gcloud firestore export` agendado via Cloud Scheduler + Cloud Function, gravando em bucket na região secundária `southamerica-west1` (Chile); réplica do bucket `/danfe/{tenantId}/...` via Cloud Storage Transfer Service; lifecycle rule de retenção de 90 dias; restore mensal híbrido (automação + validação humana); custo recorrente de GCP (US$ 5-20/mês estimados) aceito explicitamente pelo usuário, fora do padrão "Zero DevOps" do `CLAUDE.md`. Aguarda apenas priorização de implementação pelo `dev-task-manager`, sem nenhuma branch aberta. |

### 7.2 Correção de Texto da Landing Page — Baixa Prioridade

Não geram UC de implementação — são tarefas de conteúdo/copy, sem prazo definido.

| Item | Promessa na Landing | Comportamento Real do Sistema | Ação Decidida |
|---|---|---|---|
| Alertas de vencimento "60/30/15 dias" | Landing promete 3 camadas fixas de alerta de vencimento. | Sistema usa um único threshold configurável (`expiry_warning_days`, padrão 30 dias — `src/lib/services/alertTriggers.ts`, `src/types/notification.ts`). | Corrigir o texto da landing para refletir o threshold único configurável real. |
| Multi-unidade/multi-sede | Landing promete "multi-unidade nativamente: inventário separado por sede, transferências internas auditadas, consolidação financeira no grupo". | 1 tenant = 1 clínica; sem nenhum conceito de sede/filial (`src/types/tenant.ts` não tem `sede_id`/`unit_id`/`branch`/`filial`). | Corrigir o texto da landing, removendo a promessa. |
| Onboarding "importação do inventário atual via planilha" | Landing sugere importação inicial via planilha. | Contradiz decisão de produto já documentada no `CLAUDE.md` — importação de NF é exclusivamente via XML NF-e SEFAZ v4.00 (`src/lib/parseNfeXml.ts`); importação via PDF/planilha foi removida definitivamente. | Corrigir o texto da landing para descrever a importação via XML de NF-e já entregue, em vez de "planilha". |
| Módulo 02 — Procedimentos: tipos categorizados | Landing promete "Modelos por tipo (full face, mid face, contorno, lábios, glabela…)". | Decisão do usuário (v3.9): tipo de procedimento categorizado **não será implementado** — não é o intuito inicial do produto; a tela real (`/clinic/requests`, UC-16/UC-17) usa descrição livre + `Protocolo` opcional, sem tipos clínicos fixos. | Remover/ajustar a promessa de "Modelos por tipo (full face, mid face, contorno, lábios, glabela…)" em `sections-product.jsx` (Módulo 02). |
| Módulo 03 — Investimentos & ROI: margem por categoria | Landing promete "Margem por categoria (HA, toxina, bioestimulador…)". | Decisão do usuário (v3.10): margem **não será implementada** — o sistema não tem (e não terá, por ora) nenhum dado de valor cobrado do paciente/valor de tabela necessário para calcular margem real; o novo UC-51 (Expansão de Relatórios Gerenciais) passa a cobrir custeio FIFO ponderado e "ticket médio de custo", mas explicitamente sem margem. | Remover/ajustar a promessa de "Margem por categoria (HA, toxina, bioestimulador…)" em `sections-product.jsx` (Módulo 03). |

### 7.3 Decisão Adiada — Pendências em Aberto (sem ação ainda)

Nem UC, nem correção de landing — apenas registrado como pendência para revisão futura.

| Item | Promessa na Landing | Situação Real | Motivo do Adiamento |
|---|---|---|---|
| Alegações específicas de criptografia | "Rotação trimestral de chaves" e "restore testado mensalmente" (TLS 1.3/AES-256 são genericamente verdadeiros por padrão de infraestrutura Firebase/GCP). | Rotação de chaves e teste de restore não são processos configurados nem verificáveis hoje. | Decisão de não implementar rotação de chaves nem corrigir o texto agora — revisão futura. |
| App nativo iOS/Android (Capacitor) + modo offline 24h | Landing sugere app nativo com modo offline. | Sistema é 100% web hoje; sem `@capacitor/core` no `package.json`, sem pastas `ios/`/`android/`, sem nenhuma implementação offline (apesar do `CLAUDE.md` citar Capacitor no stack planejado). | Adiada. |
| Integrações/API REST documentada + conectores nativos | Landing promete "conectores nativos para os principais sistemas brasileiros". | Nenhuma rota de integração/webhook pública em `src/app/api` hoje. | Adiada. |

---

## 8. Histórico de Versões

| Versão | Data | Autor | O que mudou |
|--------|------|-------|--------------|
| 1.0 | 15/07/2026 | uc-issues-tracker | Levantamento inicial a partir de UC-01 a UC-49. 146 itens classificados por severidade (6 Crítica, 23 Alta, 35 Média, 82 Baixa), 12 decisões de produto pendentes e 16 itens de código morto/rotas órfãs consolidados. |
| 1.1 | 15/07/2026 | uc-issues-tracker (revisão com o PO) | Item 1/6 da revisão dos críticos: decisão de descartar a correção da Auditoria de Inventário (UC-14) em favor de remover a funcionalidade inteira (ferramenta de experimentação, projeto em pré-MVP). 4 itens de UC-14 movidos para "Descartado" (1 Crítica, 1 Alta, 1 Média, 1 Baixa); rota órfã correspondente anotada na Seção 5; nova entrada na Seção 6 com a ação de código pendente (remoção) e a sinalização futura para `uml-use-case-writer` marcar UC-14 como Obsoleto quando a remoção for implementada. |
| 1.2 | 15/07/2026 | uc-issues-tracker (revisão com o PO) | Item 2/6 da revisão dos críticos: decisão de reconectar o botão "Suspender" de consultor (UC-29) à rota `DELETE /api/consultants/[id]` já existente, em vez de reforçar o `PUT`. UC-29-RN-01 movido de "Aberto" para "Em Correção" (1 Crítica); decisão pendente correspondente removida da Seção 4 (não é mais pendente); rota `DELETE` anotada na Seção 5 como prestes a deixar de ser código morto; nova entrada na Seção 6 com a ação de código pendente e a sinalização futura para `uml-use-case-writer`/`uc-issues-tracker`. |
| 1.3 | 15/07/2026 | uc-issues-tracker (revisão com o PO) | Item 3/6 da revisão dos críticos: decisão de bloquear a exclusão de Documento Legal (UC-34) quando houver aceites registrados em `user_document_acceptances`, em vez de permitir hard delete irrestrito. UC-34-RN-03 movido de "Aberto" para "Em Correção" (1 Crítica); nova entrada na Seção 6 com a ação de código pendente e a sinalização futura para `uml-use-case-writer`/`uc-issues-tracker`. |
| 1.4 | 15/07/2026 | uc-issues-tracker (revisão com o PO) | Item 4/6 da revisão dos críticos: decisão de construir `PUT /api/users/[id]` (rota nova) sincronizando custom claims e desabilitando Auth para "Editar Usuário" (UC-36), empacotando junto a checagem de "último clinic_admin" (RN-07) com confirmação explícita do System Admin. UC-36-RN-02 e UC-36-RN-03 movidos de "Aberto" para "Em Correção" (2 Crítica); UC-36-RN-07 também movido para "Em Correção" (1 Média, empacotado); decisão pendente correspondente removida da Seção 4; nova entrada na Seção 6 com a ação de código pendente e a sinalização futura para `uml-use-case-writer`/`uc-issues-tracker`. |
| 1.5 | 15/07/2026 | uc-issues-tracker (revisão com o PO) | Item 5/6 (último item crítico da Seção 2) da revisão dos críticos: decisão de corrigir apenas o bug `updateDoc`→`setDoc` em UC-43, sem religar a navegação de `/clinic/settings` por ora — decisão de automatizar os alertas (Scheduled Function) fica para depois. UC-43-RN-01/02 movido de "Aberto" para "Em Correção" (1 Crítica, **zera a coluna Aberto da severidade Crítica**). Durante a revisão, o PO identificou um achado novo fora do escopo dos UCs: a landing page do produto promete "alertas automáticos de vencimento" e "60/30/15 dias", o que não corresponde ao comportamento real (100% manual, limiar único configurável) documentado em UC-42/UC-43. Achado registrado na Seção 6 e enviado para mapeamento formal via `uml-use-case-writer` (atualização de UC-42 e UC-43 em execução paralela) — pendente de incorporação completa a este mapa numa próxima atualização. **Com este item, todos os 6 itens críticos da Seção 2 têm decisão registrada: 1 Descartado (UC-14, remoção de funcionalidade) e 5 Em Correção (UC-29, UC-34, UC-36×2, UC-43).** |
| 1.6 | 15/07/2026 | uc-issues-tracker (revisão com o PO) | Mapeamento formal concluído pelo `uml-use-case-writer`: UC-42 (v1.1→1.2) e UC-43 (v1.1→1.2) ganharam RN-08 documentando a divergência landing page vs. sistema real de alertas (Seção 6 atualizada de "pendente" para "concluído"). Nova linha na Seção 4 (`UC-42-RN-08 / UC-43-RN-08`) formaliza a decisão de produto pendente com as duas opções (automatizar vs. ajustar copy da landing page). Registrados, sem mapear ainda, dois achados relacionados em `sections-personas.jsx` (linhas 7 e 171) que sugerem a mesma promessa de notificação automática — fica como sugestão para o PO decidir se merecem revisão própria. |
| 1.7 | 15/07/2026 | uc-issues-tracker (revisão com o PO) | Decisão tomada sobre `UC-42-RN-08 / UC-43-RN-08`: ajustar a copy da landing page agora (opção b), tratando a automação real (opção a) como item de roadmap pós-MVP, não descartado. Linha removida da Seção 4 (não é mais pendente) e movida para a Seção 6 com status "Decidido — copy" e a ação de código pendente (editar `sections-product.jsx`). |
| 1.8 | 15/07/2026 | uc-issues-tracker (revisão com o PO) | `sections-product.jsx` editado nesta sessão (linhas 96 e 99) — copy corrigida para remover as promessas de automação e escalonamento 60/30/15. `UC-42-RN-08 / UC-43-RN-08` movido para "Corrigido e Documentado" na Seção 6. **Ciclo de revisão dos 6 itens críticos + achado da landing page está completo.** |
| 1.9 | 16/07/2026 | uc-issues-tracker (Modo B) | Correção verificada em código: `UC-43-RN-01/02` (Crítica) movido de "Em Correção" para "Corrigido e Documentado". `src/lib/services/notificationService.ts` lido nesta execução e confirmado que `initializeNotificationSettings` e o fallback de `saveNotificationSettings` usam `setDoc` (commit `a38e581`, branch `bugfix/notification-settings-setdoc`). `uml-use-case-writer` já atualizou UC-43 para v1.2.1. Resumo Executivo (Seção 1) ajustado: Crítica — Em Correção 5→4, Corrigido e Documentado 0→1; Total geral — Em Correção 6→5, Corrigido e Documentado 0→1. Item atualizado nas Seções 2 e 6, e no destaque nº 3 da Seção 1. |
| 1.10 | 16/07/2026 | uc-issues-tracker (Modo B) | Correção verificada em código: `UC-29-RN-01` (Crítica) movido de "Em Correção" para "Corrigido e Documentado". `src/app/api/consultants/[id]/route.ts` (handlers `DELETE`/`PUT`), `src/app/(admin)/admin/consultants/page.tsx` (`handleSuspend`/`handleReactivate`) e `src/app/api/consultants/route.ts` (filtro `status=suspended`) lidos nesta execução — confirmado que `DELETE` desabilita a conta no Firebase Auth e zera o claim `active` preservando as demais claims reais (via `adminAuth.getUser`), que `PUT` restaura simetricamente na reativação, que o botão "Suspender" chama `DELETE` e "Reativar" chama `PUT`, e que o filtro "Suspensos" já inclui `'inactive'`. Commits: `b08fdeb`, `dbf128f`, `d167883` (RN-01/RN-02) e `3ab00b6` (achado adicional RN-07, encontrado e corrigido no mesmo ciclo, nunca esteve "Aberto" neste mapa). `uml-use-case-writer` já atualizou UC-29 para v1.2.1. **Gerada em paralelo à v2.0 (branch separada) — reconciliada na v2.1.** Item atualizado nas Seções 2, 5, 6 e no destaque nº 2 da Seção 1. **Nota para a próxima execução em Modo A:** UC-29-RN-08 (ordem de escrita assimétrica Firestore/Auth entre `DELETE` e `PUT`-reativação) é um achado novo, ainda não rastreado como item formal neste mapa — deve ser incorporado como novo item "Aberto" (Débito técnico, provável severidade Média) na próxima passada de levantamento. |
| 2.0 | 16/07/2026 | uc-issues-tracker (Modo B) | Correção verificada em código: `UC-34-RN-03` (Crítica) movido de "Em Correção" para "Corrigido e Documentado". `src/app/(admin)/admin/legal-documents/page.tsx` lido nesta execução e confirmado que `handleDelete` consulta `user_document_acceptances` por `document_id` (com `limit(1)`) e bloqueia o `deleteDoc` com toast explicativo quando há qualquer aceite registrado (commit `4561a2a`, branch `bugfix/block-legal-document-deletion-with-acceptances`). `uml-use-case-writer` já atualizou UC-34 para v1.0.1 e UC-09 para v1.3.1 (referência cruzada). **Gerada em paralelo à v1.10 (branch separada) — reconciliada na v2.1.** Item atualizado nas Seções 2 e 6, e no destaque nº 5 da Seção 1. |
| 2.1 | 16/07/2026 | Guilherme Scandelari (resolução de merge, PR #201 → gscandelari_setup) | **Reconciliação das versões 1.10 e 2.0**, que divergiram em paralelo a partir da v1.9 (branches `bugfix/suspend-consultant-reconnect-delete` e `bugfix/block-legal-document-deletion-with-acceptances`, cada uma resolvendo um item Crítica diferente). Conflito real limitado a este arquivo (demais arquivos do merge resolveram automaticamente sem conflito). Resumo Executivo (Seção 1) corrigido para refletir **as duas resoluções somadas**, não apenas uma: Crítica — Em Correção 4→2 (não 3, como cada branch calculou isoladamente), Corrigido e Documentado 1→3 (não 2); Total geral — Em Correção 5→3, Corrigido e Documentado 1→3. Seção 2 com as duas linhas (`UC-29-RN-01`, `UC-34-RN-03`) marcadas "Corrigido e Documentado". Seções 5 e 6 já continham as duas entradas completas (mescla automática sem conflito, por serem inserções em pontos distintos do arquivo). |
| 2.2 | 17/07/2026 | uc-issues-tracker (Modo B) | Correção verificada em código: `UC-36-RN-02` e `UC-36-RN-03` (Crítica) e `UC-36-RN-07` (Média, empacotado) movidos de "Em Correção" para "Corrigido e Documentado". `src/app/api/users/[id]/route.ts` (nova rota `PUT`) e `src/app/(admin)/admin/users/page.tsx` (`handleSaveUser`) lidos nesta execução e confirmados: sincronização de custom claims (`setCustomUserClaims`), desabilitação real da conta (`updateUser({disabled})`) e checagem de "último `clinic_admin` ativo" (409 + confirmação) implementadas e em uso pelo fluxo principal. Commits `2c7b023`, `e0f0e74`, `69bc944` (branch `bugfix/sync-user-claims-on-edit`) confirmados via `git log`. `uml-use-case-writer` já atualizou UC-36 para v1.1. **Gerada em paralelo à v2.1 (branch separada, criada a partir da v1.9, antes das resoluções de UC-29/UC-34 chegarem a esta cópia do arquivo) — reconciliada na v2.3.** Item atualizado nas Seções 1, 2, 3 e 6 (apenas a parte referente a UC-36; os números de UC-29/UC-34 desta execução ficaram desatualizados e foram corrigidos na reconciliação v2.3). |
| 2.3 | 17/07/2026 | Guilherme Scandelari (resolução de merge, branch `bugfix/sync-user-claims-on-edit` → `gscandelari_setup`) | **Reconciliação final dos 4 itens críticos priorizados.** A v2.2 (UC-36, gerada numa branch baseada na v1.9) e a v2.1 (UC-29 + UC-34, já mescladas em `gscandelari_setup`) divergiram em paralelo — mesmo padrão já visto na reconciliação anterior (v2.1). Resumo Executivo (Seção 1) corrigido para refletir **as três resoluções somadas**: Crítica — Aberto 0, Em Correção 2→0, Corrigido e Documentado 3→5; Média — Em Correção 0, Corrigido e Documentado 1 (inalterado, único item Média já estava correto); Total geral — Em Correção 0, Corrigido e Documentado 6. **Com esta reconciliação, a coluna "Em Correção" da severidade Crítica finalmente zera** — os 4 itens críticos priorizados na revisão com o PO (UC-14 descartado; UC-29, UC-34, UC-36 corrigidos e documentados) estão todos com status final registrado. Seção 2 com as 4 linhas (`UC-29-RN-01`, `UC-34-RN-03`, `UC-36-RN-02`, `UC-36-RN-03`) marcadas "Corrigido e Documentado". Seção 6 já continha as três entradas completas (mescla automática sem conflito). **Ciclo completo encerrado:** mapeamento (v1.0) → revisão de priorização com o PO (v1.1-v1.8) → implementação de código dos 4 itens em branches paralelas → documentação e reconciliação (v1.9-v2.3). |
| 2.4 | 17/07/2026 | uc-issues-tracker | Registrado novo item na Seção 4, `ADR-QA-AUTOMATION` — não originado de nenhum UC, mas de uma proposta de processo/infraestrutura discutida diretamente com o usuário durante a revisão dos 4 bugfixes críticos (item anterior, v2.3). ADR em `ONLY_FOR_DEVS/TO_DO/ADR-automacao-qa-playwright-firebase-emulator.md` (v1.0, "Planejamento — PAUSADO deliberadamente") propõe um "Agente de QA" (Playwright + Firebase Emulator Suite) para substituir a justificativa de "mock frágil do Admin SDK" usada para pular testes automatizados; 7 perguntas em aberto na Seção 10.1 do ADR, nenhuma decisão tomada. Pedido explícito do usuário: revisar este item **antes de iniciar qualquer nova implementação** — sinalizado com destaque visual na Seção 4 ("Passo 0"). Sem alteração de status de nenhum item já existente; contagem de decisões de produto pendentes ajustada de 12 para 13 (nota do cabeçalho da Seção 1 e cabeçalho do documento). |
| 2.5 | 17/07/2026 | uc-issues-tracker | Novo achado registrado (Seção 3, Autenticação/Acesso), descoberto durante validação manual pós-fix de UC-29/UC-36, ainda não documentado em nenhum UC de origem: a tela de login não trata o código `auth/user-disabled` do Firebase Auth (`src/hooks/useAuth.ts:75-82` repassa `error.message` bruto em vez de `error.code`; `translateFirebaseError` em `login/page.tsx:136-153` não tem `case` para `user-disabled`, caindo no `default` e exibindo a mensagem crua em inglês do SDK). Achado só se tornou relevante em produção depois que UC-29 (suspensão de consultor) e UC-36 (status de usuário de clínica) passaram a desabilitar a conta de fato (`disabled: true`) no Firebase Auth — antes das correções desses dois UCs, esse code path era praticamente inatingível. Classificado como Achado de UX / Média (bloqueio de acesso funciona corretamente; a lacuna é só a mensagem exibida). ID provisório `UC-29-RN-09 / UC-36-RN-10` — numeração final de RN cabe ao `uml-use-case-writer` quando os UCs forem atualizados (ver sinalização ao final desta execução). Nenhuma correção implementada ainda — apenas mapeado. Resumo Executivo (Seção 1) ajustado: Média — Aberto 33→34, Total 35→36; Total geral — Aberto 136→137, Total 146→147. |
| 2.6 | 17/07/2026 | uc-issues-tracker | Consolidado o novo UC-50 (Consultar Inventário e Detalhe do Item de Estoque, v1.0, Rascunho) — primeiro UC dedicado às telas `/clinic/inventory` e `/clinic/inventory/[id]`. 6 achados extraídos das Seções 9/14 do UC-50: `UC-50-RN-03` (Bug funcional, Alta — Dashboard/`NotificationBell` navegam com `?filter=expired`/`?filter=low-stock`, mas `InventoryView.applyFilter` só reconhece `expiring`/`low_stock`/`out_of_stock`; o filtro pretendido nunca é aplicado, silenciosamente); `UC-50-RN-01`, `UC-50-RN-02` e `UC-50-RN-07` (Divergência texto/UI vs lógica, Média — rótulos "Total de Produtos" e "Valor Total em Estoque", e o badge de status de estoque, com semântica/cálculo diferente entre a lista, o detalhe e a tela de Relatórios/UC-47); `UC-50-RN-04` (Achado de UX, Baixa — texto de estado vazio da lista ainda referencia upload de DANFE em PDF, fluxo removido do sistema); `UC-50-RN-05` (Decisão de produto pendente, Seção 4 — item desativado nunca aparece na lista, mas seu detalhe continua acessível por URL direta, sem filtro de `active`). `UC-50-RN-06` e `UC-50-RN-08` não geraram item no mapa — são confirmações de comportamento (combinação de filtros por E lógico; coincidência do limite padrão "10" com outros mecanismos), sem achado correspondente na Seção 14 do UC. Todos os itens novos classificados no Módulo Inventário. Resumo Executivo ajustado: Alta — Aberto 22→23, Total 23→24; Média — Aberto 34→37, Total 36→39; Baixa — Aberto 81→82, Total 82→83; Total geral — Aberto 137→142, Total 147→152. Decisões de produto pendentes 13→14. Nota de Status dos UCs de origem atualizada (11/49 → 12/50, incluindo UC-50 em Rascunho). |
| 2.7 | 18/07/2026 | uc-issues-tracker (Modo B) | Lote de 5 correções de severidade Baixa verificadas em código e documentação, todas do commit único `53df743` (`fix: lote de correções de baixa severidade (UC-04, UC-08, UC-30, UC-37, UC-47)`), com os UCs de origem já atualizados pelo `uml-use-case-writer`: `UC-04-Q1` (salto duplo de redirecionamento do `clinic_consultant` — UC-04 v1.1.2), `UC-08-RNF-02` (`confirm()` nativo → `AlertDialog` — UC-08 v1.2.1), `UC-30-RN-06` (`error.message` bruto do SDK em erro 500 — UC-30 v1.1.1), `UC-37-RN-06` (mesmo achado de UC-30, rota de usuários de clínica — UC-37 v1.0.1) e `UC-47-RNF-01` (`alert()` nativo → `toast` padrão — UC-47 v1.0.1). Todos os 5 movidos de "Aberto" para "Corrigido e Documentado" diretamente (não passaram por "Em Correção"/"Corrigido — doc pendente" neste ciclo, pois a documentação já havia sido atualizada antes desta execução). Resumo Executivo (Seção 1) ajustado: Baixa — Aberto 82→77, Corrigido e Documentado 0→5 (Total 83, inalterado); Total geral — Aberto 142→137, Corrigido e Documentado 6→11 (Total 152, inalterado). Itens atualizados nas Seções 3 e 6. |
| 2.8 | 18/07/2026 | uc-issues-tracker (Modo B) | Segundo lote de correções de severidade Baixa verificadas em código e documentação, todas do commit único `ec31c27` (`fix: segundo lote de correções de baixa severidade (UC-22, UC-30, UC-37, UC-38, UC-41)`), com os UCs de origem já atualizados pelo `uml-use-case-writer`: `UC-38-RN-04` e `UC-41-RN-04` (tratamento de `auth/invalid-credential` na reautenticação, mesma correção em rotas irmãs — UC-38 v1.0.1, UC-41 v1.0.1), `UC-30-RN-03` e `UC-37-RN-03` (UI passou a exibir a mensagem de sucesso diferenciada retornada pela API ao definir senha, mesma correção em rotas irmãs — UC-30 v1.1.2, UC-37 v1.0.2). Os 4 movidos de "Aberto" para "Corrigido e Documentado". **Item adicional do mesmo commit, `UC-22-RN-08`, tratado como resolução parcial:** apenas a parte de tratamento de erro foi corrigida (`alert()` → `setError` inline em `admin/tenants/page.tsx`, alinhando com a tela de detalhe); a ausência de mensagem de sucesso ao desativar pela listagem permanece sem correção, registrada como pendência aberta no próprio UC-22 (v1.2.1, seção 14). Por não existir status formal de "Parcialmente corrigido" neste mapa, o item **permaneceu `Aberto`**, com o resumo do achado reescrito na Seção 3 para refletir com precisão o que já foi corrigido e o que ainda falta, e detalhamento completo adicionado na Seção 6 — sem alteração de contagem para este item. Resumo Executivo (Seção 1) ajustado: Baixa — Aberto 77→73, Corrigido e Documentado 5→9 (Total 83, inalterado); Total geral — Aberto 137→133, Corrigido e Documentado 11→15 (Total 152, inalterado). Itens atualizados nas Seções 3 e 6. |
| 2.9 | 18/07/2026 | uc-issues-tracker (Modo A — auditoria completa) | **Reconstrução completa do mapa a partir da releitura dos 50 UCs (UC-01 a UC-50), pedida explicitamente pelo usuário após um merge de duas branches que haviam atualizado este arquivo de forma independente a partir da mesma base (v2.8)** — a versão mantida como placeholder no merge estava correta para 49 dos 50 UCs, mas **desatualizada para UC-22**, que teve duas rodadas inteiras de correção não refletidas aqui: **v2.0 (commit `90b8a6f`)** — reconexão do mecanismo rico de suspensão de clínica (`SuspendTenantDialog`/`ReactivateTenantDialog`) como único caminho de UI, cascata real sobre usuários (claims + Firestore), paridade "Reativar" na listagem e auditoria completa, corrigindo `UC-22-RN-03/04/05/07` e `UC-22-RNF-01` — e **v2.0.1 (commit `529adc3`)** — remoção do campo "Status" redundante do formulário de edição (fechando `UC-22-RN-02`, achado elevado na própria revisão da v2.0, e `UC-22-RN-06`/`RN-08` na prática) e adição de `adminAuth.updateUser({disabled})` à cascata (fechando `UC-22-RNF-05`, achado técnico novo da v2.0). `UC-22-RN-05`, `UC-22-RN-06` (Alta) e `UC-22-RN-07`, `UC-22-RN-08`, `UC-22-RNF-01` (Baixa) movidos de "Aberto" para "Corrigido e Documentado"; `UC-22-RN-02` e `UC-22-RNF-05` registrados apenas na Seção 6 (nasceram e já foram corrigidos dentro do mesmo ciclo — mesmo precedente de `UC-29-RN-07` —, nunca contabilizados como "Aberto"); `UC-22-Decisão` removida da Seção 4 (resolvida). Extração baseada na documentação do próprio UC-22 v2.0.1 (Modo A — não houve releitura do código-fonte da aplicação nesta execução). **Os outros 49 UCs foram reconferidos e não apresentaram nenhuma divergência de conteúdo** em relação ao que já estava registrado neste mapa (confirmado por comparação de cabeçalho/versão de cada UC e por busca de marcadores `CORRIGIDO`/`RESOLVIDO`/achado em todos os 50 arquivos). Também finalizada a numeração antes provisória do achado compartilhado `UC-29-RN-09 / UC-36-RN-10` (confirmada por leitura direta de ambos os UCs) e removido um parágrafo de nota desatualizado na Seção 6 (escrito num ponto intermediário do ciclo de correções dos críticos, antes de UC-29/UC-34 terem sido confirmados como corrigidos — já contradito pelas próprias entradas acima dele na mesma seção). Resumo Executivo (Seção 1) ajustado: Alta — Aberto 23→21, Corrigido e Documentado 0→2 (Total 24, inalterado); Baixa — Aberto 73→70, Corrigido e Documentado 9→12 (Total 83, inalterado); Total geral — Aberto 133→128, Corrigido e Documentado 15→20 (Total 152, inalterado). **Segunda correção de consistência, sem relação com UC-22:** a contagem de "decisões de produto pendentes" do cabeçalho vinha sendo carregada como delta acumulado entre versões (12→13→14) sem recontagem das linhas reais da Seção 4; contada diretamente nesta auditoria, a tabela tem **11 linhas** (10 UCs + 1 ADR), não 14/13 como constava. Corrigido para 11 (10 UC + 1 ADR) no cabeçalho e na Seção 1 — nenhuma linha foi removida além de `UC-22-Decisão` (resolvida); a diferença remanescente é a correção do erro de contagem em si, não uma remoção adicional de itens. Itens atualizados nas Seções 1, 2, 3, 4 e 6. |
| 3.0 | 18/07/2026 | uc-issues-tracker (Modo B) | Terceiro lote de correções de severidade Baixa verificadas em código e documentação, todas do commit único `2ddebd6` (`fix: terceiro lote de correções de baixa severidade (UC-32, UC-38, UC-41, UC-44)`), com os UCs de origem já atualizados pelo `uml-use-case-writer`: `UC-38-RN-03` e `UC-41-RN-03` (bloqueio de "nova senha igual à senha atual" na troca de senha do próprio usuário, mesma correção em rotas irmãs — UC-38 v1.0.2, UC-41 v1.0.2), `UC-32-RN-06` (opção "Sem categoria" adicionada ao `<Select>` de edição do catálogo master, mais um bug adicional corrigido no mesmo commit em `updateMasterProduct` — categoria nunca era de fato removida no Firestore por ignorar `category === undefined`; corrigido com `deleteField()` — UC-32 v1.0.1) e `UC-44-RN-07` (confirmação nativa antes de excluir notificação individual, alinhando ao padrão já usado pela limpeza em lote — UC-44 v1.0.1). Os 4 movidos de "Aberto" para "Corrigido e Documentado" diretamente. Resumo Executivo (Seção 1) ajustado: Baixa — Aberto 70→66, Corrigido e Documentado 12→16 (Total 83, inalterado); Total geral — Aberto 128→124, Corrigido e Documentado 20→24 (Total 152, inalterado). Itens atualizados nas Seções 3 e 6. |
| 3.1 | 18/07/2026 | uc-issues-tracker (Modo B) | Registrado novo achado, `UC-32-RN-06b`, descoberto e corrigido no mesmo ciclo (durante a validação manual do RN-06) — mesmo precedente já usado para `UC-29-RN-07` e `UC-22-RN-02`/`UC-22-RNF-05`: o filtro de categoria da tela de **listagem** de produtos master (`admin/products/page.tsx`) tinha a mesma lacuna original do RN-06 (sem opção "Sem categoria"), não coberta pela correção do formulário de edição (commit `2ddebd6`). Corrigido no commit `199d09b` (`fix(admin): adiciona filtro "Sem categoria" na listagem de produtos master`) — verificado por leitura direta do código-fonte nesta execução. `uml-use-case-writer` já atualizou UC-32 para **v1.0.2**, documentando o achado como sub-nota do RN-06 (não como RN novo). Nasce diretamente como "Corrigido e Documentado" — nunca esteve "Aberto", sem linha própria nas Seções 2/3 e **sem impacto nos contadores da Seção 1** (Resumo Executivo inalterado nesta versão). Item registrado apenas na Seção 6, para rastreabilidade. |
| 3.2 | 18/07/2026 | uc-issues-tracker (Modo B) | Dois itens de Alta severidade verificados corrigidos em código, ambos no commit único `467f462` (`fix: dois itens de alta severidade (UC-50, UC-39)`), com os UCs de origem já atualizados pelo `uml-use-case-writer`: `UC-50-RN-03` (filtro `?filter=expired`/`?filter=low-stock` do Dashboard não reconhecido por `InventoryView.applyFilter` — UC-50 v1.1) e `UC-39-RN-03` (usuário criado pelo System Admin não recebia `requirePasswordChange: true` — UC-39 v1.2). Ambos movidos de "Aberto" para "Corrigido e Documentado" (verificado por leitura direta de `InventoryView.tsx`, `dashboard/page.tsx` e `api/users/create/route.ts` nesta execução). **Tratamento diferenciado para um terceiro item, `UC-46-RN-03`/`UC-46-RN-04`:** UC-46 foi atualizado para v1.1, mas apenas para registrar que uma **decisão de produto já foi tomada** (remover `/clinic/consultant/transfer` e substituir por um fluxo novo de convite/transferência, spec completa e pronta em `ONLY_FOR_DEVS/TO_DO/FEAT-unificacao-vinculo-transferencia-consultor.md` v1.1) — **nenhuma linha de código foi alterada** (confirmado por leitura de `clinic/consultant/transfer/page.tsx`, comportamento as-is inalterado). Os dois itens movidos de "Aberto" para "Em Correção" (não "Corrigido e Documentado"), mesmo tratamento histórico já usado para UC-29/UC-34/UC-36/UC-43 quando a decisão precede a implementação; a linha `UC-46-Decisão` foi removida da Seção 4 por deixar de ser uma decisão em aberto — passa a ser uma implementação pendente de decisão já tomada, rastreada na Seção 6. Resumo Executivo (Seção 1) ajustado: Alta — Aberto 21→18, Em Correção 0→1, Corrigido e Documentado 2→4 (Total 24, inalterado); Média — Aberto 37→36, Em Correção 0→1 (Total 39, inalterado); Total geral — Aberto 124→120, Em Correção 0→2, Corrigido e Documentado 24→26 (Total 152, inalterado). Decisões de produto pendentes 11→10 (9 UC + 1 ADR). Itens atualizados nas Seções 1, 2, 3, 4 e 6. |
| 3.3 | 19/07/2026 | uc-issues-tracker (Modo B) | Correção de segurança verificada em código/documentação: `UC-02-RNF-01` (Alta) movido de "Aberto" para "Corrigido e Documentado" — `POST /api/access-requests/[id]/approve` não validava Bearer token, corrigido exigindo `Authorization: Bearer {idToken}` + `adminAuth.verifyIdToken` + checagem de `is_system_admin` (commit `33c5ef3`); `uml-use-case-writer` já atualizou UC-02 para v2.1. **Achado gêmeo registrado no mesmo ciclo:** `UC-03-RNF-04`, descoberto durante a investigação de `UC-02-RNF-01` e corrigido no mesmo commit `33c5ef3` — a rota irmã `POST /api/access-requests/[id]/reject` (não usada pela UI real, protegida apenas indiretamente pela regra do Firestore) tinha o mesmo problema de ausência de autenticação; corrigida com o mesmo padrão. Classificado como Média (não Alta) por impacto menor que a aprovação forjada e por nunca ter tido vetor de exploração pela UI. Mesmo precedente já usado neste mapa para achados nascidos e corrigidos no mesmo ciclo (`UC-29-RN-07`, `UC-22-RN-02`/`UC-22-RNF-05`, `UC-32-RN-06b`): nunca esteve "Aberto", registrado apenas na Seção 6, **sem impacto nos contadores da Seção 1**. `uml-use-case-writer` já atualizou UC-03 para v1.3 (nova RNF-04, já nascendo `[CORRIGIDO]`). Resumo Executivo (Seção 1) ajustado: Alta — Aberto 18→17, Corrigido e Documentado 4→5 (Total 24, inalterado); Total geral — Aberto 120→119, Corrigido e Documentado 26→27 (Total 152, inalterado). Itens atualizados nas Seções 1, 2 e 6. **Nota retrospectiva (ver v3.4):** esta versão (3.3) foi produzida em `gscandelari_setup`, em paralelo a uma outra v3.3 (commit `339fa86`, branch `fix/uc40-users-list-rule`) que corrigia UC-40/UC-44 a partir da mesma base v3.2 — o merge das duas branches (`ad5e692`) manteve esta cópia como placeholder, deixando-a temporariamente desatualizada para UC-40/UC-44 até a reconciliação da v3.4. |
| 3.4 | 19/07/2026 | uc-issues-tracker (Modo A — auditoria completa) | **Reconstrução completa do mapa a partir da releitura dos 50 UCs**, pedida explicitamente pelo usuário após identificar que o merge de `fix/uc40-users-list-rule` (PR aberta a partir de `339fa86`) com `gscandelari_setup` (já contendo a v3.3 de UC-02/UC-03, PR #231) havia mantido como placeholder a v3.3 de UC-02/UC-03, desatualizada para **UC-40/UC-44** — mesmo padrão de reconciliação já usado para UC-22 (v2.8→v2.9) e para o par UC-29/UC-34 (v1.9→v2.1). Confirmado por leitura de `git log`/`git show` que o commit `339fa86` (branch `fix/uc40-users-list-rule`) já continha a resolução completa e correta para UC-40/UC-44 — reaproveitada aqui. Todos os 50 UCs relidos nesta auditoria (cabeçalho Status/Versão de cada um conferido via leitura direta; busca por `[CORRIGIDO]`/`[RESOLVIDO]`/`[Corrigido]`/`[Resolvido]` em todos os 50 arquivos, com cada ocorrência comparada contra o que já constava neste mapa) — **nenhuma divergência de conteúdo além de UC-40/UC-44** encontrada; `UC-02` (v2.1) e `UC-03` (v1.3) já estavam corretamente refletidos no placeholder mantido pelo merge. Três itens de Alta severidade, todos do commit único `eb12c91` (`fix: dois itens de alta severidade (UC-40, UC-44)`, confirmado via `git log`/`git show` nesta execução) e já documentados pelo `uml-use-case-writer` (UC-40 v1.1, UC-44 v1.1), movidos de "Aberto" para "Corrigido e Documentado": `UC-40-RN-04` (regra `allow list` do Firestore para `users/{userId}` passou a exigir `resource.data.tenant_id == request.auth.token.tenant_id`), `UC-44-RN-01` (`NotificationBell` passou a exibir toast de erro para as quatro ações de escrita, com a ressalva de que falhas do listener `onSnapshot` permanecem fora do escopo) e `UC-44-RN-02` (botões de exclusão passam a só renderizar para `clinic_admin`, alinhando a UI à regra do Firestore). **Verificação aritmética realizada:** soma das linhas das Seções 2 a 5 conferida contra os totais da Seção 1 (ver nota completa no cabeçalho da Seção 1) — sem divergências. Resumo Executivo (Seção 1) ajustado a partir da v3.3 mantida pelo merge: Alta — Aberto 17→14, Corrigido e Documentado 5→8 (Total 24, inalterado); Total geral — Aberto 119→116, Corrigido e Documentado 27→30 (Total 152, inalterado). Itens atualizados nas Seções 1, 2 e 6. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado com os novos números. |
| 3.5 | 19/07/2026 | uc-issues-tracker (Modo A — auditoria completa) | **Reconstrução completa do mapa a partir da releitura dos 50 UCs**, pedida explicitamente pelo usuário após identificar que uma terceira branch envolvida no mesmo ciclo de merges — commit `91eb37e` (`fix: dois itens de alta severidade (UC-21, UC-13)`) — também não estava refletida no placeholder mantido pela v3.4, mesmo padrão de reconciliação já usado para UC-22 (v2.8→v2.9) e para UC-40/UC-44 (v3.3→v3.4). Todos os 50 UCs relidos nesta auditoria (cabeçalho Status/Versão de cada um conferido via leitura direta; histórico de versões completo lido para todo UC com versão acima de `1.0` — 25 arquivos; busca por `[CORRIGIDO]`/`[RESOLVIDO]`/`[Corrigido]`/`[Resolvido]` nos 50 arquivos, cada ocorrência comparada contra este mapa) — **nenhuma divergência de conteúdo além de UC-21/UC-13** encontrada; `UC-02`, `UC-03`, `UC-40` e `UC-44` já estavam corretamente refletidos no placeholder mantido pela v3.4. Dois itens de Alta severidade, ambos do commit único `91eb37e` (confirmado via `git log -1 --stat 91eb37e` nesta execução — toca `clinic/inventory/[id]/page.tsx` e `api/tenants/create/route.ts`) e já documentados pelo `uml-use-case-writer` (UC-21 v1.1, UC-13 v1.1), movidos de "Aberto" para "Corrigido e Documentado": `UC-21-RN-05` (senha inicial do admin de clínica passa a exigir troca obrigatória — `requirePasswordChange: true`) e `UC-13-RN-07` (verificação de reservas ativas antes de desativar item de estoque deixa de ser fail-open e passa a bloquear a desativação em caso de falha). **Verificação aritmética realizada:** soma das linhas das Seções 2 a 5 conferida contra os totais da Seção 1 (ver nota completa no cabeçalho da Seção 1) — sem divergências. Resumo Executivo (Seção 1) ajustado a partir da v3.4: Alta — Aberto 14→12, Corrigido e Documentado 8→10 (Total 24, inalterado); Total geral — Aberto 116→114, Corrigido e Documentado 30→32 (Total 152, inalterado). Itens atualizados nas Seções 1, 2 e 6. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado com os novos números. |
| 3.6 | 20/07/2026 | uc-issues-tracker (Modo B) | Três itens de Alta severidade verificados corrigidos em código, todos no commit único `f6e9161` (`fix: tres itens de alta severidade (UC-32, UC-04, UC-39)`), com os UCs de origem já atualizados pelo `uml-use-case-writer`: `UC-32-RN-01` (`fragmentacaoAlterada` em `admin/products/[id]/page.tsx` — checagem de "produto em uso" deixa de ser disparada incondicionalmente — UC-32 v1.0.3), `UC-04-RN-09` (ex-`Q3`, renumerado para Regra de Negócio — `ProtectedRoute` passa a verificar `requirePasswordChange` para sessões já autenticadas navegando direto — UC-04 v1.1.3; sub-parte de "clínica suspensa" do achado original confirmada já coberta por `SuspensionInterceptor.tsx`, não corrigida por não ser mais um gap real) e `UC-39-RN-02` (decisão do PO pela opção "a" — texto do diálogo "Adicionar Novo Usuário" corrigido para não prometer envio de credenciais por e-mail — UC-39 v1.2.1; `UC-39-Decisão` removida da Seção 4). Todos os três movidos de "Aberto" para "Corrigido e Documentado" diretamente. Branch `fix/uc32-uc04-uc39-alta-prioridade` verificada sem divergência em relação a `develop` antes da atualização (`git diff develop f6e9161 -- _MAPA-DE-BUGS-E-MELHORIAS.md` vazio) — nenhuma reconciliação necessária. Resumo Executivo (Seção 1) ajustado: Alta — Aberto 12→9, Corrigido e Documentado 10→13 (Total 24, inalterado); Total geral — Aberto 114→111, Corrigido e Documentado 32→35 (Total 152, inalterado). Decisões de produto pendentes 10→9 (8 UC + 1 ADR). Itens atualizados nas Seções 1, 2 e 6. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado com os novos números. |
| 3.7 | 20/07/2026 | uc-issues-tracker (Modo A — auditoria completa) | **Reconstrução completa do mapa a partir da releitura dos 50 UCs**, pedida explicitamente pelo usuário após o merge de `fix/uc09-uc11-uc42-alta-prioridade` (criada a partir de `develop`, antes da PR #237 ser mesclada em `gscandelari_setup`) com `gscandelari_setup` (já contendo a v3.6 deste mapa, com os fixes de UC-32/UC-04/UC-39 da PR #237) — o conflito de merge foi resolvido com `git checkout --theirs` como placeholder tanto para este mapa quanto para o `README.md`, deixando ambos temporariamente sem as 4 correções da branch de fix (mesmo padrão de "branches divergentes" já visto e reconciliado neste mapa para UC-22, UC-29/UC-34, UC-36, UC-40/UC-44 e UC-21/UC-13). Todos os 50 UCs relidos nesta auditoria (cabeçalho Status/Versão de cada um conferido via leitura direta; 23 arquivos com marcador `CORRIGIDO`/`RESOLVIDO`/`[Corrigido]`/`[Resolvido]` lidos/grepados por completo e comparados um a um contra o que já constava neste mapa) — **divergência confirmada, restrita a 4 itens, todos do commit único `16877f1`** (`fix: tres itens de alta severidade (UC-09, UC-11, UC-42)`, documentado nos UCs de origem pelo commit `182c973`): `UC-09-RN-02` e `UC-09-RN-03` (critério de "documento pendente" unificado entre `usePendingTerms`/`TermsInterceptor` e as páginas de aceite, eliminando o loop de redirecionamento — UC-09 v1.4), `UC-11-RN-07` (`handleSaveNF` passou a usar `writeBatch`, gravação atômica de `nf_imports` + itens de inventário, mesmo padrão de UC-10 — UC-11 v1.1) e `UC-42-RN-02` (`checkExpiredProducts` passou a persistir de fato a notificação de produto vencido via novo helper `createExpiredProductNotification` — UC-42 v1.3). Todos os 4 movidos de "Aberto" para "Corrigido e Documentado". **Nenhuma outra divergência de conteúdo encontrada** nos 50 UCs — os fixes da PR #237 já estavam corretos no placeholder v3.6; as variações adicionais de versão em UC-29 (v1.2.2) e UC-36 (v1.1.1) correspondem à formalização do achado já rastreado `UC-29-RN-09 / UC-36-RN-10` (sem mudança de status); UC-01, UC-05, UC-15 e UC-23 tiveram apenas atualizações pontuais de cross-reference, sem achado novo. **Verificação aritmética completa realizada:** contagem direta de todas as linhas das Seções 2 a 5 confere exatamente com os totais da Seção 1 (Crítica 6; Alta 24; Média 39, recontada por módulo; Baixa 83, recontada por módulo; Total 152; Seção 4 com 9 linhas — 8 UC + 1 ADR; Seção 5 com 16 itens de código morto) — nenhuma divergência aritmética encontrada. Resumo Executivo (Seção 1) ajustado a partir da v3.6: Alta — Aberto 9→5, Corrigido e Documentado 13→17 (Total 24, inalterado); Total geral — Aberto 111→107, Corrigido e Documentado 35→39 (Total 152, inalterado). Itens atualizados nas Seções 1, 2 e 6. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado com os novos números. |
| 3.9 | 20/07/2026 | uc-issues-tracker | **Reconciliação da Seção 7.1** após a elicitação real do que era a reserva "UC-51 (Módulo Procedimentos)": ao investigar para escrever o UC, o `uml-use-case-writer` e o usuário constataram que o "Módulo 02 — Procedimentos" da landing já é, na prática, a tela `/clinic/requests` (UC-16/UC-17) — gap real bem menor que o presumido na v3.8. Decisões finais: (a) "tipo de procedimento categorizado" — não implementar, movido para correção de texto da landing (Seção 7.2, 3→4 itens); (b) identificador de sessão `SES-XXXXXX` — decisão já tomada e documentada como RN-08 em UC-19 (v1.0→v1.1), implementação de código pendente; (c) painel "Histórico do lote" — remanejado para o escopo do novo UC-52 (ex-UC-53). Nenhum arquivo `UC-51-*.md` foi criado; a numeração das reservas seguintes foi renumerada sem lacunas: UC-52→UC-51 (Investimentos & ROI, agora sem dependência de um "UC-51 Procedimentos" separado), UC-53→UC-52 (Expansão de Relatórios Gerenciais, escopo agora incluindo "Histórico do lote"), UC-54→UC-53 (Motor de Recomendação/Projeção de Reposição), UC-55→UC-54 (Trilha de Auditoria Completa), UC-56→UC-55 (Backup Geográfico Automatizado). Confirmado, por conferência da listagem de arquivos `UC-*.md` existentes, que a nova reserva UC-51 a UC-55 não conflita com nenhum UC já escrito. **Sinalizado para o `uml-use-case-writer`:** UC-19 v1.1 (RN-08, coluna "Observação") ainda cita a numeração antiga ("remanejado para UC-53") para o painel de histórico do lote — precisa ser atualizado para "UC-52" numa próxima revisão do UC. **Resumo Executivo (Seção 1) sem alteração na tabela de severidade** — os 12 itens da Seção 7 permanecem 12 no total, apenas redistribuídos (5 a implementar, antes 6; 4 correção de landing, antes 3; 3 adiados, inalterado). `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data e bullet sobre os gaps da landing page atualizados. |
| 3.8 | 20/07/2026 | uc-issues-tracker | Criada a nova **Seção 7 — "Gaps: Landing Page vs. Sistema Real — Decisões de Produto"** (Histórico de Versões renumerado de Seção 7 para **Seção 8**), a partir de uma auditoria comparando as promessas comerciais da landing page (`public/landing/*.jsx`) contra o sistema real — trazida e já decidida item a item pelo usuário/orquestrador, **não originada de nenhum UC existente**, diferente de todos os demais achados deste mapa. 7.1: **6 itens com decisão de implementar**, numeração de UC reservada sequencialmente sem lacunas a partir do maior UC existente (UC-50) — **UC-51** (Módulo Procedimentos), **UC-52** (Módulo Investimentos & ROI, depende de UC-51), **UC-53** (Expansão de Relatórios Gerenciais), **UC-54** (Motor de Recomendação/Projeção de Reposição), **UC-55** (Trilha de Auditoria Completa) e **UC-56** (Backup Geográfico Automatizado — sinalizado como trabalho de infraestrutura/DevOps). Nenhum dos 6 UCs foi escrito nesta execução — apenas a numeração foi reservada; nem `uml-use-case-writer` nem `dev-task-manager` foram acionados. 7.2: **3 itens com decisão de corrigir apenas o texto da landing page** (baixa prioridade, sem UC): alertas "60/30/15 dias", promessa de multi-unidade/multi-sede, onboarding "via planilha" (contradiz decisão já documentada no `CLAUDE.md`). 7.3: **3 itens com decisão adiada**, registrados apenas como pendência em aberto: alegações específicas de criptografia (rotação de chaves, restore testado), app nativo iOS/Android + offline (Capacitor), integrações/API REST + conectores nativos. **Resumo Executivo (Seção 1) sem alteração na tabela de severidade** (nenhum destes 12 itens é um bug classificável por severidade, mesmo tratamento das Seções 4 e 5) — apenas o parágrafo explicativo logo abaixo da tabela foi estendido para citar os 12 novos itens. Confirmado, por conferência da listagem de arquivos `UC-*.md` existentes (`UC-01` a `UC-50`), que a reserva `UC-51` a `UC-56` não conflita com nenhum UC já escrito. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.8 e novo bullet informativo sobre os 12 gaps da landing page adicionado — a tabela de severidade do README não mudou, pois os contadores por severidade do mapa permaneceram os mesmos desta versão. |
| 3.12 | 21/07/2026 | uc-issues-tracker (Modo B) | **UC-52 deixou de ser apenas um número reservado**: o `uml-use-case-writer` escreveu e o usuário aprovou o arquivo formal [UC-52](UC-52-consultar-projecao-de-reposicao-de-estoque.md), v1.0, Status: Aprovado — "Consultar Projeção de Reposição de Estoque" (projeção de data estimada de esgotamento de estoque por `codigo_produto`, agregada como em UC-15; taxa de consumo calculada a partir de `Solicitacao concluida`, mesma fonte de UC-47; janela de histórico em cascata 90→60→30 dias, RN-03/RN-04, critério de suficiência de dados a validar tecnicamente, pendência não bloqueante; cálculo sob demanda ao carregar a tela, sem cron novo, RN-07; Consultor Rennova vinculado vê a projeção completa das clínicas vinculadas, achado confirmado de que a regra genérica `tenants/{tenantId}/{document=**}` já cobre essa leitura sem exigir mudança em `firestore.rules`, RN-08; "sugestão de quantidade a pedir" — Módulo 05 da landing — fora de escopo, RN-10); código ainda não implementado. Diferente do UC-51, o UC-52 não passou por nenhuma rodada de descarte/renumeração — a reserva permaneceu estável desde a v3.10. Linha do UC-52 atualizada na Seção 7.1 (tabela e notas introdutórias) para refletir o novo status "Escrito e Aprovado — aguardando priorização/planejamento de implementação pelo `dev-task-manager`", mesmo padrão já usado para o UC-51 na v3.11. UC-53 e UC-54 inalterados (ainda apenas reservados). Verificação de código não realizada (item puramente documental, sem correção de código a verificar). **Resumo Executivo (Seção 1) sem alteração na tabela de severidade** nem no total de itens da Seção 7 (permanece 12: 4 a implementar/5 correção de landing/3 adiados) — apenas o estado do item UC-52 mudou de "reservado" para "escrito e aprovado". `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.12, número de UCs mapeados ajustado para "UC-01 a UC-52" e bullet sobre os gaps da landing page ajustado para citar que UC-51 e UC-52 já foram escritos e aprovados. |
| 3.11 | 20/07/2026 | uc-issues-tracker (Modo B) | **UC-51 deixou de ser apenas um número reservado**: o `uml-use-case-writer` escreveu e o usuário aprovou o arquivo formal [UC-51](UC-51-gerar-relatorios-gerenciais-avancados-e-custeio-por-procedimento.md), v1.1, Status: Aprovado — "Expansão de Relatórios Gerenciais e Custeio por Procedimento" (4 capacidades: Custo por Procedimento/RN-01-03, Histórico do Lote/RN-05, Fechamento Executivo Mensal e Mix de Produtos por Trimestre, exportáveis em PDF; margem por categoria fora de escopo/RN-04; ator único `clinic_admin`; biblioteca de PDF ainda não decidida, RNF-01, pendência não bloqueante); código ainda não implementado. Linha do UC-51 atualizada na Seção 7.1 (tabela e notas introdutórias) para refletir o novo status "Escrito e Aprovado — aguardando priorização/planejamento de implementação pelo `dev-task-manager`", em vez de "reservado/a implementar". Adicionada nota curta em 7.1 esclarecendo que as notas históricas de reconciliação (v3.9/v3.10, sobre os dois "UC-51" descartados) permanecem válidas e não precisaram de correção — apenas deixaram de ser a última palavra sobre o estado do item. UC-52 a UC-54 inalterados (ainda apenas reservados). Verificação de código não realizada (item puramente documental, sem correção de código a verificar). **Resumo Executivo (Seção 1) sem alteração na tabela de severidade** nem no total de itens da Seção 7 (permanece 12: 4 a implementar/5 correção de landing/3 adiados) — apenas o estado do item UC-51 mudou de "reservado" para "escrito e aprovado". `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.11, número de UCs mapeados ajustado para "UC-01 a UC-51" e bullet sobre os gaps da landing page ajustado para citar que o UC-51 já foi escrito e aprovado. |
| 3.10 | 20/07/2026 | uc-issues-tracker | **Segunda reconciliação consecutiva da Seção 7.1**, agora com o que era a reserva "UC-51 (Investimentos & ROI)": ao investigar para escrever este UC, o `uml-use-case-writer` e o usuário decidiram, em conjunto: (1) FIFO — o sistema deve calcular custo médio ponderado por ordem de entrada (FIFO real) quando o mesmo produto tiver múltiplos lotes consumidos num período, sem alterar a escolha manual de lote de UC-16/UC-17; (2) margem por categoria — decisão de **não implementar** (nenhum campo de valor cobrado do paciente/valor de tabela existe ou será criado agora), movida para item de correção de texto da landing (Seção 7.2, 4→5 itens); (3) o motor de custeio (FIFO ponderado, custo real por procedimento, "ticket médio de custo", sem margem) não tem tela/ator/gatilho próprios — seguindo o mesmo precedente já usado para o UC-51 anterior (Módulo Procedimentos → UC-19/RN-08, v3.9), o `uml-use-case-writer` decidiu **não criar um segundo arquivo `UC-51-*.md`** — o motor de custeio passa a ser documentado como Regras de Negócio dentro do que hoje é o novo UC-51 ("Expansão de Relatórios Gerenciais", ex-UC-52); (4) ator confirmado para esse UC: `clinic_admin` apenas. **Nenhum arquivo `UC-51-*.md` foi criado** (segunda vez consecutiva). **Renumeração sequencial sem lacunas aplicada** às reservas seguintes: UC-52→**UC-51** (Expansão de Relatórios Gerenciais, escopo agora também incluindo o motor de custeio FIFO ponderado e o "ticket médio de custo", além do "Histórico do lote" já absorvido na v3.9), UC-53→**UC-52** (Motor de Recomendação/Projeção de Reposição), UC-54→**UC-53** (Trilha de Auditoria Completa), UC-55→**UC-54** (Backup Geográfico Automatizado). Nenhum dos 4 tem arquivo `.md` próprio ainda; confirmado, por conferência da listagem de arquivos `UC-*.md` existentes, que a nova reserva UC-51 a UC-54 não conflita com nenhum UC já escrito (`UC-01` a `UC-50`). Adicionado à Seção 7.2 o item "Módulo 03 — Investimentos & ROI: margem por categoria" (landing-copy fix). **Sinalização registrada para o `uml-use-case-writer`:** quando o novo UC-51 (ex-UC-52) for escrito/atualizado, deve citar que o motor de custeio foi cogitado como UC próprio duas vezes e absorvido por não ter ator/gatilho/tela próprios; e o próprio UC-19 v1.1 (RN-08) — já sinalizado na v3.9 com a numeração antiga "UC-53" para o painel de histórico do lote — precisa ser atualizado novamente, desta vez para "UC-51" (não mais "UC-52"), já que a numeração mudou de novo nesta versão. **Resumo Executivo (Seção 1) sem alteração na tabela de severidade** — os 12 itens da Seção 7 permanecem 12 no total, apenas redistribuídos (4 a implementar, antes 5; 5 correção de landing, antes 4; 3 adiados, inalterado). `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.10 e bullet sobre os gaps da landing page ajustado para citar a nova numeração UC-51 a UC-54 e o segundo descarte/absorção consecutivo de "UC-51". |
| 3.13 | 22/07/2026 | uc-issues-tracker (Modo B) | **UC-53 deixou de ser apenas um número reservado**: o `uml-use-case-writer` escreveu e o usuário aprovou o arquivo formal [UC-53](UC-53-consultar-e-exportar-trilha-de-auditoria.md), v1.0, Status: Aprovado — "Consultar e Exportar Trilha de Auditoria" (`system_admin` visão cross-tenant e `clinic_admin` visão restrita ao próprio tenant consultam e exportam, CSV e PDF, um histórico unificado de escritas administrativas sensíveis — criar/editar/ativar/desativar/suspender/mudar papel/definir senha/excluir — sobre Usuários e Consultores, Clínicas, Produtos Master, Documentos Legais e Configurações Globais, somado ao `inventory_activity` já existente e já gravado hoje mas nunca exposto em nenhuma tela; rastreia apenas escritas, nunca leituras/visualizações; nova coleção `audit_log` proposta; log explícito rota a rota, mesmo padrão de `writeActivityLogs`, sem Cloud Function `onWrite` automática; retenção indefinida, sem TTL; trilha não retroativa); código ainda não implementado. Diferente do UC-51, o UC-53 não passou por nenhuma rodada de descarte/renumeração — a reserva permaneceu estável desde a v3.10. Linha do UC-53 atualizada na Seção 7.1 (tabela e notas introdutórias) para refletir o novo status "Escrito e Aprovado — aguardando priorização/planejamento de implementação pelo `dev-task-manager`", mesmo padrão já usado para o UC-51 na v3.11 e para o UC-52 na v3.12. UC-54 inalterado (ainda apenas reservado). Verificação de código não realizada (item puramente documental, sem correção de código a verificar). **Resumo Executivo (Seção 1) sem alteração na tabela de severidade** nem no total de itens da Seção 7 (permanece 12: 4 a implementar/5 correção de landing/3 adiados) — apenas o estado do item UC-53 mudou de "reservado" para "escrito e aprovado". `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.13, número de UCs mapeados ajustado para "UC-01 a UC-53" e bullet sobre os gaps da landing page ajustado para citar que UC-51, UC-52 e UC-53 já foram escritos e aprovados. |
| 3.15 | 24/07/2026 | uc-issues-tracker (Modo B) | Cinco itens de Alta severidade verificados corrigidos em código, todos no commit único `66c75fa` (`fix: cinco itens de alta severidade (UC-21, UC-28, UC-35)`), com os UCs de origem já atualizados pelo `uml-use-case-writer`: `UC-21-RN-03` (senha removida do e-mail de boas-vindas do admin de clínica — template padrão e `getPreviewEmail()`, com defesa contra reintrodução manual do placeholder; texto de ajuda da UI corrigido — UC-21 v1.2), `UC-28-RN-04` (`generateConsultantWelcomeEmail` perde o parâmetro `tempPassword` — UC-28 v1.1) e `UC-35-RN-04/05/06` (`ProtectedRoute.tsx` passa a ler `maintenance_mode` e redirecionar para `/maintenance`; `POST /api/access-requests` passa a rejeitar com 403 quando `registration_enabled` é falso, ressalva de validação apenas server-side registrada como melhoria futura; `useSessionTimeout.ts` passa a ler `session_timeout_minutes` com fallback de 15 minutos — UC-35 v2.0, major bump). Todos os 5 movidos de "Aberto" para "Corrigido e Documentado". **Efeito colateral em UC-04:** o mesmo commit fecha `UC-04-RN-07` (achado cross-referenciado, nunca catalogado como item próprio neste mapa — confirmado por releitura nesta execução) — UC-04 atualizado para v1.1.4. Branch `fix/uc21-uc28-uc35-alta-prioridade` verificada sem divergência em relação a `develop` (`git status` limpo além dos arquivos desta sessão) — nenhuma reconciliação necessária. **Com esta atualização, a coluna "Aberto" da severidade Alta zera** — não restam itens Abertos de severidade Crítica nem Alta neste mapa. Resumo Executivo (Seção 1) ajustado: Alta — Aberto 5→0, Corrigido e Documentado 17→22 (Total 24, inalterado); Total geral — Aberto 107→102, Corrigido e Documentado 39→44 (Total 152, inalterado). Itens atualizados nas Seções 1, 2 e 6. `README.md` (`## Roadmap e Backlog Técnico`) sincronizado com os novos números. |
| 3.14 | 24/07/2026 | uc-issues-tracker (Modo B) | **Último item reservado da Seção 7.1 fechado — não como UC, mas como ADR.** O item antes reservado como "UC-54 — Backup Geográfico Automatizado" foi descartado como caso de uso formal: na elicitação com o usuário, ficou decidido que ele deveria ser documentado como um ADR (Architecture Decision Record), por não envolver ator humano nem tela — é um processo de infraestrutura agendado (backup automatizado do Firestore + Cloud Storage) — e por restarem apenas decisões técnicas de arquitetura a formalizar, mesmo perfil do único outro ADR do projeto (`ADR-automacao-qa-playwright-firebase-emulator.md`, que permanece pausado). Arquivo criado: `ONLY_FOR_DEVS/TO_DO/ADR-backup-geografico-automatizado.md`, v1.1, Status: Aprovado — decisões de arquitetura fechadas (mecanismo `gcloud firestore export` + Cloud Scheduler/Cloud Function; réplica de `/danfe/{tenantId}/...` via Cloud Storage Transfer Service; região secundária `southamerica-west1`; retenção de 90 dias via lifecycle rule; restore mensal híbrido — automação + validação humana; custo recorrente de GCP aceito explicitamente pelo usuário, US$ 5-20/mês estimados); implementação de código não priorizada, conforme decisão explícita do usuário de "documentar agora, implementar depois". A numeração "UC-54" não será reaproveitada; a linha correspondente na tabela da Seção 7.1 passa de "reservado" para "Descartado como UC — documentado como ADR". Não foi adicionada nova linha na Seção 4 (Decisões de Produto Pendentes): diferente do `ADR-QA-AUTOMATION` (ainda pausado/sem decisão), este ADR já nasce com todas as decisões técnicas fechadas — apenas uma nota cruzada foi registrada em 7.1. **Com este item, a Seção 7.1 está 100% documentada** — nenhum dos 6 itens originalmente reservados (UC-51 a UC-56 na numeração da v3.8) resta apenas como número sem nenhum documento formal. Verificação de código não realizada (item puramente documental, ADR aprovado sem implementação reivindicada). **Resumo Executivo (Seção 1) sem alteração na tabela de severidade** nem no total de itens da Seção 7 (permanece 12: 4 a implementar/5 correção de landing/3 adiados) — apenas o estado do item ex-UC-54 mudou de "reservado" para "descartado como UC, documentado como ADR". `README.md` (`## Roadmap e Backlog Técnico`) sincronizado: linha de versão/data atualizada para v3.14 e bullet sobre os gaps da landing page ajustado para destacar que a Seção 7.1 está agora 100% documentada (UC-51, UC-52, UC-53 escritos e aprovados + 1 ADR aprovado para o item de backup geográfico). |
